# Workflow разработки: Frontend (Figma Make) + Backend (локально)

## 🎯 Проблема

- **Frontend** разрабатывается в **Figma Make** → создает свой репозиторий на GitHub
- **Backend** разрабатывается **локально** в этом репозитории
- Нужен способ **синхронизации** изменений между двумя репозиториями

---

## ✅ Рекомендуемый Workflow

### Вариант 1: Figma Make как основной источник (РЕКОМЕНДУЕТСЯ) ⭐

**Принцип:** Все изменения фронтенда делаются в Figma Make, затем синхронизируются в локальный проект.

#### Шаги:

1. **Разработка в Figma Make:**
   - Делаете изменения в Figma Make
   - Коммитите в репозиторий Figma Make: `https://github.com/Prygunov-Andrei/Ankiflashcardgenerator.git`

2. **Синхронизация в локальный проект:**
   ```bash
   # Запускаете скрипт синхронизации (я создам его)
   ./scripts/sync_frontend.sh
   ```
   Скрипт:
   - Клонирует/обновляет репозиторий Figma Make
   - Копирует изменения в `frontend/`
   - Показывает diff (что изменилось)
   - Предлагает закоммитить изменения

3. **Локальные изменения фронтенда (если нужны):**
   - Можете делать изменения прямо в `frontend/`
   - Но **важно:** перед следующим коммитом в Figma Make нужно:
     - Либо откатить локальные изменения
     - Либо вручную перенести их в Figma Make
     - Либо использовать merge (см. Вариант 2)

#### Плюсы:
- ✅ Простой workflow
- ✅ Figma Make остается источником истины
- ✅ Легко отслеживать изменения

#### Минусы:
- ⚠️ Локальные изменения фронтенда могут потеряться при синхронизации
- ⚠️ Нужно помнить о синхронизации

---

### Вариант 2: Git Submodule (для продвинутых)

**Принцип:** Использовать репозиторий Figma Make как Git submodule.

#### Настройка:

```bash
# Удалить текущий frontend/ (если есть)
rm -rf frontend

# Добавить как submodule
git submodule add https://github.com/Prygunov-Andrei/Ankiflashcardgenerator.git frontend
```

#### Работа:

1. **Обновление после коммита в Figma Make:**
   ```bash
   git submodule update --remote frontend
   git add frontend
   git commit -m "Update frontend from Figma Make"
   ```

2. **Локальные изменения:**
   ```bash
   cd frontend
   # Делаете изменения
   git add .
   git commit -m "Local frontend changes"
   git push origin main  # Пушите в Figma Make репозиторий
   ```

#### Плюсы:
- ✅ Автоматическое отслеживание версий
- ✅ Можно делать локальные изменения и пушить в Figma Make репозиторий
- ✅ Git управляет синхронизацией

#### Минусы:
- ⚠️ Сложнее для новичков
- ⚠️ Нужно понимать работу с submodules

---

### Вариант 3: Ручная синхронизация (простой, но неудобный)

**Принцип:** Каждый раз вручную клонировать и копировать.

#### Шаги:

1. После коммита в Figma Make:
   ```bash
   cd /tmp
   git clone https://github.com/Prygunov-Andrei/Ankiflashcardgenerator.git figma-temp
   cd /Users/andrei_prygunov/obsidian/deutsch/anki_cards
   rm -rf frontend
   cp -r /tmp/figma-temp frontend
   rm -rf /tmp/figma-temp
   git add frontend
   git commit -m "Sync frontend from Figma Make"
   ```

#### Плюсы:
- ✅ Полный контроль
- ✅ Просто понять

#### Минусы:
- ❌ Очень неудобно
- ❌ Легко забыть синхронизировать
- ❌ Локальные изменения теряются

---

## 🛠️ Рекомендуемое решение: Скрипт синхронизации

Создам скрипт `scripts/sync_frontend.sh`, который:

1. **Проверяет изменения** в репозитории Figma Make
2. **Клонирует/обновляет** репозиторий
3. **Копирует** изменения в `frontend/`
4. **Показывает diff** (что изменилось)
5. **Предлагает закоммитить** изменения

### Использование:

```bash
# Простая синхронизация
./scripts/sync_frontend.sh

# С автоматическим коммитом
./scripts/sync_frontend.sh --auto-commit

# С просмотром diff перед коммитом
./scripts/sync_frontend.sh --review
```

---

## 📋 Рекомендуемый Workflow (пошагово)

### Ежедневная работа:

1. **Утро:**
   ```bash
   # Синхронизируете фронтенд (если были изменения в Figma Make)
   ./scripts/sync_frontend.sh
   
   # Запускаете бэкенд
   ./start.sh
   
   # Запускаете ngrok (в отдельном терминале)
   ngrok http 8000
   ```

2. **Разработка:**
   - **Frontend:** Работаете в Figma Make, коммитите в Figma Make репозиторий
   - **Backend:** Работаете локально, коммитите в этот репозиторий

3. **Перед коммитом бэкенда:**
   ```bash
   # Синхронизируете фронтенд (если были изменения)
   ./scripts/sync_frontend.sh
   
   # Коммитите все вместе
   git add .
   git commit -m "Backend changes + synced frontend"
   ```

4. **Вечер:**
   ```bash
   # Финальная синхронизация
   ./scripts/sync_frontend.sh
   
   # Коммит и пуш
   git add .
   git commit -m "End of day: sync frontend"
   git push
   ```

---

## 🔄 Что делать с локальными изменениями фронтенда?

### Сценарий 1: Быстрое исправление

Если нужно быстро исправить что-то локально:

1. **Делаете изменение** в `frontend/`
2. **Тестируете** локально
3. **Варианты:**
   - **A:** Вручную переносите изменение в Figma Make
   - **B:** Коммитите локально с пометкой `[LOCAL]`, потом переносите в Figma Make

### Сценарий 2: Множественные локальные изменения

Если нужно сделать много локальных изменений:

1. **Создаете ветку** для локальных изменений:
   ```bash
   git checkout -b frontend-local-changes
   cd frontend
   # Делаете изменения
   ```

2. **Тестируете** локально

3. **Переносите в Figma Make:**
   - Либо вручную копируете изменения
   - Либо используете Git (если настроен submodule)

4. **После коммита в Figma Make:**
   ```bash
   git checkout main
   ./scripts/sync_frontend.sh
   ```

---

## ⚠️ Важные правила

1. **Figma Make репозиторий = источник истины для фронтенда**
   - Все изменения фронтенда должны быть в Figma Make репозитории
   - Локальный `frontend/` - это копия для разработки бэкенда

2. **Локальные изменения фронтенда:**
   - Можно делать, но нужно помнить о синхронизации
   - Лучше делать изменения в Figma Make, если возможно

3. **Коммиты:**
   - **Backend изменения** → коммит в этот репозиторий
   - **Frontend изменения** → коммит в Figma Make репозиторий
   - **Синхронизация фронтенда** → коммит в этот репозиторий с пометкой `[SYNC]`

---

## 🚀 Следующие шаги

1. **Создать скрипт синхронизации** `scripts/sync_frontend.sh`
2. **Добавить в `.gitignore`** временные файлы синхронизации
3. **Протестировать workflow** на практике

---

## 📝 Примеры коммитов

```bash
# Синхронизация фронтенда
git commit -m "[SYNC] Update frontend from Figma Make repository"

# Backend изменения
git commit -m "Add new API endpoint for deck management"

# Backend + синхронизация фронтенда
git commit -m "Backend: add token validation + [SYNC] frontend updates"
```

---

**Последнее обновление:** 7 декабря 2025

