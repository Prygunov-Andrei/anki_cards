# 📋 Чек-лист деплоя на удаленный сервер

## 🔍 Перед началом

### Необходимые данные о сервере:
- [ ] **SSH доступ**: `user@host` или `user@ip`
- [ ] **Путь на сервере**: где будет развернуто приложение (например: `/opt/anki_cards`)
- [ ] **Домен/IP**: для настройки `ALLOWED_HOSTS`
- [ ] **SSH ключи**: настроены для доступа без пароля

### Переменные окружения для `.env`:
- [ ] `POSTGRES_DB=anki_db`
- [ ] `POSTGRES_USER=anki_user`
- [ ] `POSTGRES_PASSWORD=<сильный_пароль>`
- [ ] `SECRET_KEY=<сгенерированный_ключ>`
- [ ] `DEBUG=False`
- [ ] `ALLOWED_HOSTS=<домен_или_ip>`
- [ ] `OPENAI_API_KEY=<ваш_ключ>`
- [ ] `GEMINI_API_KEY=<ваш_ключ>` (опционально)

### Переменные для `backend/.env`:
- [ ] `SECRET_KEY=<тот_же_ключ>`
- [ ] `DEBUG=False`
- [ ] `OPENAI_API_KEY=<ваш_ключ>`
- [ ] `GEMINI_API_KEY=<ваш_ключ>` (опционально)

---

## 📦 Подготовка локально

### 1. Создание бэкапа данных
```bash
./scripts/backup_data.sh
```
**Результат:** архив `backups/anki_backup_YYYYMMDD_HHMMSS.tar.gz`

### 2. Проверка локального деплоя
```bash
# Убедитесь, что локально все работает
docker-compose build
docker-compose up -d
# Проверьте http://localhost
docker-compose down
```

### 3. Подготовка переменных окружения
Создайте файлы `.env` и `backend/.env` с необходимыми переменными (см. выше)

---

## 🚀 Деплой на сервер

### Вариант 1: Автоматический деплой (рекомендуется)

```bash
./scripts/deploy.sh user@server_host [remote_path]
```

**Пример:**
```bash
./scripts/deploy.sh root@192.168.1.100 /opt/anki_cards
```

Скрипт автоматически:
1. ✅ Создаст бэкап данных
2. ✅ Проверит подключение к серверу
3. ✅ Создаст архив проекта
4. ✅ Скопирует на сервер
5. ✅ Остановит старые контейнеры
6. ✅ Удалит старые образы
7. ✅ Распакует новый код
8. ✅ Проверит переменные окружения
9. ✅ Соберет и запустит контейнеры
10. ✅ Применит миграции
11. ✅ Предложит восстановить данные из бэкапа

### Вариант 2: Ручной деплой

#### Шаг 1: Подключение к серверу
```bash
ssh user@server_host
```

#### Шаг 2: Остановка старых контейнеров
```bash
cd /opt/anki_cards  # или ваш путь
docker-compose down
docker-compose -f docker-compose.yml -f docker-compose.prod.yml down
```

#### Шаг 3: Удаление старых образов
```bash
docker image prune -f
docker system prune -f
docker images | grep anki_cards | awk '{print $3}' | xargs docker rmi -f
```

#### Шаг 4: Клонирование/обновление кода
```bash
# Если код уже есть
cd /opt/anki_cards
git pull origin main

# Или клонируйте заново
git clone https://github.com/your-username/anki_cards.git /opt/anki_cards
cd /opt/anki_cards
```

#### Шаг 5: Настройка переменных окружения
```bash
# Создайте .env
nano .env

# Создайте backend/.env
nano backend/.env
```

#### Шаг 6: Сборка и запуск
```bash
docker-compose -f docker-compose.yml -f docker-compose.prod.yml build --no-cache
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
```

#### Шаг 7: Миграции
```bash
docker-compose exec backend python manage.py migrate
docker-compose exec backend python manage.py collectstatic --noinput
```

#### Шаг 8: Восстановление данных
```bash
# Скопируйте бэкап на сервер
scp backups/anki_backup_*.tar.gz user@server:/tmp/

# На сервере
./scripts/restore_data.sh /tmp/anki_backup_*.tar.gz
```

---

## ✅ Проверка после деплоя

### 1. Проверка контейнеров
```bash
ssh user@server 'cd /opt/anki_cards && docker-compose ps'
```

**Ожидаемый результат:**
```
NAME                STATUS          PORTS
anki_backend        Up              ...
anki_frontend       Up              ...
anki_db             Up              ...
```

### 2. Проверка логов
```bash
ssh user@server 'cd /opt/anki_cards && docker-compose logs --tail=50'
```

### 3. Health check
```bash
curl http://your-server/api/health/
```

**Ожидаемый ответ:**
```json
{"status": "ok", "message": "Backend is running"}
```

### 4. Проверка фронтенда
Откройте в браузере: `http://your-server/`

### 5. Проверка админки
Откройте: `http://your-server/admin/`

### 6. Проверка данных
- [ ] Пользователи присутствуют
- [ ] Колоды присутствуют
- [ ] Слова присутствуют
- [ ] Медиафайлы отображаются

---

## 🔄 Обновление приложения

### Быстрое обновление (без удаления данных):
```bash
ssh user@server <<EOF
cd /opt/anki_cards
git pull origin main
docker-compose -f docker-compose.yml -f docker-compose.prod.yml build
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d
docker-compose exec backend python manage.py migrate
EOF
```

### Полное обновление (с очисткой):
```bash
./scripts/deploy.sh user@server_host /opt/anki_cards
```

---

## 🐛 Решение проблем

### Проблема: Контейнеры не запускаются
```bash
# Проверьте логи
docker-compose logs

# Проверьте конфигурацию
docker-compose config
```

### Проблема: База данных не подключается
```bash
# Проверьте переменные окружения
docker-compose exec backend env | grep DATABASE

# Проверьте доступность БД
docker-compose exec backend python manage.py dbshell
```

### Проблема: Старые образы занимают место
```bash
# Удалите неиспользуемые образы
docker image prune -a -f

# Удалите все неиспользуемые ресурсы
docker system prune -a -f
```

### Проблема: Медиафайлы не отображаются
```bash
# Проверьте права доступа
docker-compose exec backend ls -la /app/media/

# Проверьте конфигурацию nginx
docker-compose exec frontend cat /etc/nginx/conf.d/default.conf
```

---

## 📝 Важные замечания

1. **⚠️ Всегда создавайте бэкап перед деплоем!**
2. **⚠️ Не коммитьте `.env` файлы в Git!**
3. **⚠️ Используйте `DEBUG=False` в продакшене!**
4. **⚠️ Настройте SSL/HTTPS для безопасности!**
5. **⚠️ Регулярно обновляйте зависимости!**

---

## 🔐 Безопасность

- [ ] `DEBUG=False` в продакшене
- [ ] Сильные пароли для БД
- [ ] `SECRET_KEY` сгенерирован и безопасен
- [ ] `ALLOWED_HOSTS` содержит только нужные домены
- [ ] SSL/HTTPS настроен
- [ ] Файрвол настроен
- [ ] SSH ключи вместо паролей

---

**Последнее обновление:** 7 декабря 2025

