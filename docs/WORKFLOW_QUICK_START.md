# 🚀 Быстрый старт: Workflow разработки

## Краткое описание

- **Frontend** разрабатывается в **Figma Make** → коммитится в `https://github.com/Prygunov-Andrei/Ankiflashcardgenerator.git`
- **Backend** разрабатывается **локально** → коммитится в этот репозиторий
- **Синхронизация** фронтенда выполняется скриптом `./scripts/sync_frontend.sh`

---

## 📋 Ежедневный workflow

### 1. Утром (начало работы)

```bash
# Синхронизируете фронтенд (если были изменения в Figma Make)
./scripts/sync_frontend.sh

# Запускаете бэкенд
./start.sh

# В отдельном терминале запускаете ngrok
ngrok http 8000
```

### 2. Во время разработки

- **Frontend изменения:**
  - Работаете в Figma Make
  - Коммитите в Figma Make репозиторий
  - **НЕ нужно** сразу синхронизировать (можно в конце дня)

- **Backend изменения:**
  - Работаете локально в этом репозитории
  - Коммитите как обычно: `git add . && git commit -m "..."`

### 3. Перед коммитом бэкенда (опционально)

```bash
# Если были изменения фронтенда в Figma Make
./scripts/sync_frontend.sh

# Коммитите все вместе
git add .
git commit -m "Backend: add feature + [SYNC] frontend updates"
```

### 4. В конце дня

```bash
# Финальная синхронизация фронтенда
./scripts/sync_frontend.sh --auto-commit

# Пуш изменений
git push
```

---

## 🔄 Синхронизация фронтенда

### Простая синхронизация

```bash
./scripts/sync_frontend.sh
```

Скрипт:
- ✅ Проверяет, есть ли новые изменения в Figma Make репозитории
- ✅ Клонирует/обновляет репозиторий
- ✅ Копирует файлы в `frontend/`
- ✅ Показывает, что изменилось
- ✅ Предлагает закоммитить изменения

### С автоматическим коммитом

```bash
./scripts/sync_frontend.sh --auto-commit
```

Автоматически создаст коммит с пометкой `[SYNC]`.

### С просмотром изменений

```bash
./scripts/sync_frontend.sh --review
```

Покажет diff перед коммитом.

---

## ⚠️ Важные правила

1. **Figma Make репозиторий = источник истины для фронтенда**
   - Все изменения фронтенда должны быть в Figma Make репозитории
   - Локальный `frontend/` - это копия для разработки бэкенда

2. **Локальные изменения фронтенда:**
   - Можно делать, но **важно помнить:**
     - При следующей синхронизации они могут быть перезаписаны
     - Лучше делать изменения в Figma Make, если возможно

3. **Коммиты:**
   - **Backend изменения** → коммит в этот репозиторий
   - **Frontend изменения** → коммит в Figma Make репозиторий
   - **Синхронизация** → коммит в этот репозиторий с пометкой `[SYNC]`

---

## 🛠️ Что делать, если...

### ...нужно быстро исправить что-то в фронтенде локально?

1. Делаете изменение в `frontend/`
2. Тестируете локально
3. **Важно:** Вручную переносите изменение в Figma Make
4. После коммита в Figma Make запускаете `./scripts/sync_frontend.sh`

### ...забыли синхронизировать фронтенд?

Не страшно! Просто запустите:

```bash
./scripts/sync_frontend.sh
```

Скрипт проверит последний синхронизированный коммит и обновит только новые изменения.

### ...хотите посмотреть, что изменилось в Figma Make?

```bash
./scripts/sync_frontend.sh --review
```

Покажет diff всех изменений.

---

## 📚 Подробная документация

- **[Полный workflow](./DEVELOPMENT_WORKFLOW.md)** - детальное описание всех вариантов
- **[Интеграция Figma Make](./FIGMA_MAKE_REPO_INTEGRATION.md)** - как интегрировать фронтенд

---

**Последнее обновление:** 7 декабря 2025

