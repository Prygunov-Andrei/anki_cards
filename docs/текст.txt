
Как создать anki карточки
	1.	Открываем чат GPT https://chatgpt.com/c/687e1aee-cf38-800e-895c-ab542a3feaeb
	2.	Пишем ему следующий текст: 
Нa примере выше прошу создай мне код в нужном Python-формате для использования в Anki-скрипте выше и вставь туда следующие слова: перечисляем слова через запятую Если есть ошибки в словах, то исправь их, если у слова несколько значений, то перечисли их через точку запятой
(если там другой язык, то допиши фразу : просьба учесть, что там португальский язык
	3.	После создания скрипта скопируй его
	4.	￼

	4.	 Открываем после этого папку на рабочем столе
	5.	￼

	5.	Открываем файл через TextEdit (нажимаем по тачпаду двумя пальцами)
	6.	￼
	7.	￼

	1.	Выделяем только кусок начинающийся с cards = [ , сначала удаляем выделенный кусок, а потом нажимаем command + v, затем закрыть
 нажать на крестик 
	7.	Потом открываем Терминал
￼

	8.	Пишем 
1. Çcd/Users/annasavinova/Desktop/anki
затем нажимаем enter
2. ls 
нажимаем, затем  enter
3.  python3 make_anki.py
затем  нажимаем enter
ждем

￼

	9.	После создания новой папки в нашей на рабочем столе, мы открываем anki
	10.	Создаем колоду
	11.	Нажимаем Импортировать внизу и выбираем из списка файл с расширением на конце csv из новой папки, которая создалась (последняя) (название можно проверить в Терминал, в конце нажать клавишу open
	12.	￼

	12.	Нужно выбрать разделение ЗАПЯТАЯ и нужное название колоды
	13.	￼

	13.	 Затем нажать сверху синюю кнопку Импортировать
	14.	Восхитительно, мы молодцы, осталось только добавить звук
	15.	Для этого после импортирования необходимо нажать сверху в anki 
Инструменты → Проверить медиафайлы
￼

	16.	После открытия нажать внизу Просмотреть файлы
	17.	Откроется папка collection.media, необходимо зайти в нашу первую папку на рабочем столе, которую сгенерировал нам чат gpt и скопировать все наши звуки 
Нажимаем command + a, command + с ( в нашей папке с audio), переходим в collection.media и нажимаем command + v 

Готово! Вы восхительны!





Skript


import csv
import os
import shutil
import zipfile
from gtts import gTTS

deck_name = "AnkiPortugueseDeck"
csv_name = "anki_cards.csv"
zip_name = "AnkiPortugueseDeck.zip"

cards = [
    ("acredita-se", "считается, верят, предполагается"),
    ("dar azar", "приносить несчастье"),
    ("atrair má sorte", "привлекать неудачу"),
    ("chamar a morte", "накликать смерть"),
    ("criar desarmonia", "создавать разлад"),
    ("sinal de alegria", "знак радости"),
    ("afastar maus espíritos", "отгонять злых духов"),
    ("abrir um guarda-chuva dentro de casa", "открыть зонт в доме"),
    ("beber leite com peixe", "пить молоко с рыбой"),
    ("cruzar os talheres", "перекрестить столовые приборы"),
    ("deixar a mala/carteira no chão", "оставить сумку/кошелёк на полу"),
    ("plantar uma figueira junto à casa", "посадить инжир рядом с домом"),
    ("partir um espelho", "разбить зеркало"),
    ("sentar-se no canto da mesa", "садиться на угол стола"),
    ("dormir com os pés virados para a porta", "спать ногами к двери"),
    ("varrer os pés de alguém", "подметать под ногами кого-то"),
    ("se uma borboleta preta entrar em casa", "если чёрная бабочка залетит в дом"),
    ("treze pessoas à mesa", "тринадцать человек за столом"),
    ("bater na madeira 3 vezes", "постучать по дереву три раза"),
    ("folhas de louro na carteira/porta-moedas", "лавровый лист в кошельке"),
    ("entornar vinho", "пролить вино"),
    ("coçar a palma da mão direita", "зуд в правой ладони"),
    ("tossir", "кашлять"),
    ("faca caiu com a lâmina virada para baixo", "нож упал лезвием вниз"),
    ("uma visita indesejada", "нежеланный гость"),
    ("faca cravada na porta", "нож, воткнутый в дверь"),
    ("sobreviver", "выжить")
]

audio_folder = os.path.join(deck_name, "audio")

# Удаляем старую папку
if os.path.exists(deck_name):
    shutil.rmtree(deck_name)
os.makedirs(audio_folder)

# Создание CSV
with open(os.path.join(deck_name, csv_name), "w", newline="", encoding="utf-8") as csvfile:
    writer = csv.writer(csvfile)
    writer.writerow(["Front", "Back"])

    for word_pt, word_ru in cards:
        audio_filename = f"{word_pt.replace(' ', '_').replace('/', '_')}.mp3"
        sound_tag = f"[sound:{audio_filename}]"

        # Карточка: Португальский → Русский (с озвучкой)
        front_pt = f"{word_pt} {sound_tag}"
        writer.writerow([front_pt, word_ru])

        # Карточка: Русский → Португальский (с озвучкой на португальский)
        back_pt = f"{word_pt} {sound_tag}"
        writer.writerow([word_ru, back_pt])

        # Генерация аудио
        tts = gTTS(text=word_pt, lang="pt")
        tts.save(os.path.join(audio_folder, audio_filename))
        print(f"🔈 Сгенерировано: {word_pt}")

# Создание ZIP
with zipfile.ZipFile(zip_name, "w", zipfile.ZIP_DEFLATED) as zipf:
    for foldername, subfolders, filenames in os.walk(deck_name):
        for filename in filenames:
            file_path = os.path.join(foldername, filename)
            arcname = os.path.relpath(file_path, deck_name)
            zipf.write(file_path, arcname)

print(f"\n✅ Готово! Архив {zip_name} создан.")















