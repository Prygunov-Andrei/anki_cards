# 🚀 Документация по деплою

> **Скрипты деплоя:** см. [`../scripts/`](../scripts/) - `deploy.sh`, `safe_deploy.sh`, `setup_domain.sh`, `fix_media.sh`

## Содержание

1. [Чеклист перед деплоем](#чеклист-перед-деплоем)
2. [Ручной деплой](#ручной-деплой)
3. [Настройка домена и SSL](#настройка-домена-и-ssl)
4. [Решение проблем](#решение-проблем)
5. [План автоматического деплоя (CI/CD)](#план-автоматического-деплоя-cicd)

---

## Чеклист перед деплоем

### ✅ Проверки конфигурации

- [x] **Gunicorn:** Workers: 2, Timeout: 120s, Max-requests: 1000
- [x] **Раздача медиафайлов:** Настроена в `backend/config/urls.py` и `frontend/nginx.conf`
- [x] **Docker Compose:** Restart policy `unless-stopped`, healthcheck для БД
- [x] **Безопасность:** Нет бесконечных циклов, правильные таймауты
- [x] **Скрипты деплоя:** Обновлены IP и пароль сервера

### ⚠️ Перед деплоем

1. **Проверить доступность сервера:**
   ```bash
   ping 72.56.83.95
   ssh root@72.56.83.95
   ```

2. **Убедиться, что Docker установлен на сервере**

3. **Выполнять деплой пошагово:**
   - Сначала остановить все (если есть)
   - Затем собрать образы
   - Затем запустить по одному сервису
   - Проверять логи после каждого шага

4. **Мониторить ресурсы:**
   ```bash
   docker stats
   top
   ```

---

## Ручной деплой

### ⚠️ ВАЖНО: Действуйте осторожно!

### Изменения для предотвращения перегрузки:

1. **Ограничения ресурсов** в docker-compose.yml:
   - Backend: максимум 2 workers
   - Restart policy: `unless-stopped`

2. **Gunicorn оптимизация:**
   - Workers: 2 (было 3)
   - `--max-requests` и `--max-requests-jitter` для автоматической перезагрузки

### Пошаговый деплой:

1. **Проверка перед деплоем:**
   ```bash
   # Проверить, что все файлы на месте
   ls -la backend/config/urls.py frontend/src/components/MediaGenerator.tsx
   
   # Проверить docker-compose.yml
   docker compose config
   ```

2. **Остановка существующих контейнеров:**
   ```bash
   cd /opt/anki_cards
   docker compose down
   ```

3. **Сборка образов (по одному):**
   ```bash
   # Сначала backend
   docker compose build backend
   
   # Проверить, что сборка успешна
   docker images | grep anki_cards
   
   # Затем frontend (ОСТОРОЖНО - тяжелая операция!)
   docker compose build frontend
   ```

4. **Запуск сервисов:**
   ```bash
   # Запустить только базу данных сначала
   docker compose up -d db
   
   # Подождать, пока база данных будет готова
   sleep 10
   docker compose ps
   
   # Запустить backend
   docker compose up -d backend
   
   # Проверить логи backend
   docker compose logs backend | tail -20
   
   # Запустить frontend
   docker compose up -d frontend
   ```

5. **Проверка после запуска:**
   ```bash
   # Проверить статус всех контейнеров
   docker compose ps
   
   # Проверить использование ресурсов
   docker stats --no-stream
   
   # Проверить логи
   docker compose logs --tail=50
   ```

### Использование скриптов:

**Основной деплой:**
```bash
./scripts/deploy.sh
```

**Безопасный деплой с проверками:**
```bash
./scripts/safe_deploy.sh
```

---

## Настройка домена и SSL

### Настройка DNS

1. На сайте регистратора домена добавить A-запись:
   - `www.get-anki.fun` → `72.56.83.95`
   - `get-anki.fun` → `72.56.83.95`

2. Проверить DNS:
   ```bash
   dig +short www.get-anki.fun A
   ```

### Настройка SSL (Let's Encrypt)

1. **Остановить frontend:**
   ```bash
   cd /opt/anki_cards
   docker compose stop frontend
   ```

2. **Установить certbot:**
   ```bash
   apt update
   apt install -y certbot
   ```

3. **Получить сертификат:**
   ```bash
   certbot certonly --standalone -d www.get-anki.fun -d get-anki.fun \
     --non-interactive --agree-tos --email admin@get-anki.fun
   ```

4. **Включить HTTPS редирект в nginx.conf:**
   - Раскомментировать строку: `return 301 https://$server_name$request_uri;`
   - Или использовать скрипт: `./scripts/setup_domain.sh`

5. **Перезапустить frontend:**
   ```bash
   docker compose restart frontend
   ```

6. **Проверить:**
   ```bash
   curl -I http://www.get-anki.fun  # Должен быть редирект 301
   curl -I -k https://www.get-anki.fun  # Должен быть 200
   ```

### Автообновление сертификата

Certbot автоматически настроит обновление сертификата каждые 90 дней через systemd timer.

---

## Решение проблем

### 🚨 Если что-то пошло не так:

1. **Немедленно остановить:**
   ```bash
   docker compose down
   ```

2. **Проверить логи:**
   ```bash
   docker compose logs > logs.txt
   ```

3. **Проверить использование ресурсов:**
   ```bash
   docker stats
   top
   ```

### Частые проблемы:

**Проблема: Сервер перегружен (100% CPU)**
- **Причина:** Сборка frontend на сервере очень тяжелая
- **Решение:** Собирать frontend локально и копировать образ на сервер

**Проблема: Медиафайлы не отображаются (404)**
- **Причина:** Nginx не проксирует `/media/` правильно
- **Решение:** Проверить конфигурацию nginx, убедиться что regex не перехватывает `/media/`

**Проблема: Backend не подключается к БД**
- **Причина:** Проблемы с Docker сетью
- **Решение:** `docker compose down && docker compose up -d`

**Проблема: SSH не подключается**
- **Причина:** Сервер перегружен
- **Решение:** Подождать 2-3 минуты, проверить через веб-консоль хостинга

---

## План автоматического деплоя (CI/CD)

### Обзор

В будущем планируется настроить автоматический деплой через GitHub Actions для деплоя при каждом push в ветку `main`.

### Архитектура

**GitHub Actions Workflow** будет автоматически:
- Запускаться при push в ветку `main` или `master`
- Собирать Docker образы (backend и frontend)
- Тестировать приложение
- Деплоить на сервер

### Что нужно будет настроить:

1. **GitHub Actions workflow файл** (`.github/workflows/deploy.yml`)
2. **GitHub Secrets** для хранения:
   - `SERVER_IP` - IP адрес сервера
   - `SERVER_USER` - пользователь для SSH
   - `SSH_PRIVATE_KEY` - приватный SSH ключ
3. **SSH ключ на сервере** для GitHub Actions
4. **Скрипт деплоя на сервере**

### Безопасность:

- Использование GitHub Secrets вместо паролей в коде
- SSH ключи вместо паролей
- Ограничение доступа к секретам
- Автоматический откат при ошибках

### Примерная структура workflow:

```yaml
name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      - name: Build and deploy
        run: |
          # Сборка образов
          # Тестирование
          # Деплой на сервер
```

### Этапы внедрения:

1. **Подготовка** (1-2 часа): SSH ключи, GitHub Secrets
2. **Базовый workflow** (2-3 часа): Создание `.github/workflows/deploy.yml`
3. **Сборка образов** (2-3 часа): Настройка Docker сборки
4. **Деплой** (2-3 часа): Настройка деплоя на сервер
5. **Улучшения** (по необходимости): Тесты, уведомления

**Оценка времени:** 8-12 часов

**Приоритет:** Средний

**Статус:** 📋 План готов к реализации

---

## Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)

---

**Текущий статус:** Используется ручной деплой через скрипты в папке [`../scripts/`](../scripts/)

