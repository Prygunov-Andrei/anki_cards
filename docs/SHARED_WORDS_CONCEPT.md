# Концепция общей базы слов (Shared Words Database)

## 🎯 Цель

Создать общую базу данных для всех пользователей, которая будет хранить:
- Слова с переводами
- Множественные варианты изображений для каждого слова
- Множественные варианты аудио для каждого слова

**Преимущества:**
- ✅ Экономия токенов (не генерируем повторно одинаковые слова)
- ✅ Экономия времени (мгновенная загрузка из БД)
- ✅ Больше вариантов выбора для пользователей
- ✅ Постепенное накопление качественного контента
- ✅ Сообщество помогает улучшать контент

---

## 📊 Архитектура данных

### 1. Модель `SharedWord` (Глобальное слово)

**Хранит базовую информацию о слове без привязки к пользователю:**

```python
class SharedWord(models.Model):
    """Глобальная база слов для всех пользователей"""
    
    original_word = models.CharField(max_length=200, db_index=True)
    language = models.CharField(max_length=2)  # 'de', 'pt'
    translation = models.CharField(max_length=200)
    
    # Статистика использования
    total_uses = models.IntegerField(default=0)  # Сколько раз использовали
    last_used_at = models.DateTimeField(auto_now=True)
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        unique_together = [['original_word', 'language', 'translation']]
        indexes = [
            models.Index(fields=['original_word', 'language']),
        ]
```

**Особенности:**
- Один и тот же `original_word` может иметь разные `translation` (контекстные переводы)
- Уникальность по комбинации `(original_word, language, translation)`

---

### 2. Модель `SharedWordImage` (Варианты изображений)

**Хранит все варианты изображений для слова:**

```python
class SharedWordImage(models.Model):
    """Варианты изображений для глобального слова"""
    
    shared_word = models.ForeignKey(
        SharedWord,
        on_delete=models.CASCADE,
        related_name='images'
    )
    
    image_file = models.ImageField(upload_to='shared_images/')
    
    # Метаданные генерации
    provider = models.CharField(max_length=20)  # 'openai', 'gemini'
    model = models.CharField(max_length=50)  # 'dalle-3', 'gemini-2.5-flash-image', etc.
    prompt_used = models.TextField(blank=True)  # Промпт, который использовался
    
    # Статистика
    usage_count = models.IntegerField(default=0)  # Сколько раз выбрали
    created_by = models.ForeignKey(
        User,
        on_delete=models.SET_NULL,
        null=True,
        related_name='shared_images_created'
    )
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        ordering = ['-usage_count', '-created_at']  # Популярные сначала
```

**Особенности:**
- Один `SharedWord` может иметь множество `SharedWordImage`
- Сортировка по популярности (`usage_count`)
- Сохраняем информацию о том, кто создал (для статистики)

---

### 3. Модель `SharedWordAudio` (Варианты аудио)

**Хранит все варианты аудио для слова:**

```python
class SharedWordAudio(models.Model):
    """Варианты аудио для глобального слова"""
    
    shared_word = models.ForeignKey(
        SharedWord,
        on_delete=models.CASCADE,
        related_name='audio_files'
    )
    
    audio_file = models.FileField(upload_to='shared_audio/')
    
    # Метаданные генерации
    voice = models.CharField(max_length=50, default='alloy')  # Голос TTS
    language = models.CharField(max_length=2)  # Язык произношения
    
    # Статистика
    usage_count = models.IntegerField(default=0)
    created_by = models.ForeignKey(
        User,
        on_delete=models.SET_NULL,
        null=True,
        related_name='shared_audio_created'
    )
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        ordering = ['-usage_count', '-created_at']
```

---

### 4. Обновление модели `Word` (Пользовательское слово)

**Добавляем связь с глобальной базой:**

```python
class Word(models.Model):
    # ... существующие поля ...
    
    # Связь с глобальной базой
    shared_word = models.ForeignKey(
        'cards.SharedWord',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='user_words',
        verbose_name='Глобальное слово'
    )
    
    # Выбранные варианты медиа из глобальной базы
    selected_shared_image = models.ForeignKey(
        'cards.SharedWordImage',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='selected_by_words'
    )
    
    selected_shared_audio = models.ForeignKey(
        'cards.SharedWordAudio',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name='selected_by_words'
    )
    
    # Локальные медиа (если пользователь сгенерировал свое)
    image_file = models.ImageField(...)  # Остается для кастомных изображений
    audio_file = models.FileField(...)  # Остается для кастомных аудио
```

**Логика:**
- Если `selected_shared_image` установлен → используем его
- Если нет → используем локальный `image_file`
- То же самое для аудио

---

## 🔄 Workflow (Процесс работы)

### Этап 1: Пользователь вводит слово

```
Пользователь вводит: "Haus"
```

### Этап 2: Поиск в глобальной базе

```python
def find_shared_word(original_word: str, language: str, translation: str = None):
    """Ищем слово в глобальной базе"""
    
    # Нормализуем слово (lowercase, убираем лишние пробелы)
    normalized_word = original_word.strip().lower()
    
    # Ищем точное совпадение
    query = SharedWord.objects.filter(
        original_word__iexact=normalized_word,
        language=language
    )
    
    if translation:
        query = query.filter(translation__iexact=translation.strip())
    
    return query.first()
```

**Результат:**
- ✅ Найдено → переходим к Этапу 3
- ❌ Не найдено → переходим к Этапу 4

---

### Этап 3: Показываем варианты пользователю

**API Response:**

```json
{
  "word": "Haus",
  "translation": "Дом",
  "language": "de",
  "found_in_shared_db": true,
  "available_images": [
    {
      "id": 1,
      "url": "/media/shared_images/haus_1.jpg",
      "provider": "openai",
      "model": "dalle-3",
      "usage_count": 45,
      "created_at": "2025-01-10T12:00:00Z"
    },
    {
      "id": 2,
      "url": "/media/shared_images/haus_2.jpg",
      "provider": "gemini",
      "model": "gemini-2.5-flash-image",
      "usage_count": 12,
      "created_at": "2025-01-15T14:30:00Z"
    }
  ],
  "available_audio": [
    {
      "id": 1,
      "url": "/media/shared_audio/haus_1.mp3",
      "voice": "alloy",
      "usage_count": 78,
      "created_at": "2025-01-10T12:00:00Z"
    }
  ]
}
```

**Frontend показывает:**
- Галерею изображений (с превью)
- Список аудио (с кнопкой прослушивания)
- Кнопку "Сгенерировать новый вариант"

---

### Этап 4: Пользователь выбирает вариант

**Вариант A: Выбрал существующий**

```python
# Пользователь выбрал изображение ID=1 и аудио ID=1
# Создаем/обновляем Word пользователя
word_obj = Word.objects.get_or_create(
    user=user,
    original_word="Haus",
    language="de",
    defaults={
        'translation': "Дом",
        'shared_word': shared_word,
        'selected_shared_image_id': 1,
        'selected_shared_audio_id': 1,
    }
)

# Увеличиваем счетчики использования
SharedWordImage.objects.filter(id=1).update(usage_count=F('usage_count') + 1)
SharedWordAudio.objects.filter(id=1).update(usage_count=F('usage_count') + 1)
SharedWord.objects.filter(id=shared_word.id).update(
    total_uses=F('total_uses') + 1
)
```

**Вариант B: Генерирует новый**

```python
# Генерируем новое изображение через LLM
new_image = generate_image(...)

# Сохраняем в глобальную базу
shared_image = SharedWordImage.objects.create(
    shared_word=shared_word,
    image_file=new_image,
    provider='openai',
    model='dalle-3',
    created_by=user,
    usage_count=1  # Первое использование
)

# Связываем с пользовательским словом
word_obj.selected_shared_image = shared_image
word_obj.save()
```

---

## 🎨 UI/UX концепция

### Сценарий 1: Слово найдено в базе

```
┌─────────────────────────────────────┐
│  Слово: Haus                        │
│  Перевод: Дом                        │
│                                     │
│  📷 Изображения (2 варианта):       │
│  ┌──────┐  ┌──────┐                │
│  │ [img]│  │ [img]│  [+ Новое]     │
│  │ 45×  │  │ 12×  │                │
│  └──────┘  └──────┘                │
│                                     │
│  🔊 Аудио (1 вариант):              │
│  ┌─────────────────────────────┐    │
│  │ ▶ [Прослушать] 78× [+ Новое]│    │
│  └─────────────────────────────┘    │
│                                     │
│  [✅ Использовать выбранные]        │
└─────────────────────────────────────┘
```

### Сценарий 2: Слово не найдено

```
┌─────────────────────────────────────┐
│  Слово: Haus                        │
│  Перевод: Дом                        │
│                                     │
│  ℹ️ Слово не найдено в базе         │
│                                     │
│  [🎨 Сгенерировать изображение]     │
│  [🔊 Сгенерировать аудио]           │
│                                     │
│  После генерации медиа будет        │
│  сохранено в общую базу для         │
│  других пользователей               │
└─────────────────────────────────────┘
```

---

## 🔍 Поиск и нормализация

### Нормализация слова

```python
def normalize_word(word: str) -> str:
    """Нормализует слово для поиска"""
    # Убираем лишние пробелы
    word = word.strip()
    
    # Приводим к lowercase (для большинства языков)
    word = word.lower()
    
    # Для немецкого: сохраняем ß, но нормализуем регистр
    # Haus, haus, HAUS → haus
    
    return word
```

### Поиск с учетом вариаций

**Вариант 1: Точное совпадение (строгий)**
- `original_word__iexact` - игнорирует регистр
- Только для 100% совпадения

**Вариант 2: Поиск с вариациями (мягкий)**
- Учитывает артикли (der, die, das для немецкого)
- Учитывает множественное число
- Требует дополнительной логики

**Рекомендация:** Начать с точного совпадения, потом добавить вариации.

---

## 📈 Статистика и аналитика

### Метрики для отслеживания:

1. **Экономия токенов:**
   - Сколько раз использовали существующие медиа
   - Сколько токенов сэкономили

2. **Популярность контента:**
   - Какие изображения выбирают чаще
   - Какие провайдеры/модели дают лучшие результаты

3. **Качество контента:**
   - Рейтинг изображений (если добавим)
   - Жалобы на неподходящий контент

---

## 🚀 Этапы внедрения

### Этап 1: Базовая структура (MVP)
- [ ] Создать модели `SharedWord`, `SharedWordImage`, `SharedWordAudio`
- [ ] Миграции
- [ ] Базовый поиск по точному совпадению

### Этап 2: Интеграция в генерацию
- [ ] Обновить `generate_cards_view` для поиска в глобальной базе
- [ ] API endpoint для получения вариантов медиа
- [ ] Сохранение выбранных вариантов

### Этап 3: Frontend
- [ ] Компонент выбора изображения из галереи
- [ ] Компонент выбора аудио
- [ ] Кнопка "Сгенерировать новый"

### Этап 4: Оптимизация
- [ ] Кэширование популярных слов
- [ ] Поиск с вариациями (артикли, множественное число)
- [ ] Статистика и аналитика

### Этап 5: Дополнительные фичи
- [ ] Рейтинг изображений
- [ ] Модерация контента
- [ ] Экспорт/импорт глобальной базы

---

## ⚠️ Потенциальные проблемы и решения

### Проблема 1: Разные переводы для одного слова

**Пример:** "Bank" может быть "банк" или "скамейка"

**Решение:**
- Хранить `(original_word, language, translation)` как уникальную комбинацию
- При поиске учитывать перевод, если он предоставлен
- Если перевод не предоставлен → показывать все варианты

### Проблема 2: Качество контента

**Проблема:** Плохие изображения могут попасть в базу

**Решение:**
- Добавить систему рейтингов (позже)
- Модерация (позже)
- Показывать популярные варианты первыми (`usage_count`)

### Проблема 3: Хранение файлов

**Проблема:** Много файлов в общей базе

**Решение:**
- Использовать облачное хранилище (S3, Cloudflare R2)
- CDN для быстрой загрузки
- Периодическая очистка неиспользуемых файлов

### Проблема 4: Конфиденциальность

**Проблема:** Пользователи могут не хотеть делиться своими словами

**Решение:**
- Опция "Не сохранять в общую базу" (по умолчанию сохранять)
- Анонимность: не показывать, кто создал (или показывать только статистику)

---

## 💡 Дополнительные идеи

### 1. Система рейтингов

```python
class SharedWordImageRating(models.Model):
    """Рейтинги изображений"""
    image = models.ForeignKey(SharedWordImage, ...)
    user = models.ForeignKey(User, ...)
    rating = models.IntegerField(choices=[(1, 'Плохо'), (5, 'Отлично')])
```

### 2. Теги и категории

```python
class SharedWordTag(models.Model):
    """Теги для слов"""
    shared_word = models.ForeignKey(SharedWord, ...)
    tag = models.CharField(max_length=50)  # 'животные', 'еда', etc.
```

### 3. История изменений

```python
class SharedWordHistory(models.Model):
    """История изменений слова"""
    shared_word = models.ForeignKey(SharedWord, ...)
    action = models.CharField(...)  # 'created', 'updated', 'deleted'
    user = models.ForeignKey(User, ...)
    timestamp = models.DateTimeField(auto_now_add=True)
```

---

## 📝 API Endpoints (предварительно)

### 1. Поиск слова в глобальной базе

```
GET /api/shared-words/search/?word=Haus&language=de&translation=Дом
```

**Response:**
```json
{
  "found": true,
  "shared_word_id": 123,
  "images": [...],
  "audio": [...]
}
```

### 2. Получить варианты медиа для слова

```
GET /api/shared-words/{id}/media/
```

### 3. Выбрать вариант медиа

```
POST /api/shared-words/{id}/select-media/
{
  "image_id": 1,
  "audio_id": 2
}
```

### 4. Сгенерировать и сохранить новый вариант

```
POST /api/shared-words/{id}/generate-image/
{
  "provider": "openai",
  "model": "dalle-3"
}
```

---

## 🎯 Итоговая концепция

**Основная идея:**
1. При вводе слова → ищем в глобальной базе
2. Если найдено → показываем варианты медиа
3. Пользователь выбирает или генерирует новый
4. Новое медиа сохраняется в глобальную базу
5. Следующий пользователь видит больше вариантов

**Результат:**
- Экономия токенов и времени
- Улучшение качества контента
- Сообщество помогает создавать контент

---

## ❓ Вопросы для обсуждения

1. **Нормализация:** Как обрабатывать артикли? (der Haus vs Haus)
2. **Переводы:** Один word может иметь разные переводы - как показывать?
3. **Приватность:** По умолчанию сохранять в общую базу или спрашивать?
4. **Модерация:** Нужна ли модерация контента сразу или позже?
5. **Хранение:** Локально или в облаке (S3, Cloudflare R2)?
6. **Кэширование:** Как кэшировать популярные слова для скорости?

