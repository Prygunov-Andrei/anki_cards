# Исправление: Обработка немецких словосочетаний и предложений

## 🔍 Проблема

**Симптом:** При вводе словосочетания или предложения на немецком (например, `"vergleichen Sie anschließend mit Ihrem Partner"`), после обработки немецких слов остается только первое слово `"Vergleichen"`, а остальная часть теряется.

**Причина:** Функция `process_german_word()` предназначена для обработки **отдельных слов** (добавление артикля, исправление регистра). Когда передается предложение или словосочетание, LLM пытается обработать его как одно слово и возвращает только первое слово.

---

## ✅ Решение (бэкенд)

### Что исправлено:

1. **Добавлена проверка на словосочетания/предложения:**
   - Если входная строка содержит **более одного слова** (разделенных пробелами), функция **не обрабатывает** её и возвращает без изменений
   - Обработка применяется только к **отдельным словам**

2. **Обновлен промпт:**
   - Добавлена инструкция для LLM: если это предложение или словосочетание, вернуть его без изменений

### Код исправления:

```python
def process_german_word(word: str, user=None) -> str:
    # Проверяем, является ли входная строка предложением/словосочетанием
    word_parts = word.strip().split()
    if len(word_parts) > 1:
        # Это предложение или словосочетание - возвращаем без изменений
        logger.info(f"Пропущена обработка словосочетания/предложения: '{word}'")
        return word
    
    # Обработка только для отдельных слов
    # ...
```

---

## 📋 Рекомендации для фронтенда

### 1. Определение словосочетаний/предложений

**Правило:** Если строка содержит пробелы и более одного слова — это словосочетание/предложение, и его **не нужно** обрабатывать через `process_german_word`.

```typescript
function isPhraseOrSentence(text: string): boolean {
  const words = text.trim().split(/\s+/);
  return words.length > 1;
}

// Примеры:
isPhraseOrSentence("Haus")           // false - одно слово
isPhraseOrSentence("das Haus")      // true - два слова (словосочетание)
isPhraseOrSentence("vergleichen Sie anschließend mit Ihrem Partner")  // true - предложение
```

### 2. Условная обработка немецких слов

**Рекомендуемый подход:**

```typescript
async function processGermanWordIfNeeded(word: string): Promise<string> {
  // Проверяем, является ли это словосочетанием/предложением
  const words = word.trim().split(/\s+/);
  
  if (words.length > 1) {
    // Это словосочетание или предложение - не обрабатываем
    console.log(`Пропущена обработка словосочетания: "${word}"`);
    return word; // Возвращаем без изменений
  }
  
  // Это отдельное слово - обрабатываем
  try {
    const response = await api.post('/cards/process-german-words/', {
      word: word
    });
    return response.data.processed_word;
  } catch (error) {
    console.error(`Ошибка обработки слова "${word}":`, error);
    return word; // В случае ошибки возвращаем исходное слово
  }
}
```

### 3. Обработка списка слов

**При обработке списка слов:**

```typescript
async function processGermanWordsList(words: string[]): Promise<string[]> {
  const processedWords = await Promise.all(
    words.map(async (word) => {
      // Проверяем, нужно ли обрабатывать
      const wordParts = word.trim().split(/\s+/);
      
      if (wordParts.length > 1) {
        // Словосочетание/предложение - не обрабатываем
        return word;
      }
      
      // Отдельное слово - обрабатываем
      try {
        const response = await api.post('/cards/process-german-words/', {
          word: word
        });
        return response.data.processed_word;
      } catch (error) {
        console.error(`Ошибка обработки "${word}":`, error);
        return word; // Возвращаем исходное слово при ошибке
      }
    })
  );
  
  return processedWords;
}
```

### 4. Визуальная индикация

**Рекомендуется показывать пользователю, какие слова будут обработаны:**

```typescript
function categorizeWords(words: string[]): {
  singleWords: string[];
  phrases: string[];
} {
  const singleWords: string[] = [];
  const phrases: string[] = [];
  
  words.forEach(word => {
    const wordParts = word.trim().split(/\s+/);
    if (wordParts.length > 1) {
      phrases.push(word);
    } else {
      singleWords.push(word);
    }
  });
  
  return { singleWords, phrases };
}

// Использование:
const { singleWords, phrases } = categorizeWords(words);

if (phrases.length > 0) {
  console.log(`Найдено ${phrases.length} словосочетаний/предложений, которые не будут обработаны:`);
  phrases.forEach(phrase => console.log(`  - "${phrase}"`));
}
```

---

## 🧪 Тестирование

### Тестовые случаи:

1. **Отдельное слово (существительное):**
   - Вход: `"haus"`
   - Ожидаемый результат: `"das Haus"` (с артиклем и заглавной буквой)

2. **Отдельное слово (глагол):**
   - Вход: `"gehen"`
   - Ожидаемый результат: `"gehen"` (без артикля, регистр исправлен)

3. **Словосочетание:**
   - Вход: `"das Haus"`
   - Ожидаемый результат: `"das Haus"` (без изменений)

4. **Предложение:**
   - Вход: `"vergleichen Sie anschließend mit Ihrem Partner"`
   - Ожидаемый результат: `"vergleichen Sie anschließend mit Ihrem Partner"` (без изменений)

5. **Словосочетание с артиклем:**
   - Вход: `"der Hund"`
   - Ожидаемый результат: `"der Hund"` (без изменений)

---

## 💡 Дополнительные улучшения (опционально)

### Вариант 1: Умное определение словосочетаний

Можно улучшить определение, учитывая артикли:

```typescript
function isSingleWord(text: string): boolean {
  const trimmed = text.trim();
  const words = trimmed.split(/\s+/);
  
  // Если одно слово - точно отдельное слово
  if (words.length === 1) {
    return true;
  }
  
  // Если два слова и первое - артикль (der, die, das, ein, eine)
  if (words.length === 2) {
    const firstWord = words[0].toLowerCase();
    const articles = ['der', 'die', 'das', 'ein', 'eine', 'einen', 'einem', 'einer'];
    if (articles.includes(firstWord)) {
      // Это существительное с артиклем - можно обработать как словосочетание
      return false; // Не обрабатываем, так как артикль уже есть
    }
  }
  
  // Все остальное - словосочетание/предложение
  return false;
}
```

### Вариант 2: Настройка пользователя

Можно добавить настройку, позволяющую пользователю выбирать:
- Обрабатывать все слова (включая словосочетания) — не рекомендуется
- Обрабатывать только отдельные слова — рекомендуется (по умолчанию)
- Не обрабатывать вообще

---

## ✅ Чек-лист для фронтенда

- [ ] Добавлена проверка на словосочетания/предложения перед вызовом `process_german_word`
- [ ] Словосочетания и предложения не обрабатываются и возвращаются без изменений
- [ ] Отдельные слова обрабатываются как обычно (добавление артикля, исправление регистра)
- [ ] Ошибки обработки не ломают весь процесс
- [ ] Пользователь видит, какие слова будут обработаны, а какие нет
- [ ] Тестирование с различными типами входных данных (слово, словосочетание, предложение)

---

## 📝 Примеры использования

### Пример 1: Обработка смешанного списка

```typescript
const words = [
  "haus",                                    // Отдельное слово
  "gehen",                                   // Отдельное слово
  "das Haus",                                // Словосочетание
  "vergleichen Sie anschließend mit Ihrem Partner"  // Предложение
];

const processed = await processGermanWordsList(words);

// Результат:
// [
//   "das Haus",                              // Обработано
//   "gehen",                                 // Обработано
//   "das Haus",                              // Без изменений
//   "vergleichen Sie anschließend mit Ihrem Partner"  // Без изменений
// ]
```

### Пример 2: Визуальная индикация

```typescript
const { singleWords, phrases } = categorizeWords(words);

// Показываем пользователю:
if (phrases.length > 0) {
  showInfo(
    `Найдено ${phrases.length} словосочетаний/предложений. ` +
    `Они будут сохранены без обработки (без добавления артиклей).`
  );
}
```

---

**Последнее обновление:** 7 декабря 2025

