# Анализ: Асинхронная генерация медиафайлов

## Текущая ситуация

**Проблема:** При генерации медиафайлов для большого количества слов (например, 100) пользователь не может использовать другие функции приложения.

### Почему это происходит?

1. **Бэкенд (синхронная обработка):**
   - Каждый запрос `POST /api/media/generate-image/` и `POST /api/media/generate-audio/` обрабатывается **синхронно**
   - HTTP-соединение остается открытым до завершения генерации (может занять 5-30 секунд на слово)
   - Для 100 слов это может занять **8-50 минут** общего времени

2. **Фронтенд (блокировка UI):**
   - Фронтенд делает последовательные запросы: `await generateImage(word1)`, затем `await generateImage(word2)`, и т.д.
   - Пока идет генерация, показывается модальное окно/индикатор прогресса, которое **блокирует взаимодействие**
   - Или фронтенд просто ждет завершения всех запросов перед разблокировкой UI

---

## Это проблема бэкенда или фронтенда?

### Оба! Но в разной степени:

#### 🔴 **Бэкенд (основная проблема):**
- Синхронная обработка блокирует HTTP-соединение
- Нет механизма фоновых задач
- Нет возможности отслеживать прогресс генерации

#### 🟡 **Фронтенд (вторичная проблема):**
- UI блокируется во время генерации
- Нет механизма для работы с фоновыми задачами
- Нет уведомлений о завершении генерации

---

## Варианты решения

### Вариант 1: Простое решение (только фронтенд) ⭐ **РЕКОМЕНДУЕТСЯ ДЛЯ НАЧАЛА**

**Сложность:** 🟢 Низкая (1-2 дня)  
**Эффективность:** 🟡 Средняя (решает проблему блокировки UI, но не ускоряет генерацию)

#### Что делать:

1. **Не блокировать UI:**
   ```typescript
   // ❌ ПЛОХО: Блокирует UI
   const generateAll = async () => {
     setLoading(true); // Блокирует весь UI
     for (const word of words) {
       await generateImage(word);
     }
     setLoading(false);
   };
   
   // ✅ ХОРОШО: Не блокирует UI
   const generateAll = async () => {
     // Показываем уведомление/бейдж с прогрессом
     setGenerationProgress({ total: words.length, current: 0 });
     
     // Запускаем генерацию в фоне
     generateInBackground(words);
   };
   
   const generateInBackground = async (words: string[]) => {
     for (let i = 0; i < words.length; i++) {
       try {
         await generateImage(words[i]);
         setGenerationProgress(prev => ({ ...prev, current: i + 1 }));
       } catch (error) {
         console.error(`Ошибка генерации для ${words[i]}:`, error);
       }
     }
   };
   ```

2. **Показывать прогресс без блокировки:**
   - Бейдж/индикатор в Header: "Генерация: 45/100"
   - Уведомления при завершении
   - Возможность отменить генерацию

3. **Разрешить работу с другими функциями:**
   - Пользователь может открывать другие колоды
   - Пользователь может редактировать слова
   - Генерация продолжается в фоне

#### Преимущества:
- ✅ Быстро реализуется
- ✅ Не требует изменений в бэкенде
- ✅ Решает проблему блокировки UI

#### Недостатки:
- ❌ Генерация все еще медленная (последовательная)
- ❌ Если пользователь закроет вкладку, генерация прервется
- ❌ Нет возможности возобновить прерванную генерацию

---

### Вариант 2: Фоновые задачи на бэкенде (Celery) ⭐⭐⭐ **ИДЕАЛЬНОЕ РЕШЕНИЕ**

**Сложность:** 🔴 Высокая (5-7 дней)  
**Эффективность:** 🟢 Высокая (полностью решает проблему)

#### Что нужно сделать:

1. **Бэкенд:**
   - Установить и настроить **Celery** (или **Django-Q** для простоты)
   - Создать фоновые задачи для генерации медиа
   - Добавить модель `GenerationTask` для отслеживания прогресса:
     ```python
     class GenerationTask(models.Model):
         user = models.ForeignKey(User, on_delete=models.CASCADE)
         task_id = models.CharField(max_length=255)  # ID задачи Celery
         status = models.CharField(choices=[
             ('pending', 'Ожидает'),
             ('processing', 'Обрабатывается'),
             ('completed', 'Завершена'),
             ('failed', 'Ошибка')
         ])
         progress = models.IntegerField(default=0)  # 0-100
         total_words = models.IntegerField()
         completed_words = models.IntegerField(default=0)
         created_at = models.DateTimeField(auto_now_add=True)
         updated_at = models.DateTimeField(auto_now=True)
     ```
   
   - Новые endpoints:
     ```python
     # POST /api/media/generate-batch/
     # Запускает фоновую задачу, возвращает task_id
     
     # GET /api/media/tasks/{task_id}/
     # Возвращает статус и прогресс задачи
     
     # DELETE /api/media/tasks/{task_id}/
     # Отменяет задачу
     ```

2. **Фронтенд:**
   - Запуск генерации: `POST /api/media/generate-batch/` → получает `task_id`
   - Периодический опрос статуса: `GET /api/media/tasks/{task_id}/` (каждые 2-3 секунды)
   - Отображение прогресса без блокировки UI
   - Уведомления при завершении

#### Преимущества:
- ✅ Генерация идет в фоне, не блокирует HTTP-соединения
- ✅ Можно закрыть вкладку — генерация продолжится
- ✅ Можно отслеживать прогресс
- ✅ Можно отменить задачу
- ✅ Можно обрабатывать несколько задач параллельно

#### Недостатки:
- ❌ Требует установки и настройки Celery (Redis/RabbitMQ)
- ❌ Более сложная архитектура
- ❌ Нужно обрабатывать ошибки и откаты

#### Пример кода бэкенда:

```python
# tasks.py
from celery import shared_task
from apps.cards.llm_utils import generate_image, generate_audio_with_tts
from apps.cards.models import GenerationTask, Word

@shared_task
def generate_media_batch(task_id, words_data):
    """
    Генерирует медиафайлы для списка слов в фоне
    """
    task = GenerationTask.objects.get(id=task_id)
    task.status = 'processing'
    task.save()
    
    total = len(words_data)
    
    for i, word_data in enumerate(words_data):
        try:
            # Генерация изображения
            if word_data.get('generate_image'):
                generate_image(...)
            
            # Генерация аудио
            if word_data.get('generate_audio'):
                generate_audio_with_tts(...)
            
            # Обновляем прогресс
            task.completed_words = i + 1
            task.progress = int((i + 1) / total * 100)
            task.save()
            
        except Exception as e:
            logger.error(f"Ошибка генерации для {word_data['word']}: {e}")
            # Продолжаем со следующим словом
    
    task.status = 'completed'
    task.save()
```

```python
# views.py
@api_view(['POST'])
@permission_classes([IsAuthenticated])
def generate_media_batch_view(request):
    """
    Запускает фоновую задачу генерации медиа
    """
    words_data = request.data.get('words', [])
    
    # Создаем задачу
    task = GenerationTask.objects.create(
        user=request.user,
        total_words=len(words_data),
        status='pending'
    )
    
    # Запускаем фоновую задачу
    generate_media_batch.delay(task.id, words_data)
    
    return Response({
        'task_id': task.id,
        'status': 'pending',
        'progress': 0
    })
```

---

### Вариант 3: Гибридное решение (упрощенный вариант 2)

**Сложность:** 🟡 Средняя (3-4 дня)  
**Эффективность:** 🟢 Высокая

#### Что делать:

1. **Бэкенд:**
   - Использовать **Django-Q** вместо Celery (проще в настройке, не требует Redis)
   - Или использовать простой механизм с моделью `GenerationTask` и периодической проверкой статуса

2. **Фронтенд:**
   - То же, что в варианте 2

#### Преимущества:
- ✅ Проще, чем Celery
- ✅ Все еще решает проблему блокировки

#### Недостатки:
- ❌ Менее масштабируемо, чем Celery
- ❌ Может быть медленнее при большом количестве задач

---

## Рекомендация

### Для быстрого решения (сейчас):
**Вариант 1** — просто не блокировать UI на фронтенде. Это решит проблему блокировки за 1-2 дня.

### Для правильного решения (позже):
**Вариант 2** — фоновые задачи с Celery. Это правильная архитектура для долгосрочного решения.

---

## Оценка сложности

| Вариант | Бэкенд | Фронтенд | Общее время |
|---------|--------|----------|-------------|
| Вариант 1 | 0 дней | 1-2 дня | 1-2 дня |
| Вариант 2 | 3-4 дня | 2-3 дня | 5-7 дней |
| Вариант 3 | 2-3 дня | 2-3 дня | 3-4 дня |

---

## Вопросы для обсуждения

1. **Как часто пользователи генерируют 100+ слов?**
   - Если редко → Вариант 1 достаточен
   - Если часто → Нужен Вариант 2

2. **Нужна ли возможность отменить генерацию?**
   - Если да → Нужен Вариант 2

3. **Нужна ли возможность возобновить прерванную генерацию?**
   - Если да → Нужен Вариант 2

4. **Есть ли возможность установить Redis/RabbitMQ?**
   - Если нет → Вариант 3 (Django-Q)

---

## Следующие шаги

1. **Сейчас:** Реализовать Вариант 1 (не блокировать UI)
2. **Позже:** Планировать Вариант 2 (фоновые задачи)
3. **Документировать:** Добавить в `FRONTEND_TASKS.md` этап про асинхронную генерацию

---

**Последнее обновление:** 7 декабря 2025

