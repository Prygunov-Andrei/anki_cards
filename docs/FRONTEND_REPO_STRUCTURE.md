# Структура проекта: Фронтенд и Бэкенд

## 🎯 Рекомендация: Монорепозиторий ⭐

**Рекомендую использовать один репозиторий для фронтенда и бэкенда** (монорепозиторий).

### Преимущества монорепозитория:

1. ✅ **Единая версионность** — фронтенд и бэкенд всегда синхронизированы
2. ✅ **Упрощенный деплой** — один репозиторий, одна команда деплоя
3. ✅ **Общая документация** — все в одном месте
4. ✅ **Проще управление** — один Git репозиторий
5. ✅ **Совместные изменения** — изменения API и фронтенда в одном коммите

### Структура монорепозитория:

```
anki-card-generator/
├── backend/              # Django проект
│   ├── apps/
│   ├── config/
│   ├── manage.py
│   └── requirements.txt
├── frontend/            # React/TypeScript проект (НОВОЕ)
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── contexts/    # React контексты
│   │   ├── hooks/       # Custom hooks
│   │   ├── lib/         # Библиотеки/утилиты
│   │   ├── locales/     # Локализация
│   │   ├── pages/       # Страницы
│   │   ├── services/    # API сервисы
│   │   ├── styles/      # Стили
│   │   ├── types/       # TypeScript типы
│   │   ├── utils/       # Утилиты
│   │   ├── App.tsx      # Главный компонент
│   │   └── index.tsx    # Точка входа
│   ├── public/          # Статические файлы
│   ├── package.json     # Зависимости
│   ├── tsconfig.json    # TypeScript конфигурация
│   └── vite.config.ts   # Vite конфигурация
├── docs/                # Документация
├── nginx/               # Nginx конфигурация
├── .gitignore           # Игнорируемые файлы
└── README.md            # Главный README
```

---

## 📋 План интеграции

### Шаг 1: Создать структуру фронтенда в текущем репозитории

```bash
# В корне проекта
mkdir frontend
cd frontend
```

### Шаг 2: Скопировать код из Figma Make

Скопируйте все файлы и папки из Figma Make в `frontend/`:

```
frontend/
├── src/
│   ├── components/      # ← скопировать из Figma Make
│   ├── contexts/        # ← скопировать из Figma Make
│   ├── hooks/           # ← скопировать из Figma Make
│   ├── lib/             # ← скопировать из Figma Make
│   ├── locales/         # ← скопировать из Figma Make
│   ├── pages/           # ← скопировать из Figma Make
│   ├── services/        # ← скопировать из Figma Make
│   ├── styles/          # ← скопировать из Figma Make
│   ├── types/           # ← скопировать из Figma Make
│   ├── utils/           # ← скопировать из Figma Make
│   └── App.tsx          # ← скопировать из Figma Make
├── public/              # ← скопировать из Figma Make (если есть)
├── package.json         # ← скопировать из Figma Make
├── tsconfig.json        # ← скопировать из Figma Make
└── vite.config.ts       # ← скопировать из Figma Make (или webpack.config.js)
```

### Шаг 3: Обновить .gitignore

Добавить в `.gitignore`:

```gitignore
# Frontend
frontend/node_modules/
frontend/dist/
frontend/build/
frontend/.env.local
frontend/.env.production
frontend/.vite/

# Backend (уже должно быть)
backend/venv/
backend/__pycache__/
backend/*.pyc
backend/db.sqlite3
backend/media/
backend/*.log
```

### Шаг 4: Обновить README.md

Добавить инструкции по запуску фронтенда.

---

## 🔄 Альтернатива: Отдельный репозиторий

Если вы предпочитаете отдельный репозиторий:

### Преимущества:
- ✅ Разделение ответственности
- ✅ Независимые версии
- ✅ Разные права доступа

### Недостатки:
- ❌ Сложнее синхронизация версий
- ❌ Два репозитория для управления
- ❌ Сложнее деплой

### Структура:

```
anki-card-generator-backend/  # Текущий репозиторий
anki-card-generator-frontend/  # Новый репозиторий
```

---

## 💡 Моя рекомендация

**Использовать монорепозиторий** по следующим причинам:

1. **Проект небольшой** — один репозиторий проще управлять
2. **Тесная интеграция** — фронтенд и бэкенд тесно связаны
3. **Упрощенный деплой** — один репозиторий = один деплой
4. **Стандартная практика** — многие проекты используют монорепозитории

---

## 📝 Что нужно сделать

### Если используем монорепозиторий (рекомендуется):

1. **Создать папку `frontend/` в текущем репозитории:**
   ```bash
   cd /Users/andrei_prygunov/obsidian/deutsch/anki_cards
   mkdir frontend
   ```

2. **Скопировать код из Figma Make:**
   - Все папки (`components`, `contexts`, `hooks`, и т.д.)
   - Все файлы (`App.tsx`, `package.json`, и т.д.)
   - В папку `frontend/`

3. **Обновить `.gitignore`:**
   - Добавить `frontend/node_modules/`
   - Добавить `frontend/dist/`

4. **Закоммитить изменения:**
   ```bash
   git add frontend/
   git commit -m "Add frontend code from Figma Make"
   ```

### Если используем отдельный репозиторий:

1. **Создать новый репозиторий на GitHub:**
   - Название: `anki-card-generator-frontend`

2. **Скопировать код из Figma Make:**
   - В новый репозиторий

3. **Настроить связи:**
   - В README указать ссылку на бэкенд репозиторий
   - В бэкенд README указать ссылку на фронтенд

---

## 🚀 После копирования кода

После того, как вы скопируете код в репозиторий, я помогу:

1. ✅ Проверить структуру проекта
2. ✅ Настроить зависимости (`package.json`)
3. ✅ Обновить конфигурацию API (baseURL)
4. ✅ Адаптировать пути импортов
5. ✅ Настроить сборку (Vite/Webpack)
6. ✅ Подготовить к деплою

---

## 📋 Чек-лист перед копированием

Перед копированием кода убедитесь, что у вас есть:

- [ ] Все папки из структуры (components, pages, services, и т.д.)
- [ ] `package.json` с зависимостями
- [ ] `tsconfig.json` (если используется TypeScript)
- [ ] Конфигурация сборки (vite.config.ts или webpack.config.js)
- [ ] `.env.example` или документация по переменным окружения
- [ ] `README.md` с инструкциями (если есть)

---

**Рекомендация:** Используйте монорепозиторий — это проще и удобнее для вашего проекта.

**Последнее обновление:** 7 декабря 2025

