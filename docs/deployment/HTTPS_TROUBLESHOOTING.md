# 🔧 Диагностика проблем с HTTPS для get-anki.fun

## Текущая ситуация

- ✅ HTTP работает: http://www.get-anki.fun/admin/
- ❌ HTTPS не работает: https://www.get-anki.fun/admin/

## Пошаговая диагностика

### 1. Проверка DNS и маршрутизации

```bash
# Проверка DNS записи
nslookup www.get-anki.fun

# Должно вернуть: 72.56.83.95
```

**Если DNS неверный:**
- Проверьте настройки DNS у регистратора домена
- Убедитесь, что A-запись указывает на `72.56.83.95`

### 2. Проверка портов на сервере

```bash
ssh root@72.56.83.95
netstat -tlnp | grep ':443\|:80'
# или
ss -tlnp | grep ':443\|:80'
```

**Ожидаемый результат:**
- Порт 80 должен быть открыт (HTTP)
- Порт 443 может быть закрыт, если используется Cloudflare

**Если порт 443 закрыт:**
- Это нормально, если используется Cloudflare (Flexible SSL)
- Cloudflare обрабатывает HTTPS и проксирует на HTTP

### 3. Проверка Cloudflare (если используется)

#### 3.1. Проверка SSL/TLS режима

В панели Cloudflare:
1. Зайдите в DNS настройки домена
2. Перейдите в SSL/TLS → Overview
3. Проверьте режим SSL/TLS:
   - **Flexible** - Cloudflare → Сервер (HTTP) ❌ НЕ РАБОТАЕТ для админки
   - **Full** - Cloudflare → Сервер (HTTPS) ✅ Рекомендуется
   - **Full (strict)** - Cloudflare → Сервер (HTTPS с проверкой) ✅ Лучший вариант

#### 3.2. Проверка проксирования

В DNS настройках:
- Оранжевое облако = проксирование включено ✅
- Серое облако = проксирование выключено ❌

#### 3.3. Проверка сертификата

```bash
# Проверка сертификата
echo | openssl s_client -connect www.get-anki.fun:443 -servername www.get-anki.fun 2>&1 | grep -E "subject=|issuer=|Verify return code"
```

**Ожидаемый результат:**
- Issuer должен быть Cloudflare или Let's Encrypt
- Verify return code: 0 (ok)

### 4. Проверка конфигурации Nginx

```bash
ssh root@72.56.83.95
cd /opt/anki_cards
docker compose exec frontend cat /etc/nginx/conf.d/default.conf | grep -E 'listen|server_name|ssl'
```

**Текущая конфигурация:**
- Nginx слушает только порт 80 (HTTP)
- SSL не настроен напрямую на сервере
- Это нормально, если используется Cloudflare

### 5. Проверка настроек Django

```bash
ssh root@72.56.83.95
cd /opt/anki_cards
docker compose exec backend python manage.py shell
```

```python
from django.conf import settings
print('ALLOWED_HOSTS:', settings.ALLOWED_HOSTS)
print('CSRF_TRUSTED_ORIGINS:', settings.CSRF_TRUSTED_ORIGINS)
print('SECURE_PROXY_SSL_HEADER:', getattr(settings, 'SECURE_PROXY_SSL_HEADER', 'Not set'))
```

**Ожидаемые значения:**
- `ALLOWED_HOSTS` должен содержать `www.get-anki.fun` и `get-anki.fun`
- `CSRF_TRUSTED_ORIGINS` должен содержать `https://www.get-anki.fun`
- `SECURE_PROXY_SSL_HEADER` должен быть `('HTTP_X_FORWARDED_PROTO', 'https')`

### 6. Проверка логов

```bash
ssh root@72.56.83.95
cd /opt/anki_cards

# Логи frontend (Nginx)
docker compose logs --tail=50 frontend | grep -i 'error\|warn\|443\|ssl'

# Логи backend (Django)
docker compose logs --tail=50 backend | grep -i 'error\|disallowed\|400\|403'
```

### 7. Проверка заголовков запроса

```bash
# Проверка заголовков от Cloudflare
curl -I https://www.get-anki.fun/admin/

# Должны быть заголовки:
# - CF-Ray (Cloudflare)
# - Server: cloudflare или nginx
```

## Возможные проблемы и решения

### Проблема 1: Cloudflare SSL режим = "Flexible"

**Симптомы:**
- HTTPS запросы не доходят до сервера
- Cloudflare проксирует на HTTP, но Django не понимает, что запрос был HTTPS

**Решение:**
1. В Cloudflare: SSL/TLS → Overview → измените на "Full" или "Full (strict)"
2. Подождите 1-2 минуты для применения изменений
3. Проверьте снова

### Проблема 2: Проксирование выключено в Cloudflare

**Симптомы:**
- DNS запись показывает серое облако
- Запросы идут напрямую на сервер, минуя Cloudflare

**Решение:**
1. В Cloudflare: DNS → найдите запись для `www.get-anki.fun`
2. Включите проксирование (оранжевое облако)
3. Подождите 1-2 минуты

### Проблема 3: Django не видит HTTPS из-за прокси

**Симптомы:**
- Запросы доходят до сервера, но Django возвращает 400 или редирект на HTTP

**Решение:**
- Убедитесь, что `SECURE_PROXY_SSL_HEADER` настроен правильно
- Проверьте, что Nginx передает `X-Forwarded-Proto` от Cloudflare

### Проблема 4: Домен не в ALLOWED_HOSTS

**Симптомы:**
- Django возвращает 400 Bad Request
- В логах: `DisallowedHost: Invalid HTTP_HOST header`

**Решение:**
- Добавьте домен в `ALLOWED_HOSTS` (уже сделано)
- Перезапустите backend: `docker compose restart backend`

### Проблема 5: CSRF ошибки

**Симптомы:**
- Страница загружается, но формы не работают
- Ошибка 403 Forbidden при отправке форм

**Решение:**
- Убедитесь, что `https://www.get-anki.fun` в `CSRF_TRUSTED_ORIGINS`
- Проверьте, что `SECURE_PROXY_SSL_HEADER` настроен

## Чеклист для проверки

- [ ] DNS запись указывает на правильный IP (72.56.83.95)
- [ ] Cloudflare проксирование включено (оранжевое облако)
- [ ] Cloudflare SSL/TLS режим = "Full" или "Full (strict)"
- [ ] `ALLOWED_HOSTS` содержит `www.get-anki.fun` и `get-anki.fun`
- [ ] `CSRF_TRUSTED_ORIGINS` содержит `https://www.get-anki.fun`
- [ ] `SECURE_PROXY_SSL_HEADER` настроен правильно
- [ ] Nginx передает `X-Forwarded-Proto` от Cloudflare
- [ ] Backend перезапущен после изменений

## Быстрая проверка

```bash
# 1. Проверка DNS
nslookup www.get-anki.fun

# 2. Проверка HTTPS доступности
curl -I https://www.get-anki.fun/admin/

# 3. Проверка сертификата
echo | openssl s_client -connect www.get-anki.fun:443 -servername www.get-anki.fun 2>&1 | grep "Verify return code"

# 4. Проверка заголовков
curl -v https://www.get-anki.fun/admin/ 2>&1 | grep -E "HTTP|Location|CF-Ray|Server"
```

## Контакты для помощи

Если проблема не решается:
1. Проверьте логи на сервере
2. Проверьте настройки Cloudflare
3. Убедитесь, что все изменения применены и контейнеры перезапущены

