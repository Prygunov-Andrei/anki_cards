# 🖥️ Информация о продакшн сервере

## Данные сервера

- **IPv4:** 72.56.83.95
- **IPv6:** 2a03:6f02::1:ef0
- **SSH:** `ssh root@72.56.83.95`
- **Root-пароль:** `(set via SSH_PASSWORD env variable)`
- **Путь деплоя:** `/opt/anki_cards`

## Закрытые порты

Следующие порты закрыты на сервере:
- 2525, 53413, 389, 587, 465, 25, 3389

## Используемые порты

- **80** - Frontend (Nginx)
- **8000** - Backend (Gunicorn, внутренний)
- **5432** - PostgreSQL (внутренний, только между контейнерами)

## Быстрый деплой

### 1. Создание бэкапа (если нужно)
```bash
./scripts/backup_data.sh
```

### 2. Настройка переменных окружения на сервере
```bash
./scripts/setup_server_env.sh
```

Скрипт запросит:
- OPENAI_API_KEY
- GEMINI_API_KEY (опционально)

И автоматически сгенерирует:
- SECRET_KEY
- Пароль для БД

### 3. Деплой
```bash
./scripts/deploy.sh
```

Или с указанием параметров:
```bash
./scripts/deploy.sh root@72.56.83.95 /opt/anki_cards
```

## Ручное подключение

```bash
ssh root@72.56.83.95
# Пароль: (set via SSH_PASSWORD env variable)
```

## Проверка после деплоя

```bash
# Проверка контейнеров
ssh root@72.56.83.95 "cd /opt/anki_cards && docker-compose ps"

# Проверка логов
ssh root@72.56.83.95 "cd /opt/anki_cards && docker-compose logs --tail=50"

# Health check
curl http://72.56.83.95/api/health/
```

## Доступ к приложению

После успешного деплоя приложение будет доступно:
- **Frontend:** http://72.56.83.95/
- **API:** http://72.56.83.95/api/
- **Admin:** http://72.56.83.95/admin/

## Безопасность

⚠️ **ВАЖНО:**
1. После первого деплоя рекомендуется:
   - Настроить SSH ключи вместо пароля
   - Изменить root-пароль
   - Настроить файрвол
   - Настроить SSL/HTTPS

2. Не коммитьте пароли в Git!

3. Храните секретные ключи в безопасном месте.

