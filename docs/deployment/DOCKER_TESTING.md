# 🧪 Локальное тестирование Docker

## Предварительные требования

1. **Docker Desktop** должен быть запущен
2. **Docker** и **Docker Compose** установлены
3. **Переменные окружения** настроены (опционально для тестирования)

---

## 🚀 Быстрый старт

### 1. Проверка Docker

```bash
docker --version
docker-compose --version
docker ps  # Должен работать без ошибок
```

### 2. Сборка образов

```bash
docker-compose build
```

**Или без кэша (для чистой сборки):**
```bash
docker-compose build --no-cache
```

### 3. Запуск контейнеров

```bash
docker-compose up -d
```

### 4. Проверка статуса

```bash
docker-compose ps
```

Должны быть запущены:
- `anki_db` (PostgreSQL)
- `anki_backend` (Django)
- `anki_frontend` (Nginx)

### 5. Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

---

## ✅ Проверка работы

### Frontend
Откройте в браузере: http://localhost

### Backend API
```bash
curl http://localhost/api/health/
```

Должен вернуть:
```json
{"status": "ok", "message": "Backend is running"}
```

### Admin панель
Откройте в браузере: http://localhost/admin/

### База данных
```bash
# Подключение к БД
docker-compose exec db psql -U anki_user -d anki_db

# Проверка таблиц
\dt
```

---

## 🛠️ Полезные команды

### Остановка контейнеров
```bash
docker-compose down
```

### Перезапуск
```bash
docker-compose restart
```

### Пересборка и перезапуск
```bash
docker-compose up -d --build
```

### Выполнение команд в контейнере

```bash
# Django shell
docker-compose exec backend python manage.py shell

# Создание суперпользователя
docker-compose exec backend python manage.py createsuperuser

# Применение миграций
docker-compose exec backend python manage.py migrate

# Сборка статики
docker-compose exec backend python manage.py collectstatic --noinput
```

### Очистка

```bash
# Остановка и удаление контейнеров
docker-compose down

# Удаление образов
docker-compose down --rmi all

# Удаление volumes (⚠️ удалит данные БД!)
docker-compose down -v
```

---

## 🐛 Решение проблем

### Проблема: Docker daemon не запущен

**Решение:**
1. Запустите Docker Desktop
2. Дождитесь полной загрузки
3. Проверьте: `docker ps`

### Проблема: Порт уже занят

**Решение:**
```bash
# Проверьте, что использует порт
lsof -i :80
lsof -i :8000

# Или измените порты в docker-compose.yml
```

### Проблема: Ошибка при сборке frontend

**Решение:**
```bash
# Проверьте наличие package.json
ls frontend/package.json

# Проверьте логи сборки
docker-compose build frontend --no-cache
```

### Проблема: Backend не подключается к БД

**Решение:**
```bash
# Проверьте статус БД
docker-compose ps db

# Проверьте логи
docker-compose logs db

# Проверьте переменные окружения
docker-compose exec backend env | grep DATABASE
```

### Проблема: Медиафайлы не отдаются

**Решение:**
```bash
# Проверьте права доступа
docker-compose exec backend ls -la /app/media/

# Проверьте конфигурацию Nginx
docker-compose exec frontend cat /etc/nginx/conf.d/default.conf
```

---

## 📊 Мониторинг ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Размер образов
docker images

# Размер volumes
docker system df -v
```

---

## 🧹 Очистка после тестирования

```bash
# Остановка контейнеров
docker-compose down

# Удаление неиспользуемых образов
docker image prune -a

# Полная очистка (⚠️ удалит все неиспользуемые ресурсы!)
docker system prune -a --volumes
```

---

## ✅ Чек-лист тестирования

- [ ] Docker запущен
- [ ] Образы собраны успешно
- [ ] Контейнеры запущены
- [ ] Frontend доступен на http://localhost
- [ ] Backend API отвечает на /api/health/
- [ ] Admin панель доступна
- [ ] База данных работает
- [ ] Миграции применены
- [ ] Статические файлы собраны
- [ ] Логи без критических ошибок

---

**Последнее обновление:** 7 декабря 2025

