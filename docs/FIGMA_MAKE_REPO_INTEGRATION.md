# Интеграция фронтенда из репозитория Figma Make

## 🎯 Проблема

Figma Make создает свой собственный репозиторий на GitHub, а не коммитит в существующий репозиторий `anki_cards`.

## ✅ Решение: Клонирование и копирование

### Вариант 1: Клонирование и ручное копирование ⭐ **РЕКОМЕНДУЕТСЯ**

**Шаги:**

1. **Клонировать репозиторий Figma Make:**
   ```bash
   # Временная папка для клонирования
   cd /tmp
   git clone https://github.com/your-username/figma-make-repo-name.git figma-frontend
   ```

2. **Скопировать файлы в текущий проект:**
   ```bash
   cd /Users/andrei_prygunov/obsidian/deutsch/anki_cards
   
   # Создать папку frontend (если еще нет)
   mkdir -p frontend
   
   # Скопировать все файлы из репозитория Figma Make
   cp -r /tmp/figma-frontend/src frontend/  # если структура src/
   # или
   cp -r /tmp/figma-frontend/* frontend/    # если файлы в корне
   ```

3. **Очистить временные файлы:**
   ```bash
   rm -rf /tmp/figma-frontend
   ```

---

### Вариант 2: Добавить remote и merge

**Шаги:**

1. **Добавить репозиторий Figma Make как remote:**
   ```bash
   cd /Users/andrei_prygunov/obsidian/deutsch/anki_cards
   git remote add figma-frontend https://github.com/your-username/figma-make-repo-name.git
   ```

2. **Получить изменения:**
   ```bash
   git fetch figma-frontend
   ```

3. **Создать ветку для интеграции:**
   ```bash
   git checkout -b integrate-figma-frontend
   ```

4. **Скопировать файлы из remote:**
   ```bash
   # Создать папку frontend
   mkdir -p frontend
   
   # Скопировать файлы из remote ветки
   git checkout figma-frontend/main -- .
   # или указать конкретные файлы/папки
   ```

5. **Переместить файлы в правильную структуру:**
   - Переместить файлы в `frontend/`
   - Обновить пути импортов

6. **Закоммитить:**
   ```bash
   git add frontend/
   git commit -m "Add frontend code from Figma Make"
   git checkout main
   git merge integrate-figma-frontend
   ```

---

### Вариант 3: Скачать ZIP архив

**Шаги:**

1. **Скачать ZIP архив репозитория Figma Make:**
   - На странице репозитория GitHub: `Code` → `Download ZIP`

2. **Распаковать:**
   ```bash
   cd /tmp
   unzip figma-make-repo.zip
   ```

3. **Скопировать файлы:**
   ```bash
   cd /Users/andrei_prygunov/obsidian/deutsch/anki_cards
   mkdir -p frontend
   cp -r /tmp/figma-make-repo/* frontend/
   ```

---

## 🔍 Определение структуры репозитория Figma Make

Перед копированием нужно понять структуру:

1. **Открыть репозиторий на GitHub:**
   - Посмотреть структуру файлов
   - Определить, где находятся компоненты

2. **Возможные структуры:**

   **Вариант A: Файлы в корне**
   ```
   figma-make-repo/
   ├── components/
   ├── pages/
   ├── App.tsx
   ├── package.json
   └── ...
   ```

   **Вариант B: Файлы в src/**
   ```
   figma-make-repo/
   ├── src/
   │   ├── components/
   │   ├── pages/
   │   └── App.tsx
   ├── package.json
   └── ...
   ```

   **Вариант C: Другая структура**
   - Нужно проанализировать конкретную структуру

---

## 📋 Пошаговый план (рекомендуемый)

### Шаг 1: Получить URL репозитория Figma Make

1. Откройте репозиторий на GitHub
2. Скопируйте URL (например: `https://github.com/your-username/repo-name.git`)

### Шаг 2: Клонировать репозиторий

```bash
# Временная папка
cd /tmp
git clone https://github.com/your-username/figma-make-repo-name.git
cd figma-make-repo-name

# Посмотреть структуру
ls -la
tree -L 2  # если установлен tree
```

### Шаг 3: Проанализировать структуру

Определить:
- Где находятся компоненты?
- Где находится `package.json`?
- Какая структура папок?

### Шаг 4: Скопировать файлы

```bash
cd /Users/andrei_prygunov/obsidian/deutsch/anki_cards

# Создать папку frontend
mkdir -p frontend

# Скопировать файлы (адаптировать под вашу структуру)
# Если файлы в корне репозитория:
cp -r /tmp/figma-make-repo-name/src frontend/
cp /tmp/figma-make-repo-name/package.json frontend/
cp /tmp/figma-make-repo-name/tsconfig.json frontend/  # если есть
cp /tmp/figma-make-repo-name/vite.config.ts frontend/  # если есть

# Или если структура другая - скопировать все:
# cp -r /tmp/figma-make-repo-name/* frontend/
```

### Шаг 5: Очистить и проверить

```bash
# Удалить временный репозиторий
rm -rf /tmp/figma-make-repo-name

# Проверить структуру
cd frontend
ls -la
```

### Шаг 6: Реорганизация (если нужно)

После копирования я помогу:
- Переместить файлы в правильную структуру
- Обновить пути импортов
- Настроить конфигурацию

---

## 🛠️ Автоматический скрипт интеграции

После того, как вы дадите мне URL репозитория Figma Make, я создам скрипт, который:

1. Клонирует репозиторий
2. Анализирует структуру
3. Копирует файлы в правильное место
4. Реорганизует структуру
5. Обновляет конфигурацию

---

## 📝 Что нужно от вас

1. **URL репозитория Figma Make:**
   - Например: `https://github.com/your-username/repo-name.git`
   - Или просто название репозитория

2. **Структура репозитория (опционально):**
   - Скриншот структуры файлов
   - Или описание, где что находится

3. **После этого я:**
   - Клонирую репозиторий
   - Проанализирую структуру
   - Скопирую файлы в правильное место
   - Реорганизую структуру
   - Обновлю конфигурацию

---

## 💡 Альтернатива: Ручное копирование

Если клонирование неудобно, можно:

1. **Скачать ZIP архив** с GitHub
2. **Распаковать локально**
3. **Скопировать файлы вручную** в `frontend/`
4. **Я помогу с реорганизацией**

---

## ✅ Чек-лист

- [ ] Получен URL репозитория Figma Make
- [ ] Репозиторий клонирован или скачан
- [ ] Структура проанализирована
- [ ] Файлы скопированы в `frontend/`
- [ ] Структура реорганизована
- [ ] Конфигурация обновлена
- [ ] Тестирование пройдено

---

**Последнее обновление:** 7 декабря 2025

