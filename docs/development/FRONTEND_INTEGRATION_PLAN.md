# План интеграции фронтенда из Figma Make

## 🎯 Цель

Получить фронтенд код из Figma Make и подготовить его к деплою вместе с бэкендом.

---

## 📋 Варианты получения кода из Figma Make

### Вариант 1: Экспорт через DevTools браузера ⭐ **РЕКОМЕНДУЕТСЯ**

**Как это работает:**
1. Открыть приложение в Figma Make
2. Открыть DevTools (F12)
3. Найти вкладку **Sources** или **Network**
4. Найти загруженные файлы JavaScript/TypeScript
5. Скопировать исходный код компонентов

**Шаги:**

1. **Открыть DevTools:**
   - В Figma Make: `F12` или `Cmd+Option+I` (Mac)
   - Перейти на вкладку **Sources**

2. **Найти исходные файлы:**
   - В левой панели найти `webpack://` или `code_components/`
   - Или найти файлы `.tsx`, `.ts`, `.jsx`, `.js`

3. **Скопировать код:**
   - Открыть каждый файл
   - Скопировать содержимое
   - Сохранить в соответствующий файл в проекте

**Преимущества:**
- ✅ Получаем реальный код, который работает
- ✅ Видим структуру компонентов
- ✅ Можем увидеть зависимости

**Недостатки:**
- ❌ Может быть минифицированный код (сложно читать)
- ❌ Нужно собирать файлы вручную

---

### Вариант 2: Экспорт через Figma Make API (если доступно)

**Как это работает:**
1. Проверить, есть ли в Figma Make возможность экспорта кода
2. Использовать API для получения исходников
3. Или запросить у Figma Make поддержку экспорт

**Шаги:**
- Проверить документацию Figma Make
- Связаться с поддержкой, если нужно

---

### Вариант 3: Ручное копирование компонентов

**Как это работает:**
1. Открыть каждый компонент в Figma Make
2. Скопировать код через интерфейс (если есть такая возможность)
3. Сохранить в файлы проекта

---

## 🏗️ Структура проекта для фронтенда

### Рекомендуемая структура:

```
anki-card-generator/
├── backend/              # Django проект (уже есть)
├── frontend/             # React/TypeScript проект (создать)
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── services/     # API сервисы
│   │   ├── types/        # TypeScript типы
│   │   ├── utils/        # Утилиты
│   │   ├── App.tsx       # Главный компонент
│   │   └── index.tsx     # Точка входа
│   ├── public/           # Статические файлы
│   ├── package.json      # Зависимости
│   ├── tsconfig.json     # TypeScript конфигурация
│   ├── vite.config.ts    # Vite конфигурация (или webpack)
│   └── .env              # Переменные окружения
├── docs/                 # Документация (уже есть)
└── nginx/                # Nginx конфигурация (уже есть)
```

---

## 📝 Пошаговый план интеграции

### Шаг 1: Создание структуры проекта (15 минут)

1. **Создать директорию `frontend/`:**
   ```bash
   mkdir frontend
   cd frontend
   ```

2. **Инициализировать React проект:**
   ```bash
   npm create vite@latest . -- --template react-ts
   # или
   npx create-react-app . --template typescript
   ```

3. **Установить зависимости:**
   ```bash
   npm install axios
   npm install react-router-dom
   # и другие зависимости, которые используются в Figma Make
   ```

---

### Шаг 2: Получение кода из Figma Make (1-2 часа)

**Метод A: Через DevTools (рекомендуется)**

1. **Открыть приложение в браузере:**
   - Запустить приложение в Figma Make
   - Открыть DevTools (`F12`)

2. **Найти исходные файлы:**
   - Вкладка **Sources** → найти файлы `.tsx`, `.ts`
   - Или вкладка **Network** → найти загруженные модули

3. **Скопировать компоненты:**
   - Для каждого компонента:
     - Открыть файл в DevTools
     - Скопировать код
     - Сохранить в `frontend/src/components/ComponentName.tsx`

4. **Скопировать сервисы:**
   - API клиент → `frontend/src/services/api.ts`
   - Другие сервисы → `frontend/src/services/`

5. **Скопировать типы:**
   - TypeScript типы → `frontend/src/types/index.ts`

**Метод B: Через экспорт (если доступно)**

1. Проверить, есть ли в Figma Make функция экспорта
2. Экспортировать проект
3. Распаковать и скопировать файлы

---

### Шаг 3: Адаптация кода (1-2 часа)

1. **Обновить пути импортов:**
   ```typescript
   // Было (в Figma Make):
   import { api } from './api';
   
   // Станет:
   import { api } from '../services/api';
   ```

2. **Обновить конфигурацию API:**
   ```typescript
   // В frontend/src/services/api.ts
   const api = axios.create({
     baseURL: import.meta.env.VITE_API_URL || 'http://localhost:8000/api/',
     // или для production:
     // baseURL: 'https://your-domain.com/api/',
   });
   ```

3. **Проверить зависимости:**
   - Убедиться, что все используемые библиотеки установлены
   - Добавить недостающие в `package.json`

4. **Обновить переменные окружения:**
   ```env
   # frontend/.env
   VITE_API_URL=https://get-anki.fan.ngrok.app/api/
   ```

---

### Шаг 4: Тестирование локально (30 минут)

1. **Запустить бэкенд:**
   ```bash
   cd backend
   source venv/bin/activate
   python manage.py runserver
   ```

2. **Запустить фронтенд:**
   ```bash
   cd frontend
   npm run dev
   ```

3. **Проверить работу:**
   - Открыть `http://localhost:5173` (или другой порт Vite)
   - Проверить подключение к бэкенду
   - Протестировать основные функции

---

### Шаг 5: Подготовка к деплою (1 час)

1. **Сборка production версии:**
   ```bash
   cd frontend
   npm run build
   ```

2. **Настройка Nginx для статики:**
   - Обновить `nginx/nginx.conf` для раздачи статических файлов фронтенда

3. **Интеграция с Django:**
   - Настроить Django для раздачи статики фронтенда
   - Или использовать Nginx для фронтенда и Django для API

---

## 🔧 Альтернативный подход: Минимальная интеграция

Если получение полного кода сложно, можно:

1. **Создать простой React проект:**
   - Базовая структура
   - API клиент
   - Основные компоненты

2. **Постепенно переносить функциональность:**
   - Начать с основных страниц
   - Добавлять компоненты по мере необходимости

---

## 📦 Что нужно от вас

### Минимально необходимое:

1. **Список компонентов:**
   - Какие компоненты есть в приложении
   - Их названия и назначение

2. **API endpoints, которые используются:**
   - Список всех API вызовов
   - Формат запросов и ответов

3. **Структура данных:**
   - Типы данных (User, Deck, Word и т.д.)
   - Формат состояний

### Идеально:

1. **Исходный код компонентов:**
   - `.tsx` или `.ts` файлы
   - Можно скопировать через DevTools

2. **Конфигурация:**
   - `package.json` с зависимостями
   - Настройки сборки

3. **Скриншоты интерфейса:**
   - Для понимания структуры
   - Для проверки после интеграции

---

## 🎯 Рекомендуемый подход

### Вариант 1: Полный экспорт (если возможно)

1. Попробовать экспортировать проект из Figma Make
2. Если есть возможность — получить ZIP архив
3. Распаковать и интегрировать

### Вариант 2: Пошаговый перенос (если экспорт невозможен)

1. **Этап 1:** Создать базовую структуру React проекта
2. **Этап 2:** Настроить API клиент
3. **Этап 3:** Перенести компоненты по одному:
   - Начать с простых (Login, Register)
   - Затем сложные (MainPage, DeckEditor)
4. **Этап 4:** Тестирование и исправление

### Вариант 3: Гибридный подход ⭐ **РЕКОМЕНДУЕТСЯ**

1. **Получить код через DevTools:**
   - Открыть приложение в браузере
   - Найти исходные файлы в DevTools
   - Скопировать основные компоненты

2. **Создать структуру проекта:**
   - Инициализировать React проект
   - Настроить зависимости

3. **Интегрировать код:**
   - Скопировать компоненты
   - Адаптировать импорты
   - Настроить API клиент

4. **Тестирование:**
   - Проверить работу локально
   - Исправить ошибки

---

## 📋 Чек-лист для передачи фронтенда

### Что нужно подготовить:

- [ ] Список всех компонентов с их названиями
- [ ] Скриншоты основных страниц
- [ ] Список используемых API endpoints
- [ ] Типы данных (TypeScript интерфейсы)
- [ ] Зависимости (`package.json` или список библиотек)
- [ ] Конфигурация API (baseURL, headers)
- [ ] Исходный код компонентов (если возможно получить)

### Как передать:

1. **Через файлы:**
   - Создать ZIP архив с кодом
   - Загрузить в репозиторий или передать файлы

2. **Через описание:**
   - Описать структуру компонентов
   - Перечислить функциональность
   - Я создам базовую структуру, а вы дополните

3. **Через скриншоты + код:**
   - Скриншоты интерфейса
   - Код ключевых компонентов
   - Я воссоздам остальное

---

## 🚀 Следующие шаги

1. **Вы определяете способ передачи:**
   - Экспорт из Figma Make
   - Копирование через DevTools
   - Описание структуры

2. **Я создаю базовую структуру проекта:**
   - React + TypeScript
   - Настройка сборки
   - API клиент

3. **Интегрируем код:**
   - Копируем компоненты
   - Адаптируем под структуру проекта
   - Исправляем ошибки

4. **Тестируем:**
   - Локально
   - Подключение к бэкенду
   - Основные функции

5. **Готовим к деплою:**
   - Production сборка
   - Настройка Nginx
   - Деплой

---

## 💡 Рекомендации

**Самый быстрый способ:**

1. Откройте приложение в браузере
2. Откройте DevTools (`F12`)
3. Перейдите на вкладку **Sources**
4. Найдите файлы компонентов (обычно в `webpack://` или похожей структуре)
5. Скопируйте код основных компонентов:
   - `App.tsx` или `MainPage.tsx`
   - `LoginPage.tsx`
   - `DeckEditor.tsx`
   - `api.ts` (API клиент)
6. Отправьте мне эти файлы или их содержимое

**Альтернатива:**

Если код минифицирован или сложно получить:
- Опишите структуру компонентов
- Перечислите основные функции
- Я создам базовую структуру, а вы дополните детали

---

**Последнее обновление:** 7 декабря 2025

