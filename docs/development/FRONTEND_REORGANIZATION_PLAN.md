# План реорганизации: Разделение фронтенда и бэкенда

## ✅ Текущая ситуация

Фронтенд код будет скопирован в репозиторий в любом месте. Позже мы реорганизуем структуру и разделим фронтенд и бэкенд.

---

## 🎯 Целевая структура

```
anki-card-generator/
├── backend/              # Django проект
│   ├── apps/
│   ├── config/
│   ├── manage.py
│   └── requirements.txt
├── frontend/            # React/TypeScript проект
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── services/
│   │   └── ...
│   ├── package.json
│   └── ...
├── docs/                # Документация
└── nginx/               # Nginx конфигурация
```

---

## 📋 План реорганизации (после копирования)

### Шаг 1: Анализ текущей структуры

1. **Определить, где находится фронтенд код:**
   ```bash
   # Найти все файлы фронтенда
   find . -name "*.tsx" -o -name "*.ts" -o -name "package.json" | grep -v node_modules
   ```

2. **Определить структуру:**
   - Где находятся компоненты?
   - Где находится `package.json`?
   - Где находятся конфигурационные файлы?

### Шаг 2: Создание структуры `frontend/`

1. **Создать папку `frontend/`:**
   ```bash
   mkdir frontend
   ```

2. **Определить, какие файлы/папки нужно переместить:**
   - Все `.tsx`, `.ts` файлы (кроме бэкенда)
   - `package.json`
   - `tsconfig.json`
   - `vite.config.ts` или `webpack.config.js`
   - Папки: `components`, `pages`, `services`, `types`, и т.д.
   - Папка `public/` (если есть)

### Шаг 3: Перемещение файлов

**Вариант A: Если фронтенд в корне репозитория**

```bash
# Создать структуру
mkdir -p frontend/src

# Переместить папки
mv components frontend/src/
mv contexts frontend/src/
mv hooks frontend/src/
mv lib frontend/src/
mv locales frontend/src/
mv pages frontend/src/
mv services frontend/src/
mv styles frontend/src/
mv types frontend/src/
mv utils frontend/src/

# Переместить файлы
mv App.tsx frontend/src/
mv index.tsx frontend/src/  # если есть

# Переместить конфигурацию
mv package.json frontend/
mv tsconfig.json frontend/
mv vite.config.ts frontend/  # или webpack.config.js
mv .env frontend/.env.example  # если есть

# Переместить public (если есть)
mv public frontend/  # если есть
```

**Вариант B: Если фронтенд в другой папке**

```bash
# Если фронтенд в папке, например, "src" или "app"
mv src frontend/src  # или как называется папка
mv package.json frontend/
# и т.д.
```

### Шаг 4: Обновление путей импортов

После перемещения нужно обновить пути импортов в файлах:

```typescript
// Было (если файлы были в корне):
import { api } from './services/api';

// Станет:
import { api } from '../services/api';  // или './services/api' если уже в src/
```

### Шаг 5: Обновление конфигурации

1. **Обновить `package.json`** (если нужно):
   ```json
   {
     "name": "anki-card-generator-frontend",
     "scripts": {
       "dev": "vite",
       "build": "vite build",
       "preview": "vite preview"
     }
   }
   ```

2. **Обновить `vite.config.ts`** (если используется Vite):
   ```typescript
   export default defineConfig({
     root: './',
     build: {
       outDir: 'dist',
     },
   })
   ```

### Шаг 6: Обновление .gitignore

```gitignore
# Frontend
frontend/node_modules/
frontend/dist/
frontend/build/
frontend/.env.local
frontend/.vite/

# Backend (уже должно быть)
backend/venv/
backend/__pycache__/
backend/*.pyc
backend/db.sqlite3
backend/media/
```

### Шаг 7: Тестирование

1. **Установить зависимости:**
   ```bash
   cd frontend
   npm install
   ```

2. **Запустить dev сервер:**
   ```bash
   npm run dev
   ```

3. **Проверить работу:**
   - Открыть приложение в браузере
   - Проверить подключение к бэкенду
   - Протестировать основные функции

---

## 🔍 Автоматический скрипт реорганизации

После того, как вы скопируете код, я создам скрипт, который:

1. Определит текущую структуру
2. Создаст папку `frontend/`
3. Переместит файлы в правильные места
4. Обновит пути импортов
5. Обновит конфигурацию

---

## 📝 Что делать сейчас

### Сейчас (перед реорганизацией):

1. **Скопируйте код из Figma Make в репозиторий:**
   - В любое место (корень, папку, как удобно)
   - Главное — скопировать все файлы и папки

2. **Сообщите мне:**
   - Куда вы скопировали код (в какую папку)
   - Какая структура получилась
   - Есть ли `package.json` и где он находится

### Потом (после копирования):

1. Я проанализирую структуру
2. Создам план реорганизации
3. Выполню реорганизацию автоматически
4. Обновлю все пути и конфигурацию
5. Протестирую работу

---

## 💡 Рекомендации при копировании

Чтобы потом было проще реорганизовать:

1. **Скопируйте все файлы и папки:**
   - Не пропускайте ничего
   - Сохраните структуру папок

2. **Сохраните конфигурационные файлы:**
   - `package.json`
   - `tsconfig.json`
   - `vite.config.ts` или `webpack.config.js`
   - `.env` (если есть)

3. **Не удаляйте исходные файлы:**
   - Оставьте их до завершения реорганизации

---

## ✅ Чек-лист после копирования

После того, как скопируете код, проверьте:

- [ ] Все папки скопированы (components, pages, services, и т.д.)
- [ ] Все файлы скопированы (App.tsx, и т.д.)
- [ ] `package.json` скопирован
- [ ] Конфигурационные файлы скопированы
- [ ] Структура папок сохранена

---

## 🚀 После копирования

Как только вы скопируете код, я:

1. ✅ Проанализирую структуру
2. ✅ Определю, что нужно переместить
3. ✅ Создам скрипт реорганизации
4. ✅ Выполню реорганизацию
5. ✅ Обновлю все пути и конфигурацию
6. ✅ Протестирую работу

---

**Вывод:** Копируйте код куда удобно. Потом я помогу реорганизовать структуру и разделить фронтенд и бэкенд.

**Последнее обновление:** 7 декабря 2025

