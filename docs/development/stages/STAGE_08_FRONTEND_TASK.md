# Этап 8: Каталог слов - Задание для фронтенда

## 📋 Обзор

Этап 8 добавляет полноценный каталог слов с расширенной фильтрацией, сортировкой, пагинацией и управлением словами:
- **Страница каталога** — просмотр всех слов пользователя
- **Расширенная фильтрация** — по статусу, части речи, категориям, колодам
- **Сортировка** — по дате, алфавиту, статусу, следующему повторению
- **Пагинация** — для больших списков
- **Статистика** — общая и по каждому слову
- **Действия** — редактирование, удаление, отправка в режим изучения, массовые операции

**Бекенд готов!** Все API эндпоинты реализованы и протестированы.

---

## 🎯 Цель

Создать полноценный каталог слов с возможностью:
1. Просмотра всех слов пользователя
2. Расширенной фильтрации и поиска
3. Сортировки по различным критериям
4. Просмотра детальной информации о слове
5. Управления словами (редактирование, удаление, массовые действия)
6. Просмотра статистики

---

## 📡 API Эндпоинты (готовы)

### 1. Список слов с фильтрацией и пагинацией
```
GET /api/words/list/
Query параметры:
  - language: string (ru|en|pt|de|es|fr|it)
  - learning_status: string (new|learning|reviewing|mastered)
  - part_of_speech: string
  - category_id: number
  - deck_id: number
  - search: string
  - has_etymology: boolean (true|false)
  - has_hint: boolean (true|false)
  - has_sentences: boolean (true|false)
  - ordering: string (created_at|-created_at|original_word|-original_word|learning_status|next_review|-next_review)
  - page: number (по умолчанию 1)
  - page_size: number (по умолчанию 20, максимум 100)

Response: {
  count: number,
  next: string | null,
  previous: string | null,
  results: WordListItem[]
}
```

### 2. Обновление слова
```
PATCH /api/words/{word_id}/
Body: WordUpdate (все поля опциональны)

Response: WordWithRelations
```

### 3. Удаление слова
```
DELETE /api/words/{word_id}/

Response: { message: string }
```

### 4. Статистика по слову
```
GET /api/words/{word_id}/stats/

Response: {
  word_id: number,
  cards_stats: {
    total_cards: number,
    normal_cards: number,
    inverted_cards: number,
    empty_cards: number,
    cloze_cards: number,
    in_learning_mode: number,
    due_for_review: number,
    mastered: number,
    next_review: string | null
  },
  learning_status: string,
  has_etymology: boolean,
  has_hint: boolean,
  has_sentences: boolean,
  sentences_count: number,
  relations_count: {
    synonyms: number,
    antonyms: number
  },
  categories_count: number,
  decks_count: number
}
```

### 5. Общая статистика по словам
```
GET /api/words/stats/

Response: {
  total_words: number,
  by_language: { [key: string]: number },
  by_status: { [key: string]: number },
  by_part_of_speech: { [key: string]: number },
  with_etymology: number,
  with_hint: number,
  with_sentences: number,
  total_cards: number,
  due_for_review: number
}
```

### 6. Отправка слова в режим изучения
```
POST /api/words/{word_id}/enter-learning/

Response: {
  message: string,
  word_id: number,
  cards_updated: number,
  learning_status: string
}
```

### 7. Массовые действия
```
POST /api/words/bulk-action/
Body: {
  word_ids: number[],
  action: 'enter_learning' | 'delete' | 'add_to_deck' | 'add_to_category' | 'remove_from_category',
  params?: {
    deck_id?: number,
    category_id?: number
  }
}

Response: {
  action: string,
  processed: number,
  successful: number,
  failed: number,
  errors: Array<{ word_id: number, error: string }>
}
```

---

## 🏗️ Структура файлов

```
frontend/src/
├── services/
│   └── words.service.ts              # Новый сервис для работы со словами
├── components/
│   ├── words/
│   │   ├── WordsListPage.tsx         # Новый: страница каталога слов
│   │   ├── WordDetailPage.tsx        # Новый: страница деталей слова
│   │   ├── WordListItem.tsx          # Новый: элемент списка слов (компактный)
│   │   ├── WordFilters.tsx           # Новый: панель фильтров
│   │   ├── WordSorting.tsx           # Новый: выбор сортировки
│   │   ├── WordStatsCard.tsx         # Новый: карточка статистики слова
│   │   ├── WordsStatsPanel.tsx       # Новый: панель общей статистики
│   │   ├── BulkActionsMenu.tsx       # Новый: меню массовых действий
│   │   └── WordEditModal.tsx         # Новый: модальное окно редактирования
│   └── category/
│       └── CategoryTree.tsx          # Новый: дерево категорий для фильтров
├── pages/
│   └── WordsPage.tsx                 # Новый/обновить: главная страница каталога
└── types/
    └── index.ts                       # Обновить: добавить типы для каталога
```

---

## 📝 Задачи

### 1. Типы TypeScript

**Файл:** `frontend/src/types/index.ts`

Добавить интерфейсы:

```typescript
// Расширенный Word для списка (с дополнительными полями)
export interface WordListItem extends Word {
  next_review: string | null;
  cards_count: number;
  categories: Array<{
    id: number;
    name: string;
    icon: string;
  }>;
  decks: Array<{
    id: number;
    name: string;
    target_lang: string;
    source_lang: string;
  }>;
}

// Ответ списка слов с пагинацией
export interface WordsListResponse {
  count: number;
  next: string | null;
  previous: string | null;
  results: WordListItem[];
}

// Параметры фильтрации
export interface WordsListFilters {
  language?: string;
  learning_status?: 'new' | 'learning' | 'reviewing' | 'mastered';
  part_of_speech?: string;
  category_id?: number;
  deck_id?: number;
  search?: string;
  has_etymology?: boolean;
  has_hint?: boolean;
  has_sentences?: boolean;
}

// Параметры сортировки
export type WordsListOrdering = 
  | 'created_at' 
  | '-created_at' 
  | 'original_word' 
  | '-original_word' 
  | 'learning_status' 
  | 'next_review' 
  | '-next_review';

// Параметры пагинации
export interface WordsListPagination {
  page: number;
  page_size: number;
}

// Статистика по слову
export interface WordStats {
  word_id: number;
  cards_stats: {
    total_cards: number;
    normal_cards: number;
    inverted_cards: number;
    empty_cards: number;
    cloze_cards: number;
    in_learning_mode: number;
    due_for_review: number;
    mastered: number;
    next_review: string | null;
  };
  learning_status: string;
  has_etymology: boolean;
  has_hint: boolean;
  has_sentences: boolean;
  sentences_count: number;
  relations_count: {
    synonyms: number;
    antonyms: number;
  };
  categories_count: number;
  decks_count: number;
}

// Общая статистика
export interface WordsStats {
  total_words: number;
  by_language: { [key: string]: number };
  by_status: { [key: string]: number };
  by_part_of_speech: { [key: string]: number };
  with_etymology: number;
  with_hint: number;
  with_sentences: number;
  total_cards: number;
  due_for_review: number;
}

// Массовые действия
export type BulkAction = 
  | 'enter_learning' 
  | 'delete' 
  | 'add_to_deck' 
  | 'add_to_category' 
  | 'remove_from_category';

export interface BulkActionRequest {
  word_ids: number[];
  action: BulkAction;
  params?: {
    deck_id?: number;
    category_id?: number;
  };
}

export interface BulkActionResponse {
  action: string;
  processed: number;
  successful: number;
  failed: number;
  errors: Array<{
    word_id: number;
    error: string;
  }>;
}
```

---

### 2. Сервис для работы со словами

**Файл:** `frontend/src/services/words.service.ts` (новый)

Создать сервис с методами:

```typescript
import apiClient from './api';
import {
  WordsListResponse,
  WordsListFilters,
  WordsListOrdering,
  WordsListPagination,
  Word,
  WordWithRelations,
  WordUpdate,
  WordStats,
  WordsStats,
  BulkActionRequest,
  BulkActionResponse,
} from '../types';

export const wordsService = {
  /**
   * Получить список слов с фильтрацией, сортировкой и пагинацией
   */
  async getWordsList(
    filters?: WordsListFilters,
    ordering?: WordsListOrdering,
    pagination?: WordsListPagination
  ): Promise<WordsListResponse> {
    const params = new URLSearchParams();
    
    // Фильтры
    if (filters?.language) params.append('language', filters.language);
    if (filters?.learning_status) params.append('learning_status', filters.learning_status);
    if (filters?.part_of_speech) params.append('part_of_speech', filters.part_of_speech);
    if (filters?.category_id) params.append('category_id', filters.category_id.toString());
    if (filters?.deck_id) params.append('deck_id', filters.deck_id.toString());
    if (filters?.search) params.append('search', filters.search);
    if (filters?.has_etymology !== undefined) params.append('has_etymology', filters.has_etymology.toString());
    if (filters?.has_hint !== undefined) params.append('has_hint', filters.has_hint.toString());
    if (filters?.has_sentences !== undefined) params.append('has_sentences', filters.has_sentences.toString());
    
    // Сортировка
    if (ordering) params.append('ordering', ordering);
    
    // Пагинация
    if (pagination?.page) params.append('page', pagination.page.toString());
    if (pagination?.page_size) params.append('page_size', pagination.page_size.toString());
    
    const response = await apiClient.get<WordsListResponse>(`/words/list/?${params.toString()}`);
    return response.data;
  },

  /**
   * Получить детали слова
   */
  async getWordDetail(wordId: number): Promise<WordWithRelations> {
    const response = await apiClient.get<WordWithRelations>(`/words/${wordId}/`);
    return response.data;
  },

  /**
   * Обновить слово
   */
  async updateWord(wordId: number, data: WordUpdate): Promise<WordWithRelations> {
    const formData = new FormData();
    
    // Добавляем только переданные поля
    if (data.original_word !== undefined) formData.append('original_word', data.original_word);
    if (data.translation !== undefined) formData.append('translation', data.translation);
    if (data.notes !== undefined) formData.append('notes', data.notes);
    if (data.hint_text !== undefined) formData.append('hint_text', data.hint_text);
    if (data.part_of_speech !== undefined) formData.append('part_of_speech', data.part_of_speech);
    if (data.learning_status !== undefined) formData.append('learning_status', data.learning_status);
    if (data.etymology !== undefined) formData.append('etymology', data.etymology);
    if (data.stickers) formData.append('stickers', JSON.stringify(data.stickers));
    if (data.sentences) formData.append('sentences', JSON.stringify(data.sentences));
    
    if (data.audio_file !== undefined && data.audio_file !== null) {
      formData.append('audio_file', data.audio_file);
    }
    if (data.image_file !== undefined && data.image_file !== null) {
      formData.append('image_file', data.image_file);
    }
    
    const response = await apiClient.patch<WordWithRelations>(
      `/words/${wordId}/`,
      formData,
      { headers: { 'Content-Type': 'multipart/form-data' } }
    );
    return response.data;
  },

  /**
   * Удалить слово
   */
  async deleteWord(wordId: number): Promise<void> {
    await apiClient.delete(`/words/${wordId}/`);
  },

  /**
   * Получить статистику по слову
   */
  async getWordStats(wordId: number): Promise<WordStats> {
    const response = await apiClient.get<WordStats>(`/words/${wordId}/stats/`);
    return response.data;
  },

  /**
   * Получить общую статистику по словам
   */
  async getWordsStats(): Promise<WordsStats> {
    const response = await apiClient.get<WordsStats>('/words/stats/');
    return response.data;
  },

  /**
   * Отправить слово в режим изучения
   */
  async enterLearningMode(wordId: number): Promise<{
    message: string;
    word_id: number;
    cards_updated: number;
    learning_status: string;
  }> {
    const response = await apiClient.post(`/words/${wordId}/enter-learning/`);
    return response.data;
  },

  /**
   * Массовые действия
   */
  async bulkAction(request: BulkActionRequest): Promise<BulkActionResponse> {
    const response = await apiClient.post<BulkActionResponse>('/words/bulk-action/', request);
    return response.data;
  },
};
```

---

### 3. Компонент WordListItem

**Файл:** `frontend/src/components/words/WordListItem.tsx` (новый)

Компактная карточка слова для списка:

**Поля:**
- Слово и перевод
- Статус обучения (бейдж)
- Количество карточек
- Категории (иконки)
- Колоды (названия)
- Следующее повторение (если есть)
- Меню действий (быстрое)

**Дизайн:**
- iOS 25 стиль
- Компактный вид для списка
- Hover эффекты
- Клик → переход на детальную страницу

---

### 4. Компонент WordFilters

**Файл:** `frontend/src/components/words/WordFilters.tsx` (новый)

Панель фильтров с возможностью:
- Выбор языка
- Выбор статуса обучения
- Выбор части речи
- Выбор категории (через CategoryTree)
- Выбор колоды (dropdown)
- Поиск по тексту
- Фильтры по наличию контента (чекбоксы: этимология, подсказка, предложения)

**UI:**
- Collapsible панель (можно свернуть/развернуть)
- Чистые фильтры (кнопка "Сбросить")
- Показ активных фильтров (бейджи)

---

### 5. Компонент WordSorting

**Файл:** `frontend/src/components/words/WordSorting.tsx` (новый)

Выбор сортировки:
- Dropdown с вариантами:
  - По дате добавления (новые первыми / старые первыми)
  - По алфавиту (A-Z / Z-A)
  - По статусу обучения
  - По следующему повторению (ближайшие первыми / дальние первыми)

**UI:**
- Select компонент
- Иконка сортировки
- Показ текущей сортировки

---

### 6. Компонент WordsListPage

**Файл:** `frontend/src/components/words/WordsListPage.tsx` (новый)

Главный компонент страницы каталога:

**Структура:**
```
┌─────────────────────────────────────┐
│ Заголовок: "Каталог слов"            │
│ [Статистика] [Настройки]             │
├─────────────────────────────────────┤
│ Фильтры (WordFilters)                │
├─────────────────────────────────────┤
│ Сортировка (WordSorting)              │
│ [Выбрать все] [Массовые действия]    │
├─────────────────────────────────────┤
│ Список слов (WordListItem[])         │
│ [Пагинация]                          │
└─────────────────────────────────────┘
```

**Функционал:**
- Загрузка слов с фильтрами
- Выбор слов (чекбоксы)
- Массовые действия
- Пагинация
- Обновление при изменении фильтров

---

### 7. Компонент WordDetailPage

**Файл:** `frontend/src/components/words/WordDetailPage.tsx` (новый)

Страница деталей слова с табами:

**Табы:**
1. **Основное** — слово, перевод, медиа, часть речи, статус
2. **Этимология** — автосгенерированная этимология (из этапа 7)
3. **Примеры** — предложения с этим словом (из этапа 7)
4. **Связи** — синонимы, антонимы
5. **Заметки** — заметки пользователя
6. **Карточки** — список карточек этого слова
7. **Статистика** — статистика по слову

**Действия:**
- Редактировать
- Удалить
- Отправить в режим изучения
- Создать карточки (inverted/empty/cloze)
- Добавить в колоду
- Добавить в категорию

---

### 8. Компонент WordStatsCard

**Файл:** `frontend/src/components/words/WordStatsCard.tsx` (новый)

Карточка статистики слова:

**Отображает:**
- Статус обучения (большой бейдж)
- Количество карточек по типам
- Карточки в режиме изучения
- Карточки на повторении
- Освоенные карточки
- Ближайшее повторение
- Наличие контента (этимология, подсказка, предложения)
- Количество связей (синонимы, антонимы)
- Количество категорий и колод

**Дизайн:**
- Красивые метрики с иконками
- Цветовое кодирование статусов
- Компактный вид

---

### 9. Компонент WordsStatsPanel

**Файл:** `frontend/src/components/words/WordsStatsPanel.tsx` (новый)

Панель общей статистики:

**Отображает:**
- Общее количество слов
- Распределение по языкам (график/таблица)
- Распределение по статусам (график/таблица)
- Распределение по частям речи (график/таблица)
- Слова с контентом (этимология, подсказка, предложения)
- Общее количество карточек
- Карточки на повторении

**UI:**
- Collapsible панель
- Красивые графики (можно использовать recharts или простые прогресс-бары)
- Цветовое кодирование

---

### 10. Компонент BulkActionsMenu

**Файл:** `frontend/src/components/words/BulkActionsMenu.tsx` (новый)

Меню массовых действий:

**Действия:**
- Отправить в режим изучения
- Удалить
- Добавить в колоду (модальное окно выбора колоды)
- Добавить в категорию (модальное окно выбора категории)
- Удалить из категории (модальное окно выбора категории)

**UI:**
- Кнопка "Действия" (активна только при выборе слов)
- Dropdown меню с действиями
- Модальные окна для выбора колоды/категории
- Показ прогресса выполнения
- Показ результатов (успешно/ошибки)

---

### 11. Компонент WordEditModal

**Файл:** `frontend/src/components/words/WordEditModal.tsx` (новый)

Модальное окно редактирования слова:

**Поля:**
- Слово (original_word)
- Перевод (translation)
- Часть речи (part_of_speech) — select
- Статус обучения (learning_status) — select (readonly, показывается)
- Заметки (notes) — textarea
- Подсказка (hint_text) — textarea
- Этимология (etymology) — textarea (readonly, показывается)
- Стикеры (stickers) — выбор эмодзи
- Медиа (audio_file, image_file) — загрузка файлов

**UI:**
- Модальное окно с формой
- Валидация
- Кнопки: Сохранить, Отмена
- Показ загрузки

---

### 12. Компонент CategoryTree

**Файл:** `frontend/src/components/category/CategoryTree.tsx` (новый)

Дерево категорий для фильтров:

**Функционал:**
- Отображение иерархии категорий
- Выбор категории (radio или checkbox)
- Возможность выбрать "Все категории"
- Компактный вид

**UI:**
- Древовидная структура с отступами
- Иконки категорий
- Клик для выбора

---

### 13. Страница WordsPage

**Файл:** `frontend/src/pages/WordsPage.tsx` (новый)

Главная страница каталога слов:

**Структура:**
- Использует `WordsListPage`
- Обработка роутинга
- Интеграция с навигацией

---

### 14. Обновление роутинга

**Файл:** `frontend/src/App.tsx` или роутер

Добавить маршруты:
- `/words` — каталог слов
- `/words/:id` — детали слова

---

## 🎨 UI/UX Требования

### Дизайн
- **iOS 25 стиль** — современный, минималистичный
- **Адаптивность** — мобильные устройства в приоритете
- **Темная тема** — поддержка dark mode
- **Анимации** — плавные переходы, hover эффекты

### Цветовое кодирование статусов
- `new` — серый (gray)
- `learning` — синий (blue)
- `reviewing` — оранжевый (orange)
- `mastered` — зеленый (green)

### Иконки
- Использовать `lucide-react` для иконок
- Статусы: BookOpen, Clock, CheckCircle, AlertCircle
- Действия: Edit, Trash2, Send, Plus, Filter, SortAsc

### Состояния загрузки
- Skeleton loaders для списка
- Spinner для действий
- Toast уведомления для результатов

---

## 🔄 Потоки пользователя

### Поток 1: Просмотр каталога
1. Пользователь открывает `/words`
2. Видит список всех слов с фильтрами по умолчанию
3. Применяет фильтры (статус, категория, поиск)
4. Меняет сортировку
5. Переходит на следующую страницу пагинации
6. Кликает на слово → переход на детальную страницу

### Поток 2: Редактирование слова
1. Пользователь открывает детали слова
2. Нажимает "Редактировать"
3. Открывается модальное окно
4. Изменяет поля
5. Сохраняет
6. Видит обновлённое слово

### Поток 3: Массовые действия
1. Пользователь выбирает несколько слов (чекбоксы)
2. Нажимает "Действия"
3. Выбирает действие (например, "Добавить в колоду")
4. Выбирает колоду в модальном окне
5. Подтверждает
6. Видит результат (успешно/ошибки)

### Поток 4: Фильтрация и поиск
1. Пользователь открывает фильтры
2. Выбирает статус "reviewing"
3. Выбирает категорию "Еда"
4. Вводит поисковый запрос "Haus"
5. Видит отфильтрованный список
6. Сбрасывает фильтры

---

## 📱 Адаптивность

### Мобильные устройства (< 768px)
- Фильтры в collapsible панели
- Список слов: 1 колонка
- Компактные карточки слов
- Модальные окна на весь экран

### Планшеты (768px - 1024px)
- Фильтры в боковой панели (опционально)
- Список слов: 2 колонки
- Стандартные карточки

### Десктоп (> 1024px)
- Фильтры в боковой панели
- Список слов: 3-4 колонки
- Расширенные карточки

---

## 🧪 Тестирование

### Unit тесты
- Компоненты фильтров
- Компоненты сортировки
- Сервис wordsService
- Утилиты форматирования

### Интеграционные тесты
- Загрузка списка слов
- Применение фильтров
- Пагинация
- Массовые действия

### E2E тесты (опционально)
- Полный поток просмотра каталога
- Редактирование слова
- Массовые действия

---

## 📋 Чеклист реализации

### Приоритет 1 (Основной функционал)
- [ ] Создать `words.service.ts`
- [ ] Обновить типы в `types/index.ts`
- [ ] Создать `WordListItem` компонент
- [ ] Создать `WordFilters` компонент
- [ ] Создать `WordSorting` компонент
- [ ] Создать `WordsListPage` компонент
- [ ] Создать `WordsPage` страницу
- [ ] Добавить роутинг

### Приоритет 2 (Детали и действия)
- [ ] Создать `WordDetailPage` компонент
- [ ] Создать `WordEditModal` компонент
- [ ] Создать `WordStatsCard` компонент
- [ ] Создать `BulkActionsMenu` компонент
- [ ] Интеграция с API

### Приоритет 3 (Статистика и улучшения)
- [ ] Создать `WordsStatsPanel` компонент
- [ ] Создать `CategoryTree` компонент
- [ ] Добавить графики статистики
- [ ] Оптимизация производительности
- [ ] Тесты

---

## 🔗 Интеграция с существующим кодом

### Использование существующих компонентов
- `WordCard` — можно использовать для детального просмотра
- `AudioPlayer` — для аудио в деталях
- `ImagePreviewModal` — для изображений
- UI компоненты из `ui/` (Button, Card, Badge, etc.)

### Использование существующих сервисов
- `api.ts` — базовый API клиент
- `auth.service.ts` — для проверки аутентификации
- `token.service.ts` — для проверки баланса токенов (если нужно)

### Использование контекстов
- `LanguageContext` — для переводов
- `ThemeContext` — для темы
- `TokenContext` — для токенов (если нужно)

---

## 📝 Примеры использования

### Пример 1: Загрузка списка слов

```typescript
import { wordsService } from '../services/words.service';

const loadWords = async () => {
  const response = await wordsService.getWordsList(
    {
      learning_status: 'reviewing',
      language: 'de',
      search: 'Haus'
    },
    'original_word',
    { page: 1, page_size: 20 }
  );
  
  console.log(`Найдено ${response.count} слов`);
  console.log(response.results);
};
```

### Пример 2: Обновление слова

```typescript
await wordsService.updateWord(wordId, {
  notes: 'Новая заметка',
  part_of_speech: 'noun'
});
```

### Пример 3: Массовое действие

```typescript
const result = await wordsService.bulkAction({
  word_ids: [1, 2, 3],
  action: 'add_to_deck',
  params: { deck_id: 5 }
});

console.log(`Успешно: ${result.successful}, Ошибок: ${result.failed}`);
```

---

## 🎯 Критерии завершения

✅ Все компоненты созданы и работают  
✅ Фильтрация работает корректно  
✅ Сортировка работает корректно  
✅ Пагинация работает корректно  
✅ Редактирование слова работает  
✅ Удаление слова работает  
✅ Массовые действия работают  
✅ Статистика отображается  
✅ Адаптивный дизайн  
✅ Темная тема поддерживается  
✅ Обработка ошибок реализована  
✅ Состояния загрузки реализованы  
✅ Тесты написаны (опционально)  

---

## 📚 Дополнительные материалы

### Ссылки на документацию
- [Django REST Framework](https://www.django-rest-framework.org/)
- [React Router](https://reactrouter.com/)
- [Lucide Icons](https://lucide.dev/)
- [Tailwind CSS](https://tailwindcss.com/)

### Примеры компонентов
- См. `WordCard.tsx` для стиля карточек
- См. `WordsTable.tsx` для стиля списков
- См. `STAGE_07_FRONTEND_TASK.md` для примера задания

---

## ⚠️ Важные замечания

1. **Производительность**: При большом количестве слов использовать виртуализацию списка (react-window или react-virtualized)

2. **Кэширование**: Кэшировать результаты фильтрации для улучшения UX

3. **Оптимистичные обновления**: При массовых действиях показывать результат сразу, затем синхронизировать с сервером

4. **Валидация**: Все формы должны валидироваться на клиенте перед отправкой

5. **Доступность**: Использовать семантический HTML, ARIA атрибуты, поддержка клавиатуры

6. **Ошибки**: Показывать понятные сообщения об ошибках, не технические детали

---

## 🚀 Готово к реализации!

Все API эндпоинты готовы и протестированы. Можно приступать к фронтенд разработке!
