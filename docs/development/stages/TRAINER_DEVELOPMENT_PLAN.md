# 🎯 План разработки Тренажёра слов

> **Версия**: 1.3  
> **Дата**: Январь 2026  
> **Статус**: В разработке (Backend этапов 4–8 готов; Frontend: этап 7 частично, этап 8 в работе)  
> **Обновлено**: v1.3 — Backend и Frontend в одном репозитории, разработка одновременно

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Архитектурные изменения](#архитектурные-изменения)
3. [Этапы разработки](#этапы-разработки)
4. [Детальное описание моделей](#детальное-описание-моделей)
5. [API эндпоинты](#api-эндпоинты)
6. [Алгоритм SM-2](#алгоритм-sm-2)
7. [Система оповещений](#система-оповещений)
8. [Кривые забывания](#кривые-забывания)
9. [Миграция данных](#миграция-данных)
10. [UI/UX](#uiux)

---

## 🎯 Обзор проекта

### Цель
Создание полноценной системы интервального повторения для изучения иностранных слов, основанной на алгоритме SuperMemo SM-2 с адаптацией под индивидуальные особенности пользователя.

### Ключевые концепции

#### Слово (Word) vs Карточка (Card)
- **Слово** — базовая единица знания. Хранит всю информацию: перевод, этимологию, примеры, медиа, подсказки
- **Карточка** — единица тренировки. Производная от слова, имеет свой прогресс обучения
- **Связи между словами** — синонимы и антонимы реализуются через отдельную модель `WordRelation`, слова остаются самостоятельными сущностями

#### Режим Изучения (Learning Mode)
Специальный предтренировочный режим для глубокого знакомства со словом:
- Автоматически активируется для новых слов
- Активируется при 4 нажатиях "Снова" подряд
- Может быть вызван вручную пользователем

#### Типы карточек
| Тип | Описание | Вспомогательная |
|-----|----------|-----------------|
| `normal` | Слово → Перевод | Нет |
| `inverted` | Перевод → Слово | Нет |
| `empty` | Медиа → Слово/Перевод | Да |
| `cloze` | Предложение с пропуском | Да |

> **Вспомогательные карточки** автоматически удаляются, когда основная карточка слова достигает стабильности (интервал > 60 дней). При необходимости их можно восстановить одной кнопкой, так как предложения для `cloze` карточек хранятся в поле `sentences` модели Word.

---

## 🏗️ Архитектурные изменения

### Текущая архитектура
```
User ──► Word ◄── Deck (M2M)
         │
         └── card_type (normal/inverted/empty)
```

### Новая архитектура
```
User ──► Word ◄── Deck (M2M)
           │
           │◄── Category (M2M, многоуровневая вложенность)
           │
           │◄──► WordRelation (синонимы/антонимы как связи)
           │
           └───► Card (1:N)
                   │
                   └── SpacedRepetition data
                   
User ──► UserTrainingSettings (все константы SM-2 настраиваемые)
           │
           └── NotificationSettings (оповещения)
```

---

## 📅 Этапы разработки

### Этап 0: Подготовка и планирование
- [x] Изучение алгоритма SuperMemo SM-2
- [x] Анализ текущей архитектуры
- [x] Создание плана разработки
- [ ] Ревью плана
- [ ] Утверждение плана

---

### Этап 1: Рефакторинг модели Word
**Цель**: Расширить модель Word новыми полями

#### Новые поля:
```python
# Контент
etymology = models.TextField(blank=True)  # Этимология (генерируется AI автоматически)
sentences = models.JSONField(default=list)  # Примеры предложений (для cloze карточек)
notes = models.TextField(blank=True)  # Заметки пользователя

# Подсказки (генерируются AI, редактируемые пользователем)
hint_text = models.TextField(blank=True)  # Текстовая подсказка (хранится, не генерируется каждый раз)
hint_audio = models.FileField(blank=True)  # Аудио подсказка на изучаемом языке

# Классификация
part_of_speech = models.CharField(max_length=20, blank=True)  # Часть речи
categories = models.ManyToManyField('Category', blank=True)  # Многоуровневые категории

# Персонализация
stickers = models.JSONField(default=list)  # Эмоции/наклейки (❤️⭐🔥)

# Статус
learning_status = models.CharField(
    max_length=20,
    choices=[
        ('new', 'Новое'),
        ('learning', 'В изучении'),
        ('reviewing', 'На повторении'),
        ('mastered', 'Освоено'),
    ],
    default='new'
)

# Связи между словами реализуются через отдельную модель WordRelation
# (см. ниже), а не через ManyToMany, т.к. синонимы/антонимы — 
# это самостоятельные слова со своими карточками
```

#### Задачи:
- [x] Создание миграции для новых полей
- [x] Обновление сериализаторов
- [x] Обновление API для работы с новыми полями
- [x] Тесты

---

### Этап 1.5: Модель WordRelation (связи между словами)
**Цель**: Реализация связей синонимов и антонимов между самостоятельными словами

#### Модель:
```python
class WordRelation(models.Model):
    """
    Связь между двумя словами.
    Каждое слово остаётся самостоятельной сущностью со своими карточками.
    """
    RELATION_TYPES = [
        ('synonym', 'Синоним'),
        ('antonym', 'Антоним'),
    ]
    
    word_from = models.ForeignKey(
        Word, 
        on_delete=models.CASCADE, 
        related_name='relations_from'
    )
    word_to = models.ForeignKey(
        Word, 
        on_delete=models.CASCADE, 
        related_name='relations_to'
    )
    relation_type = models.CharField(max_length=20, choices=RELATION_TYPES)
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        unique_together = ['word_from', 'word_to', 'relation_type']
```

#### Логика:
- При создании связи A→B автоматически создаётся обратная B→A
- При удалении слова все его связи удаляются
- Синоним/антоним — это отдельное слово, которое можно добавить в колоду, создать карточки

#### Методы Word:
```python
class Word:
    def get_synonyms(self):
        """Возвращает все синонимы этого слова"""
        return Word.objects.filter(
            relations_to__word_from=self,
            relations_to__relation_type='synonym'
        )
    
    def get_antonyms(self):
        """Возвращает все антонимы этого слова"""
        return Word.objects.filter(
            relations_to__word_from=self,
            relations_to__relation_type='antonym'
        )
    
    def add_synonym(self, other_word):
        """Добавляет синоним (создаёт двустороннюю связь)"""
        WordRelation.objects.get_or_create(
            word_from=self, word_to=other_word, relation_type='synonym'
        )
        WordRelation.objects.get_or_create(
            word_from=other_word, word_to=self, relation_type='synonym'
        )
```

#### Задачи:
- [ ] Создание модели и миграции
- [ ] Методы для Word (get_synonyms, get_antonyms, add_synonym, add_antonym)
- [ ] API для управления связями
- [ ] Тесты

---

### Этап 2: Создание модели Category
**Цель**: Иерархическая система категорий для слов (неограниченная вложенность)

#### Модель:
```python
class Category(models.Model):
    """
    Категории создаются пользователем самостоятельно.
    Поддерживается неограниченная вложенность через parent.
    """
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    name = models.CharField(max_length=100)
    parent = models.ForeignKey(
        'self', 
        null=True, 
        blank=True, 
        on_delete=models.CASCADE,
        related_name='children'
    )
    icon = models.CharField(max_length=10, blank=True)  # Эмодзи
    order = models.IntegerField(default=0)
    created_at = models.DateTimeField(auto_now_add=True)
    
    def get_ancestors(self):
        """Возвращает всех родителей вверх по иерархии"""
        ancestors = []
        current = self.parent
        while current:
            ancestors.append(current)
            current = current.parent
        return ancestors
    
    def get_descendants(self):
        """Возвращает всех потомков вниз по иерархии"""
        descendants = list(self.children.all())
        for child in self.children.all():
            descendants.extend(child.get_descendants())
        return descendants
```

#### Задачи:
- [x] Создание модели и миграции
- [x] CRUD API для категорий
- [x] Получение дерева категорий (рекурсивно)
- [x] Перемещение категории (изменение parent)
- [x] Тесты

---

### Этап 3: Создание модели Card
**Цель**: Отделить карточки от слов, добавить данные для SM-2

> 📋 **Детальный план**: [STAGE_03_CARD.md](./STAGE_03_CARD.md)

#### Ключевые компоненты:

| Компонент | Описание |
|-----------|----------|
| `Card` модель | SM-2 параметры, типы карточек, статусы |
| `CardManager` | Методы фильтрации (due_for_review, in_learning) |
| Сигнал | Автосоздание карточки при создании Word |
| API | CRUD для карточек + suspend/unsuspend |

#### Типы карточек:
- `normal` — Слово → Перевод
- `inverted` — Перевод → Слово  
- `empty` — Медиа → Слово (вспомогательная)
- `cloze` — Предложение с пропуском (вспомогательная)

#### Задачи:
- [ ] Создание модели `Card` с полями SM-2
- [ ] `CardManager` с методами фильтрации
- [ ] Сигнал автосоздания при создании Word
- [ ] Фабричные методы: `create_from_word`, `create_inverted`, `create_empty`, `create_cloze`
- [ ] API эндпоинты (8 штук)
- [ ] Сериализаторы (5 штук)
- [ ] TypeScript типы
- [ ] Тесты (30+ тестов)

---

### Этап 4: Настройки пользователя для тренировки
**Цель**: Индивидуальные параметры алгоритма SM-2 (ВСЕ константы настраиваемые)

> 📋 **Детальный план**: [STAGE_04_TRAINING_SETTINGS.md](./STAGE_04_TRAINING_SETTINGS.md)

#### Ключевые компоненты:

| Компонент | Описание |
|-----------|----------|
| `UserTrainingSettings` модель | Все настраиваемые константы SM-2 |
| Инициализация по возрасту | 3 возрастные группы с разными параметрами |
| API | GET/PATCH настроек, сброс к умолчанию |
| Калибровка | Автоматическая корректировка interval_modifier |

#### Модель:
```python
class UserTrainingSettings(models.Model):
    """
    ВСЕ константы алгоритма SM-2 настраиваемые пользователем.
    Значения по умолчанию инициализируются на основе возраста.
    """
    AGE_GROUPS = [
        ('young', 'До 18 лет'),
        ('adult', '18-50 лет'),
        ('senior', '50+ лет'),
    ]
    
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    
    # Возраст (влияет на начальные параметры при регистрации)
    age_group = models.CharField(max_length=20, choices=AGE_GROUPS, default='adult')
    
    # ═══════════════════════════════════════════════════════════════
    # КОНСТАНТЫ АЛГОРИТМА SM-2 (все настраиваемые!)
    # ═══════════════════════════════════════════════════════════════
    
    # Ease Factor (коэффициент лёгкости)
    starting_ease = models.FloatField(default=2.5)  # Начальный EF для новых карточек
    min_ease_factor = models.FloatField(default=1.3)  # Минимальный EF
    
    # Изменения EF при разных ответах
    again_ef_delta = models.FloatField(default=-0.2)  # При "Снова"
    hard_ef_delta = models.FloatField(default=-0.15)  # При "Трудно"
    good_ef_delta = models.FloatField(default=0.0)  # При "Хорошо"
    easy_ef_delta = models.FloatField(default=0.15)  # При "Легко"
    
    # Модификаторы интервалов
    interval_modifier = models.FloatField(default=1.0)  # Глобальный модификатор
    hard_interval_modifier = models.FloatField(default=1.2)  # Для "Трудно"
    easy_bonus = models.FloatField(default=1.3)  # Бонус для "Легко"
    
    # Шаги обучения (минуты) — внутрисессионное повторение
    learning_steps = models.JSONField(default=lambda: [2, 10])
    
    # Интервалы выпуска из обучения
    graduating_interval = models.IntegerField(default=1)  # После шагов → дни
    easy_interval = models.IntegerField(default=4)  # При "Легко" сразу → дни
    
    # ═══════════════════════════════════════════════════════════════
    # НАСТРОЙКИ СЕССИИ
    # ═══════════════════════════════════════════════════════════════
    default_session_duration = models.IntegerField(default=20)  # Минуты
    
    # ═══════════════════════════════════════════════════════════════
    # ПОРОГИ И ЛИМИТЫ
    # ═══════════════════════════════════════════════════════════════
    lapse_threshold = models.IntegerField(default=4)  # Провалов → режим Изучения
    stability_threshold = models.IntegerField(default=60)  # Дни → удаление вспомогательных
    calibration_interval = models.IntegerField(default=50)  # Каждые N ответов калибровка
    
    # ═══════════════════════════════════════════════════════════════
    # КАЛИБРОВКА (автоматическая)
    # ═══════════════════════════════════════════════════════════════
    total_reviews = models.IntegerField(default=0)
    successful_reviews = models.IntegerField(default=0)
    last_calibration_at = models.IntegerField(default=0)
    
    # Целевой процент успеха для калибровки
    target_retention = models.FloatField(default=0.90)  # 90%
```

#### Инициализация по возрасту:
```python
AGE_SETTINGS = {
    'young': {
        'starting_ease': 2.5, 
        'interval_modifier': 1.0,
        'min_ease_factor': 1.3,
    },
    'adult': {
        'starting_ease': 2.5, 
        'interval_modifier': 1.0,
        'min_ease_factor': 1.3,
    },
    'senior': {
        'starting_ease': 2.3, 
        'interval_modifier': 0.9,
        'min_ease_factor': 1.3,
        'target_retention': 0.85,  # Чуть ниже для пожилых
    },
}
```

#### UI настроек:
В профиле пользователя — раздел "Настройки тренировки" с возможностью изменить:
- Все константы EF
- Все модификаторы интервалов
- Шаги обучения
- Пороги
- Кнопка "Сбросить к значениям по умолчанию"

#### Задачи:
- [ ] Создание модели и миграции
- [ ] Добавление поля `age_group` в регистрацию
- [ ] API для настроек (CRUD)
- [ ] UI страница настроек тренировки
- [ ] Логика калибровки каждые N ответов
- [ ] Сброс к значениям по умолчанию
- [ ] Тесты

---

### Этап 5: Алгоритм SM-2
**Цель**: Реализация алгоритма интервального повторения

**Статус**: ✅ Детально спроектирован (см. `STAGE_05_SM2_ALGORITHM.md`)

#### Файл: `backend/apps/training/sm2.py`

```python
class SM2Algorithm:
    """
    Реализация алгоритма SuperMemo SM-2 с адаптацией
    """
    
    @classmethod
    def process_answer(
        cls,
        card: Card,
        answer: int,
        settings: UserTrainingSettings,
        time_spent: float = None
    ) -> dict:
        """
        Обрабатывает ответ пользователя
        
        answer:
            0 = Снова (Again)
            1 = Трудно (Hard)
            2 = Хорошо (Good)
            3 = Легко (Easy)
        
        Returns:
            dict с результатами обработки
        """
        ...
    
    @classmethod
    def calculate_next_interval(cls, card: Card, answer: int, settings: UserTrainingSettings) -> int:
        """Рассчитывает следующий интервал в днях"""
        ...
    
    @classmethod
    def should_enter_learning_mode(cls, card: Card, settings: UserTrainingSettings) -> bool:
        """Проверяет, нужно ли отправить карточку в режим изучения"""
        return card.consecutive_lapses >= settings.lapse_threshold
    
    @classmethod
    def update_ease_factor(cls, card: Card, answer: int, settings: UserTrainingSettings) -> float:
        """Обновляет Ease Factor карточки"""
        ...
    
    @classmethod
    def apply_interval_modifiers(cls, base_interval: int, answer: int, settings: UserTrainingSettings) -> int:
        """Применяет модификаторы интервала"""
        ...
```

#### Задачи:
- [ ] Реализация базового алгоритма SM-2
- [ ] Внутрисессионное обучение (шаги из settings.learning_steps)
- [ ] Обработка всех типов ответов (Again, Hard, Good, Easy)
- [ ] Долгосрочные интервалы с модификаторами
- [ ] Автоматическая отправка в режим изучения при провалах
- [ ] Интеграция с калибровкой из UserTrainingSettings
- [ ] Обработка граничных случаев
- [ ] Юнит-тесты алгоритма (25+ тестов)

---

### Этап 6: API для тренировки
**Цель**: Эндпоинты для проведения тренировочных сессий

**Статус**: ✅ Завершён (см. `STAGE_06_TRAINING_API.md`)

#### Эндпоинты:

```python
# Получить карточки для сессии
GET /api/training/session/
Query params:
  - deck_id: int (опционально, если не указан — все карточки)
  - duration: int (минуты, по умолчанию из settings)
  - new_cards: bool (включать новые, по умолчанию true)

Response:
{
  "session_id": "uuid",
  "cards": [...],
  "estimated_time": 20,
  "new_count": 5,
  "review_count": 15,
  "learning_count": 3,
  "total_count": 23
}

# Отправить ответ
POST /api/training/answer/
Body:
{
  "session_id": "uuid" (опционально),
  "card_id": 123,
  "answer": 2,  # 0=Again, 1=Hard, 2=Good, 3=Easy
  "time_spent": 5.2  # Секунды на ответ (опционально)
}

Response:
{
  "card_id": 123,
  "new_interval": 4,
  "new_ease_factor": 2.5,
  "next_review": "2026-01-13",
  "entered_learning_mode": false,
  "exited_learning_mode": false,
  "learning_step": -1,
  "calibrated": false,
  "card": {...}
}

# Отправить в режим изучения
POST /api/training/enter-learning/
Body: {"card_id": 123}

# Завершить режим изучения
POST /api/training/exit-learning/
Body: {"card_id": 123}

# Статистика
GET /api/training/stats/
Query params:
  - period: "day" | "week" | "month" | "all"

Response:
{
  "period": "all",
  "total_reviews": 150,
  "successful_reviews": 130,
  "success_rate": 0.87,
  "streak_days": 5,
  "cards_by_status": {...},
  "reviews_by_day": [...],
  "average_time_per_card": 12.5,
  "total_time_spent": 1800
}
```

#### Задачи:
- [x] Реализация функции build_card_queue() с приоритизацией
- [x] Реализация эндпоинтов (5 эндпоинтов)
- [x] Формирование очереди карточек с ограничением по времени
- [x] Интеграция с SM2Algorithm для обработки ответов
- [x] Реализация статистики (period, streak, cards_by_status)
- [x] Тесты API (29 тестов: 7 для session_utils, 22 для API)

---

### Этап 7: Генерация контента (AI)
**Цель**: Автоматическая генерация этимологии, подсказок, предложений

**Статус**: 📋 Детальный план готов (см. `STAGE_07_AI_GENERATION.md`)

#### Функции:

```python
# backend/apps/training/ai_generation.py

def generate_etymology(word: str, language: str, user: User) -> str:
    """
    Генерирует этимологию слова через AI
    Списывает токены
    """
    ...

def generate_hint(word: str, translation: str, language: str, user: User) -> tuple[str, str]:
    """
    Генерирует текстовую и аудио подсказку
    Returns: (hint_text, hint_audio_path)
    """
    ...

def generate_sentence(word: str, language: str, user: User) -> str:
    """
    Генерирует предложение с использованием слова
    """
    ...

def generate_synonym_word(word: Word, user: User) -> Word:
    """
    Создаёт новое слово-синоним
    """
    ...
```

#### Автоматическая генерация:
- **Этимология** — генерируется автоматически при добавлении слова
- **Подсказка** — по запросу пользователя
- **Предложение** — по запросу или автоматически для cloze карточек

#### Задачи (Backend):
- [x] Реализация функций генерации (4 функции)
- [x] Промпты для генерации (дефолтные + пользовательские)
- [x] API эндпоинты (4 эндпоинта)
- [x] Django signals для автоматической генерации этимологии
- [x] Интеграция с системой токенов
- [x] Тесты (11 тестов, все проходят)

#### Задачи (Frontend):
- [x] Создание сервиса `ai-generation.service.ts`
- [x] Компоненты генерации (EtymologyGenerator, HintGenerator, SentenceGenerator, SynonymGenerator)
- [x] Страница деталей слова с табами
- [x] Интеграция с TokenContext
- [x] Обработка ошибок и состояний загрузки
- [ ] Загрузка слова по `GET /api/words/{id}/` (для входа из каталога без deckId)
- [ ] (Опционально) Компонент AIGenerationButton, UI/UX по iOS 25

**Детальное задание:** См. `docs/development/stages/STAGE_07_FRONTEND_TASK.md`

---

### Этап 8: Каталог слов (Backend + Frontend)
**Цель**: API и страница для просмотра и управления всеми словами

**Статус**: 📋 Детальный план готов (см. `STAGE_08_WORDS_CATALOG_BACKEND.md`)

#### Backend задачи:
- [x] Создать `utils.py` с функциями определения статуса слова
- [x] Расширить `words_list_view` (фильтрация, сортировка, пагинация)
- [x] Создать эндпоинты для действий (обновление, удаление, статистика)
- [x] Добавить сигналы для автоматического обновления статуса
- [x] Тесты (26 тестов, все проходят)

#### Frontend задачи:
- [x] Сервис `words.service.ts` и типы каталога
- [x] Страница WordsPage (каталог), маршрут `/words`
- [x] Компонент WordListItem (превью), базовые фильтры и сортировка на странице
- [x] Загрузка слова по `GET /api/words/{id}/` в WordDetailPage (вход из каталога)
- [ ] Компонент CategoryTree (для фильтров по категориям)
- [ ] Массовые действия (BulkActionsMenu), модальное редактирование слова
- [ ] Тесты

**Детальный план Backend:** См. `docs/development/stages/STAGE_08_WORDS_CATALOG_BACKEND.md`  
**Детальное задание Frontend:** См. `docs/development/stages/STAGE_08_FRONTEND_TASK.md`

---

### Этап 9: Управление категориями (Frontend)
**Цель**: Страница для создания и организации категорий

**Статус**: ✅ Готово

#### Страница `/categories`

**Функционал:**
- Дерево категорий с раскрытием/сворачиванием
- Создание категории (название, иконка emoji, родитель)
- Редактирование категории
- Удаление категории (с подтверждением)
- Просмотр слов в категории
- Добавление подкатегорий
- Фильтр по категориям на странице каталога слов

#### Задачи:
- [x] Сервис `categories.service.ts` и API endpoints
- [x] Компонент CategoryTree (рекурсивный, reusable)
- [x] Компонент CategoryFormModal (создание/редактирование)
- [x] Компонент CategoriesPage (CRUD, дерево, просмотр слов)
- [x] Фильтр по категориям в WordsPage
- [x] Маршрут `/categories` в App.tsx
- [x] Навигация в Header + все 7 локалей
- [x] Развёрнуто на сервере

---

### Этап 10: Страница тренировки (Frontend)
**Цель**: Интерфейс для проведения тренировочных сессий

**Статус**: ✅ Готово

#### Страница `/training` — Выбор режима

**Опции запуска:**
1. Тренировать все слова
2. Настройка времени: 5/10/15/20/30/∞ мин
3. Включать/не включать новые карточки

**Статистика на стартовой странице:**
- Streak (дни подряд), успешность, всего слов
- Карточки по статусам: изучение, повторение, новые

#### Страница `/training/session` — Сама тренировка

**Компоненты:**
1. **TrainingCard** — карточка с CSS-анимацией переворота, показ этимологии/подсказки/предложений на обороте
2. **AnswerButtons** — 4 кнопки SM-2 (Снова/Трудно/Хорошо/Легко) + горячие клавиши 1-4
3. **SessionProgress** — прогресс-бар + счётчики по типам
4. **SessionTimer** — таймер с обратным отсчётом / бесконечный
5. **Экран завершения** — результаты сессии (карточек, верных, успешность%)

#### Задачи:
- [x] Сервис `training.service.ts` + API endpoints + типы
- [x] TrainingStartPage (статистика, настройки, запуск)
- [x] TrainingSessionPage (очередь карточек, ответы, прогресс)
- [x] TrainingCard (анимация переворота, доп. инфо на обороте)
- [x] AnswerButtons (4 кнопки + keyboard shortcuts)
- [x] SessionProgress + SessionTimer
- [x] Маршруты `/training` и `/training/session`
- [x] Навигация + все 7 локалей
- [x] Развёрнуто на сервере

---

### Этап 11: Миграция данных ✅ Готово
**Цель**: Безопасный перенос существующих данных

#### Выполнено:
- [x] Бэкап production БД перед миграцией (230KB dump)
- [x] Management command `migrate_data` с dry-run и --apply
- [x] Идемпотентная миграция: 1407 Card из 1407 Word
- [x] Создание UserTrainingSettings для 5 пользователей
- [x] Обновление learning_status: new → learning
- [x] Верификация на production: повторный запуск = 0 изменений
- [x] Сохранение всех типов: 627 normal + 608 inverted + 172 empty

---

### Этап 12: Интеграция и тестирование ✅ Готово
**Цель**: Полное тестирование системы

#### Выполнено:
- [x] 416 backend тестов
- [x] 80% покрытие кода
- [x] Тесты для SM2 edge cases (ValueError, граничные значения)
- [x] Тесты для migrate_data (dry-run, apply, идемпотентность, card_type=empty)
- [x] Тесты для уведомлений (модель, API, quiet hours, триггеры)
- [x] Тесты для кривых забывания (пустые данные, с повторениями)
- [x] Все тесты проходят на CI

---

### Этап 13: Система оповещений
**Цель**: Агрессивные напоминания о необходимости тренировки (веб-приложение)

#### Каналы оповещений:

##### 1. Web Push Notifications (основной)
```python
# Используем django-webpush или pywebpush
class PushSubscription(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    subscription_info = models.JSONField()  # endpoint, keys
    created_at = models.DateTimeField(auto_now_add=True)
    is_active = models.BooleanField(default=True)
```

**Триггеры:**
- Есть карточки для повторения, но пользователь не заходил > 4 часов
- Streak в опасности (не занимался сегодня, вечер)
- Накопилось > 50 карточек для повторения

##### 2. Email-уведомления (резервный)
```python
class EmailNotificationSettings(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    daily_reminder = models.BooleanField(default=True)
    daily_reminder_time = models.TimeField(default='09:00')
    weekly_digest = models.BooleanField(default=True)
    streak_warning = models.BooleanField(default=True)
```

##### 3. Telegram-бот (опционально, будущее)
- Отправка напоминаний в Telegram
- Быстрые карточки прямо в чате

#### Модель настроек оповещений:
```python
class NotificationSettings(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    
    # Web Push
    push_enabled = models.BooleanField(default=True)
    push_frequency = models.CharField(
        max_length=20,
        choices=[
            ('aggressive', 'Агрессивный (каждые 4 часа)'),
            ('normal', 'Обычный (2 раза в день)'),
            ('minimal', 'Минимальный (1 раз в день)'),
        ],
        default='normal'
    )
    
    # Email
    email_enabled = models.BooleanField(default=True)
    email_daily_time = models.TimeField(default='09:00')
    
    # Тихие часы
    quiet_hours_start = models.TimeField(default='22:00')
    quiet_hours_end = models.TimeField(default='08:00')
    
    # Timezone пользователя
    timezone = models.CharField(max_length=50, default='Europe/Moscow')
```

#### Celery задачи:
```python
# backend/apps/training/tasks.py

@shared_task
def check_and_send_notifications():
    """Проверяет пользователей и отправляет напоминания"""
    for user in User.objects.filter(notification_settings__push_enabled=True):
        cards_due = Card.objects.filter(
            user=user, 
            next_review__lte=now()
        ).count()
        
        if cards_due > 0 and should_notify(user):
            send_push_notification(user, f"У вас {cards_due} карточек для повторения!")

@shared_task
def send_daily_email_reminders():
    """Рассылка ежедневных email-напоминаний"""
    ...
```

#### Реализовано:
- [x] Модель NotificationSettings (частота, триггеры, тихие часы)
- [x] API: GET/PATCH настроек + GET /check/ для проверки
- [x] Browser Notification API (запрос разрешения, показ уведомлений)
- [x] useNotifications хук (проверка каждые 5 минут)
- [x] Страница настроек уведомлений (/notifications)
- [x] Навигация в Header + все 7 локалей
- [x] Тесты модели и API (quiet hours, should_notify, triggers)
- [x] Развёрнуто на production

---

### Этап 14: Кривые забывания (визуализация)
**Цель**: Построение индивидуальных кривых забывания для пользователя и карточек

#### Данные для построения:

##### Индивидуальная кривая пользователя (в профиле):
```python
class UserForgettingCurve(models.Model):
    """
    Агрегированные данные для построения кривой забывания пользователя
    """
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    interval_days = models.IntegerField()  # Интервал в днях
    success_count = models.IntegerField(default=0)  # Успешных ответов
    total_count = models.IntegerField(default=0)  # Всего ответов
    retention_rate = models.FloatField()  # Процент удержания
    updated_at = models.DateTimeField(auto_now=True)
```

##### Кривая карточки:
```python
# В модели CardReview уже есть все данные:
# - interval_before (интервал до ответа)
# - answer (успех/провал)
# 
# Можно строить кривую по истории ответов конкретной карточки
```

#### API:
```python
GET /api/training/forgetting-curve/
# Возвращает данные для графика кривой пользователя

GET /api/cards/{id}/forgetting-curve/
# Возвращает данные для графика кривой карточки
```

#### Визуализация (Frontend):
- График с осями: X = дни, Y = вероятность вспоминания (%)
- Точки — реальные данные пользователя
- Линия — теоретическая кривая R(t) = e^(-t/S)
- Вертикальные линии — интервалы повторений

```
100% ┤●━━━━●
 90% ┤      ╲━━━●
 80% ┤           ╲━━━●
 70% ┤                ╲━━━●
 60% ┤                     ╲
     └─────┴─────┴─────┴─────┴───► дни
         1     7    30    90
```

#### Реализовано:
- [x] API GET /api/training/forgetting-curve/ (агрегация без доп. модели)
- [x] Кривая пользователя: реальные точки по бакетам интервалов
- [x] Теоретическая кривая R(t) = e^(-t/S)
- [x] Recharts: AreaChart + BarChart + таблица
- [x] Summary карточки: total_reviews, avg_retention, stability
- [x] Страница /forgetting-curve в навигации
- [x] Навигация + все 7 локалей
- [x] Тесты API (пустые данные, с карточками)
- [x] Развёрнуто на production

---

### Этап 15: Документация и деплой ✅ Готово
**Цель**: Подготовка к релизу

#### Выполнено:
- [x] Обновление TRAINER_DEVELOPMENT_PLAN.md (все этапы помечены ✅)
- [x] Финальный бэкап production БД
- [x] 416 тестов, 80% покрытие
- [x] Все этапы (11-15) развёрнуты на production (72.56.83.95)
- [x] Backend: 23 API endpoints
- [x] Frontend: 12 страниц, 7 локалей

---

## 📊 Детальное описание моделей

### Word (обновлённая)

```python
class Word(models.Model):
    """
    Слово — базовая единица знания.
    Синонимы/антонимы реализуются через WordRelation.
    """
    # Существующие поля
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='words')
    original_word = models.CharField(max_length=200)
    translation = models.CharField(max_length=200)
    language = models.CharField(max_length=2)
    audio_file = models.FileField(blank=True)
    image_file = models.ImageField(blank=True)
    
    # ═══════════════════════════════════════════════════════════════
    # НОВЫЕ ПОЛЯ
    # ═══════════════════════════════════════════════════════════════
    
    # Контент (генерируется AI)
    etymology = models.TextField(blank=True, verbose_name='Этимология')
    sentences = models.JSONField(default=list, verbose_name='Предложения')
    # Формат sentences: [{"text": "Der Hund läuft.", "source": "ai"}, ...]
    
    # Пользовательский контент
    notes = models.TextField(blank=True, verbose_name='Заметки')
    
    # Подсказки (генерируются AI, редактируются пользователем)
    hint_text = models.TextField(blank=True, verbose_name='Текстовая подсказка')
    hint_audio = models.FileField(upload_to='hints/', blank=True, verbose_name='Аудио подсказка')
    
    # Классификация
    part_of_speech = models.CharField(max_length=20, blank=True, verbose_name='Часть речи')
    categories = models.ManyToManyField('Category', blank=True, related_name='words')
    
    # Персонализация
    stickers = models.JSONField(default=list, verbose_name='Стикеры')
    # Формат stickers: ["❤️", "⭐", "🔥"]
    
    # Статус
    learning_status = models.CharField(
        max_length=20,
        choices=[
            ('new', 'Новое'),
            ('learning', 'В изучении'),
            ('reviewing', 'На повторении'),
            ('mastered', 'Освоено'),
        ],
        default='new'
    )
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        unique_together = ['user', 'original_word', 'language']
    
    # ═══════════════════════════════════════════════════════════════
    # МЕТОДЫ ДЛЯ РАБОТЫ СО СВЯЗЯМИ
    # ═══════════════════════════════════════════════════════════════
    
    def get_synonyms(self):
        """Возвращает все слова-синонимы"""
        return Word.objects.filter(
            relations_to__word_from=self,
            relations_to__relation_type='synonym'
        )
    
    def get_antonyms(self):
        """Возвращает все слова-антонимы"""
        return Word.objects.filter(
            relations_to__word_from=self,
            relations_to__relation_type='antonym'
        )
    
    def add_relation(self, other_word, relation_type):
        """Добавляет двустороннюю связь"""
        WordRelation.objects.get_or_create(
            word_from=self, word_to=other_word, relation_type=relation_type
        )
        WordRelation.objects.get_or_create(
            word_from=other_word, word_to=self, relation_type=relation_type
        )
```

### WordRelation (связи между словами)

```python
class WordRelation(models.Model):
    """
    Связь между двумя словами (синоним/антоним).
    Каждое слово остаётся самостоятельной сущностью.
    """
    RELATION_TYPES = [
        ('synonym', 'Синоним'),
        ('antonym', 'Антоним'),
    ]
    
    word_from = models.ForeignKey(
        Word, 
        on_delete=models.CASCADE, 
        related_name='relations_from'
    )
    word_to = models.ForeignKey(
        Word, 
        on_delete=models.CASCADE, 
        related_name='relations_to'
    )
    relation_type = models.CharField(max_length=20, choices=RELATION_TYPES)
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        unique_together = ['word_from', 'word_to', 'relation_type']
```

### Category

```python
class Category(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='categories')
    name = models.CharField(max_length=100)
    parent = models.ForeignKey(
        'self', 
        null=True, 
        blank=True, 
        on_delete=models.CASCADE,
        related_name='children'
    )
    icon = models.CharField(max_length=10, blank=True)
    order = models.IntegerField(default=0)
    created_at = models.DateTimeField(auto_now_add=True)
    
    class Meta:
        verbose_name_plural = 'Categories'
        ordering = ['order', 'name']
        unique_together = ['user', 'name', 'parent']
```

### Card

```python
class Card(models.Model):
    CARD_TYPES = [
        ('normal', 'Обычная'),
        ('inverted', 'Инвертированная'),
        ('empty', 'Пустая'),
        ('cloze', 'С пропуском'),
    ]
    
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='cards')
    word = models.ForeignKey(Word, on_delete=models.CASCADE, related_name='cards')
    card_type = models.CharField(max_length=20, choices=CARD_TYPES, default='normal')
    
    # Для cloze
    cloze_sentence = models.TextField(blank=True)
    cloze_word_index = models.IntegerField(default=0)
    
    # SM-2 параметры
    ease_factor = models.FloatField(default=2.5)
    interval = models.IntegerField(default=0)  # Дни
    repetitions = models.IntegerField(default=0)
    lapses = models.IntegerField(default=0)
    consecutive_lapses = models.IntegerField(default=0)
    
    # Планирование
    next_review = models.DateTimeField(null=True, blank=True)
    last_review = models.DateTimeField(null=True, blank=True)
    
    # Статусы
    is_in_learning_mode = models.BooleanField(default=True)
    is_auxiliary = models.BooleanField(default=False)
    is_suspended = models.BooleanField(default=False)
    
    # Внутрисессионное обучение
    learning_step = models.IntegerField(default=0)
    
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        indexes = [
            models.Index(fields=['user', 'next_review']),
            models.Index(fields=['user', 'is_in_learning_mode']),
        ]
```

### UserTrainingSettings

```python
class UserTrainingSettings(models.Model):
    """
    ВСЕ константы алгоритма SM-2 настраиваемые пользователем.
    """
    AGE_GROUPS = [
        ('young', 'До 18 лет'),
        ('adult', '18-50 лет'),
        ('senior', '50+ лет'),
    ]
    
    user = models.OneToOneField(User, on_delete=models.CASCADE, related_name='training_settings')
    age_group = models.CharField(max_length=20, choices=AGE_GROUPS, default='adult')
    
    # ═══════════════════════════════════════════════════════════════
    # КОНСТАНТЫ SM-2 (все настраиваемые!)
    # ═══════════════════════════════════════════════════════════════
    
    # Ease Factor
    starting_ease = models.FloatField(default=2.5)
    min_ease_factor = models.FloatField(default=1.3)
    
    # Дельты EF
    again_ef_delta = models.FloatField(default=-0.2)
    hard_ef_delta = models.FloatField(default=-0.15)
    good_ef_delta = models.FloatField(default=0.0)
    easy_ef_delta = models.FloatField(default=0.15)
    
    # Модификаторы интервалов
    interval_modifier = models.FloatField(default=1.0)
    hard_interval_modifier = models.FloatField(default=1.2)
    easy_bonus = models.FloatField(default=1.3)
    
    # Шаги обучения
    learning_steps = models.JSONField(default=lambda: [2, 10])
    graduating_interval = models.IntegerField(default=1)
    easy_interval = models.IntegerField(default=4)
    
    # ═══════════════════════════════════════════════════════════════
    # НАСТРОЙКИ СЕССИИ
    # ═══════════════════════════════════════════════════════════════
    default_session_duration = models.IntegerField(default=20)
    
    # ═══════════════════════════════════════════════════════════════
    # ПОРОГИ
    # ═══════════════════════════════════════════════════════════════
    lapse_threshold = models.IntegerField(default=4)
    stability_threshold = models.IntegerField(default=60)
    calibration_interval = models.IntegerField(default=50)
    target_retention = models.FloatField(default=0.90)
    
    # ═══════════════════════════════════════════════════════════════
    # КАЛИБРОВКА
    # ═══════════════════════════════════════════════════════════════
    total_reviews = models.IntegerField(default=0)
    successful_reviews = models.IntegerField(default=0)
    last_calibration_at = models.IntegerField(default=0)
```

### NotificationSettings

```python
class NotificationSettings(models.Model):
    """Настройки оповещений пользователя"""
    user = models.OneToOneField(User, on_delete=models.CASCADE, related_name='notification_settings')
    
    # Web Push
    push_enabled = models.BooleanField(default=True)
    push_frequency = models.CharField(
        max_length=20,
        choices=[
            ('aggressive', 'Агрессивный'),
            ('normal', 'Обычный'),
            ('minimal', 'Минимальный'),
        ],
        default='normal'
    )
    
    # Email
    email_enabled = models.BooleanField(default=True)
    email_daily_time = models.TimeField(default='09:00')
    
    # Тихие часы
    quiet_hours_start = models.TimeField(default='22:00')
    quiet_hours_end = models.TimeField(default='08:00')
    
    timezone = models.CharField(max_length=50, default='Europe/Moscow')
```

### PushSubscription

```python
class PushSubscription(models.Model):
    """Подписки на Web Push уведомления"""
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='push_subscriptions')
    subscription_info = models.JSONField()  # endpoint, keys
    user_agent = models.CharField(max_length=500, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
    is_active = models.BooleanField(default=True)
```

### UserForgettingCurve

```python
class UserForgettingCurve(models.Model):
    """Данные для построения кривой забывания пользователя"""
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='forgetting_curve_data')
    interval_days = models.IntegerField()  # Группировка по интервалам
    success_count = models.IntegerField(default=0)
    total_count = models.IntegerField(default=0)
    retention_rate = models.FloatField()  # success_count / total_count
    updated_at = models.DateTimeField(auto_now=True)
    
    class Meta:
        unique_together = ['user', 'interval_days']
```

### StudySession

```python
class StudySession(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='study_sessions')
    deck = models.ForeignKey('cards.Deck', null=True, blank=True, on_delete=models.SET_NULL)
    
    started_at = models.DateTimeField(auto_now_add=True)
    ended_at = models.DateTimeField(null=True, blank=True)
    target_duration = models.IntegerField()  # Минуты
    
    # Статистика
    cards_reviewed = models.IntegerField(default=0)
    cards_new = models.IntegerField(default=0)
    cards_learning = models.IntegerField(default=0)
    
    again_count = models.IntegerField(default=0)
    hard_count = models.IntegerField(default=0)
    good_count = models.IntegerField(default=0)
    easy_count = models.IntegerField(default=0)
```

### CardReview (история ответов)

```python
class CardReview(models.Model):
    card = models.ForeignKey(Card, on_delete=models.CASCADE, related_name='reviews')
    session = models.ForeignKey(StudySession, on_delete=models.CASCADE, related_name='reviews')
    
    answer = models.IntegerField()  # 0-3
    time_spent = models.FloatField()  # Секунды
    
    # Состояние до ответа
    ease_factor_before = models.FloatField()
    interval_before = models.IntegerField()
    
    # Состояние после ответа
    ease_factor_after = models.FloatField()
    interval_after = models.IntegerField()
    
    created_at = models.DateTimeField(auto_now_add=True)
```

---

## 🔌 API эндпоинты

### Слова (Words)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/words/` | Список слов с фильтрами |
| POST | `/api/words/` | Создание слова |
| GET | `/api/words/{id}/` | Детали слова |
| PATCH | `/api/words/{id}/` | Обновление слова (включая hint_text) |
| DELETE | `/api/words/{id}/` | Удаление слова |
| POST | `/api/words/{id}/generate-etymology/` | Генерация этимологии |
| POST | `/api/words/{id}/generate-hint/` | Генерация подсказки (сохраняется в слове) |
| POST | `/api/words/{id}/generate-sentence/` | Генерация предложения |
| POST | `/api/words/{id}/create-card/` | Создать карточку |
| GET | `/api/words/{id}/forgetting-curve/` | Кривая забывания слова |

### Связи между словами (WordRelation)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/words/{id}/relations/` | Все связи слова |
| POST | `/api/words/{id}/add-synonym/` | Добавить синоним (создаёт связь) |
| POST | `/api/words/{id}/add-antonym/` | Добавить антоним (создаёт связь) |
| DELETE | `/api/words/{id}/relations/{relation_id}/` | Удалить связь |
| POST | `/api/words/{id}/generate-synonym/` | AI генерирует синоним как новое слово |
| POST | `/api/words/{id}/generate-antonym/` | AI генерирует антоним как новое слово |

### Категории (Categories)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/categories/` | Дерево категорий |
| POST | `/api/categories/` | Создание категории |
| PATCH | `/api/categories/{id}/` | Обновление категории |
| DELETE | `/api/categories/{id}/` | Удаление категории |

### Карточки (Cards)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/cards/` | Список карточек |
| GET | `/api/cards/{id}/` | Детали карточки |
| DELETE | `/api/cards/{id}/` | Удаление карточки |
| POST | `/api/cards/{id}/suspend/` | Приостановить |
| POST | `/api/cards/{id}/unsuspend/` | Возобновить |

### Тренировка (Training)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/training/session/` | Получить карточки для сессии |
| POST | `/api/training/answer/` | Отправить ответ |
| POST | `/api/training/enter-learning/` | В режим изучения |
| POST | `/api/training/exit-learning/` | Выйти из режима изучения |
| GET | `/api/training/stats/` | Статистика |
| GET | `/api/training/settings/` | Настройки алгоритма |
| PATCH | `/api/training/settings/` | Обновить настройки алгоритма |
| POST | `/api/training/settings/reset/` | Сбросить к значениям по умолчанию |
| GET | `/api/training/forgetting-curve/` | Кривая забывания пользователя |

### Оповещения (Notifications)

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/notifications/settings/` | Настройки оповещений |
| PATCH | `/api/notifications/settings/` | Обновить настройки |
| POST | `/api/notifications/push/subscribe/` | Подписаться на Web Push |
| POST | `/api/notifications/push/unsubscribe/` | Отписаться от Web Push |
| GET | `/api/notifications/push/vapid-key/` | Получить VAPID публичный ключ |

---

## 🧮 Алгоритм SM-2

> ⚠️ **Важно**: ВСЕ константы алгоритма настраиваются пользователем в `UserTrainingSettings`.
> Значения ниже — это значения по умолчанию.

### Порог для режима изучения

На основе анализа теории SuperMemo и практики использования Anki, оптимальный порог:

**4 нажатия "Снова" подряд** → автоматическая отправка в режим Изучения

#### Обоснование:
- 1-2 провала — нормальная ситуация, слово ещё закрепляется
- 3 провала — уже сигнал о сложности
- 4 провала — явная проблема с пониманием, нужно глубокое изучение
- 5+ провалов — слишком поздно, пользователь уже расстроен

### Формулы

#### Новый интервал:

```
if answer == AGAIN:
    interval = 0  # Сброс, внутрисессионное обучение
    consecutive_lapses += 1
    
elif answer == HARD:
    interval = current_interval * 1.2
    EF = max(1.3, EF - 0.15)
    
elif answer == GOOD:
    interval = current_interval * EF
    
elif answer == EASY:
    interval = current_interval * EF * 1.3
    EF = EF + 0.15
```

#### Калибровка (каждые 50 ответов):

```python
def calibrate(settings):
    if settings.total_reviews - settings.last_calibration_at < 50:
        return
    
    # Вычисляем фактический % успеха за последние 50 ответов
    recent_success_rate = recent_successful / 50
    target_rate = 0.90  # Целевые 90%
    
    # Корректируем interval_modifier
    if recent_success_rate < 0.85:
        settings.interval_modifier *= 0.95  # Уменьшаем интервалы
    elif recent_success_rate > 0.95:
        settings.interval_modifier *= 1.05  # Увеличиваем интервалы
    
    settings.last_calibration_at = settings.total_reviews
    settings.save()
```

---

## 🔔 Система оповещений

### Проблема
Веб-приложение без мобильного клиента. Нужна агрессивная система напоминаний.

### Решение: многоканальные оповещения

#### 1. Web Push Notifications (основной канал)
- Работает даже при закрытом браузере
- Требует разрешения пользователя
- Используем: `django-webpush` + `pywebpush`

**Как работает:**
1. При первом входе — запрос разрешения на уведомления
2. Сохраняем `subscription_info` в `PushSubscription`
3. Celery задача проверяет пользователей каждые 30 минут
4. Отправляем Push если есть карточки для повторения

#### 2. Email (резервный канал)
- Ежедневное напоминание в выбранное время
- Недельный дайджест (статистика)
- Предупреждение о streak

#### 3. In-app уведомления
- Баннер при входе: "У вас 25 карточек для повторения"
- Звуковой сигнал при открытом табе

### Триггеры оповещений

| Триггер | Push | Email |
|---------|------|-------|
| Есть карточки, не заходил 4+ часа | ✅ | - |
| Накопилось 50+ карточек | ✅ | ✅ |
| Streak в опасности (вечер) | ✅ | ✅ |
| Ежедневное напоминание | - | ✅ |
| Недельный дайджест | - | ✅ |

### Частота оповещений (настраивается)

| Режим | Описание |
|-------|----------|
| Агрессивный | Push каждые 4 часа если есть карточки |
| Обычный | Push 2 раза в день (утро, вечер) |
| Минимальный | Push 1 раз в день |

---

## 📈 Кривые забывания

### Индивидуальная кривая пользователя

Строится на основе реальных данных ответов:
- X-ось: интервал в днях (1, 7, 30, 60, 90...)
- Y-ось: процент успешных ответов (retention rate)

```
100% ┤●━━━●
 90% ┤     ╲━━━●
 80% ┤          ╲━━━●
 70% ┤               ╲━━━●
 60% ┤                    ╲
     └────┴────┴────┴────┴───► дни
         1    7   30   60  90
```

### Кривая конкретной карточки

Показывает историю ответов карточки:
- Точки — моменты повторения
- Цвет точки — результат (зелёный=успех, красный=провал)
- Линия — рост интервала

### Использование

1. **В профиле пользователя**: виджет с кривой + сравнение с "идеальной"
2. **На странице слова**: кривая конкретного слова
3. **Калибровка**: если реальная кривая сильно отличается от целевой — корректировка `interval_modifier`

---

## 🔄 Миграция данных

### Порядок выполнения:

1. **Создать новые модели** без удаления существующих
2. **Миграция Word**:
   - Добавить новые поля
   - Установить `learning_status = 'new'`
3. **Создать Card** для каждого Word:
   - `card_type` из старого поля Word.card_type
   - `is_auxiliary = True` для empty
4. **Создать UserTrainingSettings** для всех пользователей
5. **Фоновая генерация этимологии** (Celery task)
6. **Удалить** старое поле `Word.card_type`

### Скрипт миграции:

```python
# backend/apps/training/management/commands/migrate_to_trainer.py

from django.core.management.base import BaseCommand
from apps.words.models import Word
from apps.training.models import Card, UserTrainingSettings
from apps.users.models import User

class Command(BaseCommand):
    help = 'Migrates existing data to new trainer structure'
    
    def handle(self, *args, **options):
        # 1. Создать UserTrainingSettings
        for user in User.objects.all():
            UserTrainingSettings.objects.get_or_create(user=user)
            self.stdout.write(f'Created settings for {user.username}')
        
        # 2. Создать Card для каждого Word
        for word in Word.objects.all():
            card_type = getattr(word, 'card_type', 'normal') or 'normal'
            
            Card.objects.get_or_create(
                user=word.user,
                word=word,
                card_type=card_type,
                defaults={
                    'is_auxiliary': card_type in ['empty'],
                    'is_in_learning_mode': True,
                }
            )
            
            # Установить learning_status
            word.learning_status = 'new'
            word.save(update_fields=['learning_status'])
        
        self.stdout.write(self.style.SUCCESS('Migration completed!'))
```

---

## 🎨 UI/UX

### Цветовая схема статусов

| Статус | Цвет | Описание |
|--------|------|----------|
| `new` | 🔵 Синий | Новое слово |
| `learning` | 🟠 Оранжевый | В процессе изучения |
| `reviewing` | 🟢 Зелёный | На повторении |
| `mastered` | 🟣 Фиолетовый | Освоено |

### Иконки типов карточек

| Тип | Иконка |
|-----|--------|
| `normal` | 📝 |
| `inverted` | 🔄 |
| `empty` | 🖼️ |
| `cloze` | ✍️ |

### Анимации

- **Переворот карточки**: 3D flip (0.3s)
- **Появление кнопок**: fade-in (0.2s)
- **Прогресс**: smooth transition
- **Режим изучения**: slide-up panel

---

## ✅ Чеклист готовности к релизу

### Backend
- [ ] Все модели созданы и протестированы
- [ ] Все API эндпоинты работают
- [ ] Алгоритм SM-2 корректно работает
- [ ] Миграция данных проверена
- [ ] AI генерация работает
- [ ] Система токенов интегрирована

### Frontend
- [ ] Каталог слов работает
- [ ] Управление категориями работает
- [ ] Страница тренировки работает
- [ ] Режим изучения работает
- [ ] Все анимации плавные
- [ ] Responsive дизайн

### Тестирование
- [ ] Unit тесты алгоритма SM-2
- [ ] API тесты
- [ ] E2E тесты тренировки
- [ ] Тест миграции на копии базы

### Документация
- [ ] API документация
- [ ] Гайд пользователя
- [ ] README обновлён

---

## 📝 Примечания

### Решённые вопросы (v1.1)
- ✅ Синонимы/антонимы — через отдельную модель `WordRelation`, слова самостоятельные
- ✅ Все константы SM-2 — настраиваемые пользователем
- ✅ Подсказки — хранятся в слове, редактируемые
- ✅ Категории — неограниченная вложенность, создаются пользователем
- ✅ Восстановление cloze — из `sentences` в модели Word
- ✅ Система оповещений — Web Push + Email

### Открытые вопросы
1. Нужен ли отдельный экран статистики или достаточно виджета на главной?
2. Как отображать синонимы/антонимы в режиме тренировки — кнопка "показать связанные"?
3. Telegram-бот — в будущем или не нужен?

### Будущие улучшения (после MVP)
- Геймификация (streak, достижения, бейджи)
- Telegram-бот для напоминаний
- Голосовой ввод ответов
- Соревнования между пользователями
- AI-анализ ошибок и рекомендации
- Интеграция с Anki (импорт/экспорт прогресса)

---

## 📊 Сводка этапов (с разделением Backend/Frontend)

### Подход: Гибридный
1. **Фаза 1 (Фундамент)**: Только Backend — модели, миграции, базовый API
2. **Фаза 2 (Фичи)**: Backend + Frontend по очереди — полностью работающие фичи

---

### 🔷 ФАЗА 1: ФУНДАМЕНТ (Backend only)

| Этап | Название | Тип | Описание |
|------|----------|-----|----------|
| 0 | Подготовка | 📋 | Планирование, ревью плана |
| 1 | Рефакторинг Word | 🔙 BE | Новые поля модели |
| 1.5 | WordRelation | 🔙 BE | Модель связей между словами |
| 2 | Category | 🔙 BE | Модель категорий |
| 3 | Card | 🔙 BE | Модель карточек + SM-2 поля |
| 4 | UserTrainingSettings | 🔙 BE | Настройки алгоритма |
| 5 | Алгоритм SM-2 | 🔙 BE | Логика (`sm2.py`) |
| 6 | Миграция данных | 🔙 BE | Перенос существующих Word → Card |

**Результат Фазы 1**: Все модели, миграции, алгоритм. API готов, но без UI.

---

### 🔷 ФАЗА 2: ФИЧИ (Backend + Frontend)

| Этап | Фича | Backend | Frontend |
|------|------|---------|----------|
| 7 | **Каталог слов** | API: `/api/words/` (CRUD, фильтры) | Страница `/words`, `/words/{id}` |
| 8 | **Категории** | API: `/api/categories/` (CRUD, дерево) | Страница `/categories`, дерево с drag-drop |
| 9 | **AI генерация** | Эндпоинты генерации (etymology, hint, sentence) | Кнопки генерации в UI слова |
| 10 | **Тренировка** | API: `/api/training/` (session, answer) | Страницы `/training`, `/training/session` |
| 11 | **Режим изучения** | API: enter/exit learning mode | Оверлей/панель режима изучения |
| 12 | **Настройки SM-2** | API: `/api/training/settings/` | Страница настроек в профиле |
| 13 | **Оповещения** | Web Push, Celery tasks, Email | Запрос разрешений, настройки |
| 14 | **Кривые забывания** | API: `/api/training/forgetting-curve/` | Графики (Chart.js/Recharts) |

---

### 🔷 ФАЗА 3: ФИНАЛИЗАЦИЯ

| Этап | Название | Описание |
|------|----------|----------|
| 15 | Тестирование | E2E тесты, нагрузка, баги |
| 16 | Документация | API docs, гайд пользователя |
| 17 | Деплой | Staging → Production |

---

## 🗓️ Визуальный таймлайн

```
ФАЗА 1: ФУНДАМЕНТ (Backend)
═══════════════════════════════════════════════════════
Week 1-2: [Этап 0-3] Модели Word, WordRelation, Category, Card
Week 3:   [Этап 4-5] Settings, SM-2 алгоритм
Week 4:   [Этап 6]   Миграция данных

ФАЗА 2: ФИЧИ (Backend + Frontend)
═══════════════════════════════════════════════════════
Week 5:   [Этап 7]   📦 AI генерация (BE + FE)
Week 6:   [Этап 8]   📦 Каталог слов (BE + FE)
Week 7:   [Этап 9]   📦 Категории (BE + FE)
Week 8-9: [Этап 10]  📦 Тренировка (BE + FE) ← CORE!
Week 10:  [Этап 11]  📦 Режим изучения (BE + FE)
Week 11:  [Этап 12]  📦 Настройки SM-2 (BE + FE)
Week 12:  [Этап 13]  📦 Оповещения (BE + FE)
Week 13:  [Этап 14]  📦 Кривые забывания (BE + FE)

ФАЗА 3: ФИНАЛИЗАЦИЯ
═══════════════════════════════════════════════════════
Week 14:  [Этап 15]  Тестирование
Week 15:  [Этап 16]  Документация
Week 16:  [Этап 17]  Деплой
```

---

## 🎯 Порядок разработки каждой фичи (Фаза 2)

Для каждого этапа в Фазе 2:

```
1. [BE] Модели/миграции (если нужно)
         ↓
2. [BE] Сериализаторы
         ↓
3. [BE] API эндпоинты
         ↓
4. [BE] Тесты API
         ↓
5. [FE] Типы TypeScript
         ↓
6. [FE] API сервис
         ↓
7. [FE] Компоненты
         ↓
8. [FE] Страница
         ↓
9. [  ] Интеграционный тест
```

---

## 💡 Альтернатива: Параллельная разработка

Если есть **два разработчика** (BE + FE):

```
Week 5:
  BE: Этап 7 (Каталог слов API)
  FE: UI компоненты (общие)

Week 6:
  BE: Этап 8 (Категории API)
  FE: Этап 7 (Каталог слов страница)

Week 7:
  BE: Этап 9 (AI генерация)
  FE: Этап 8 (Категории страница)

... и так далее (FE отстаёт на 1 неделю)
```

---

> **Следующий шаг**: Этап 11 — Миграция данных. Перенос существующих данных в новую архитектуру (Card из Word). Далее — Этапы 12-15.
