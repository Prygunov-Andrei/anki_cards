# 🚨 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Генерация медиа

## ❌ Проблема

В предыдущей реализации (Этапы 11-12) медиа-файлы **НЕ генерировались**, хотя флаги `generate_images` и `generate_audio` передавались в backend.

**Причина:** Backend НЕ генерирует медиа автоматически. Он ожидает, что медиа уже сгенерированы и переданы в параметрах `image_files` и `audio_files`.

## ✅ Решение

Полностью переработан flow генерации карточек с правильным циклом генерации медиа **перед** созданием колоды.

---

## 📊 Сравнение: ДО vs ПОСЛЕ

### ❌ ДО (НЕПРАВИЛЬНО)

```typescript
// Пользователь нажимает "Сгенерировать"
const { file_id } = await deckService.generateCards({
  words: "apple,cat",
  translations: { apple: "яблоко", cat: "кошка" },
  generate_images: true,  // ← Флаг игнорируется backend!
  generate_audio: true,    // ← Флаг игнорируется backend!
  image_files: {},         // ← ПУСТО!
  audio_files: {}          // ← ПУСТО!
});

// ❌ Результат: .apkg без медиа, скачивается мгновенно
```

### ✅ ПОСЛЕ (ПРАВИЛЬНО)

```typescript
// ЭТАП 1: Генерация изображений (3-10 сек на слово)
for (let i = 0; i < translations.length; i++) {
  const { image_url } = await deckService.generateImage({
    word: pair.word,
    translation: pair.translation,
    language: targetLang,
    image_style: imageStyle,
  });
  imageFiles[pair.word] = image_url;
}

// ЭТАП 2: Генерация аудио (2-3 сек на слово)
for (let i = 0; i < translations.length; i++) {
  const { audio_url } = await deckService.generateAudio({
    word: pair.word,
    language: targetLang,
  });
  audioFiles[pair.word] = audio_url;
}

// ЭТАП 3: Создание колоды с медиа
const { file_id } = await deckService.generateCards({
  words: ["apple", "cat"],
  translations: { apple: "яблоко", cat: "кошка" },
  image_files: { apple: "/media/images/xxx.jpg", ... },  // ✅ ЗАПОЛНЕНО!
  audio_files: { apple: "/media/audio/yyy.mp3", ... }     // ✅ ЗАПОЛНЕНО!
});

// ✅ Результат: .apkg с медиа внутри
```

---

## 🎯 Реализованные изменения

### 1. Новые API методы в `deck.service.ts`

```typescript
/**
 * Генерация изображения для слова
 */
async generateImage(data: {
  word: string;
  translation: string;
  language: string;
  image_style: 'minimalistic' | 'balanced' | 'creative';
}): Promise<{ image_url: string }> {
  const response = await api.post('/api/media/generate-image/', data);
  return response.data;
}

/**
 * Генерация аудио для слова
 */
async generateAudio(data: {
  word: string;
  language: string;
}): Promise<{ audio_url: string }> {
  const response = await api.post('/api/media/generate-audio/', data);
  return response.data;
}
```

### 2. Обновлен `generateCards()` 

**ДО:**
```typescript
async generateCards(data: {
  words: string;
  translations: Record<string, string>;
  language: string;
  deck_name: string;
  generate_images: boolean;  // ❌ Удалено
  generate_audio: boolean;   // ❌ Удалено
  image_style: string;       // ❌ Удалено
})
```

**ПОСЛЕ:**
```typescript
async generateCards(data: {
  words: string[];           // ✅ Теперь массив
  translations: Record<string, string>;
  language: string;
  deck_name: string;
  image_files?: Record<string, string>;  // ✅ Добавлено
  audio_files?: Record<string, string>;  // ✅ Добавлено
})
```

### 3. Создан компонент `GenerationProgress`

**Файл:** `/components/GenerationProgress.tsx`

**Возможности:**
- Отображение текущего этапа:
  - `generating_images` - Генерация изображений
  - `generating_audio` - Генерация аудио
  - `creating_deck` - Создание колоды
  - `complete` - Готово
- Прогресс-бар с процентами
- Показ текущего слова
- Анимированные индикаторы загрузки
- Информация о времени (3-10 сек для изображений, 2-3 сек для аудио)

**Дизайн:**
```
┌─────────────────────────────────────────────┐
│ 🎨 Генерация изображений                    │
│ Создаём изображение для "apple"...         │
│                                             │
│ ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░        │
│ 3 из 5                              60%     │
│                                             │
│ ⏱️ Генерация изображений может занять      │
│   3-10 секунд на слово                      │
│                                             │
│ ● ● ●  (анимированные точки)                │
└─────────────────────────────────────────────┘
```

### 4. Полностью переписан `handleGenerate()` в `MainPage.tsx`

**Новый flow:**

```typescript
const handleGenerate = async () => {
  // 1. Валидация
  if (!validateForm()) return;
  
  // 2. Проверка токенов
  if (balance < requiredTokens) {
    setShowInsufficientTokensModal(true);
    return;
  }
  
  setIsGenerating(true);
  
  // 3. Словари для медиа
  const imageFiles: Record<string, string> = {};
  const audioFiles: Record<string, string> = {};
  
  // 4. ЭТАП 1: Генерация изображений
  if (generateImages) {
    setGenerationStatus('generating_images');
    
    for (let i = 0; i < translations.length; i++) {
      setGenerationProgress({
        current: i + 1,
        total: translations.length,
        currentWord: pair.word,
      });
      
      const { image_url } = await deckService.generateImage({...});
      imageFiles[pair.word] = image_url;
    }
  }
  
  // 5. ЭТАП 2: Генерация аудио
  if (generateAudio) {
    setGenerationStatus('generating_audio');
    
    for (let i = 0; i < translations.length; i++) {
      setGenerationProgress({
        current: i + 1,
        total: translations.length,
        currentWord: pair.word,
      });
      
      const { audio_url } = await deckService.generateAudio({...});
      audioFiles[pair.word] = audio_url;
    }
  }
  
  // 6. ЭТАП 3: Создание колоды с медиа
  setGenerationStatus('creating_deck');
  
  const { file_id } = await deckService.generateCards({
    words: translations.map(p => p.word),
    translations: {...},
    language: targetLang,
    deck_name: deckName,
    image_files: imageFiles,  // ✅ Сгенерированные изображения
    audio_files: audioFiles,  // ✅ Сгенерированные аудио
  });
  
  // 7. Скачивание
  const blob = await deckService.downloadDeck(file_id);
  // ... скачивание файла
  
  // 8. Завершение
  setGenerationStatus('complete');
  showSuccess('Карточки успешно созданы!');
  
  // 9. Сброс формы через 2 секунды
  setTimeout(() => {
    setWords([]);
    setTranslations([]);
    setGenerationStatus('idle');
  }, 2000);
};
```

### 5. Новые state переменные

```typescript
// Прогресс генерации
const [generationStatus, setGenerationStatus] = useState<GenerationStatus>('idle');
const [generationProgress, setGenerationProgress] = useState({
  current: 0,
  total: 0,
  currentWord: ''
});
```

---

## ⏱️ Ожидаемое время генерации

| Кол-во слов | Изображения | Аудио | Колода | **Итого** |
|-------------|-------------|-------|--------|-----------|
| 1 слово     | 3-10 сек    | 2-3 сек | 1 сек | **6-14 сек** |
| 5 слов      | 15-50 сек   | 10-15 сек | 1 сек | **26-66 сек** |
| 10 слов     | 30-100 сек  | 20-30 сек | 1 сек | **51-131 сек** |
| 20 слов     | 60-200 сек  | 40-60 сек | 1 сек | **101-261 сек** |

**Примечание:** Если какой-то тип медиа отключен, время сокращается соответственно.

---

## 🎨 UI изменения

### Расположение компонентов (обновленное):

```
1. Настройки колоды
2. WordChipsInput
3. Кнопка "Автоперевод"
4. TranslationTable
5. Настройки медиа
6. ╔═══════════════════════════════════════════╗
   ║ GenerationProgress (новый!)               ║  ← Показывается во время генерации
   ║  • Генерация изображений: 3/5             ║
   ║  • Генерация аудио: 5/5                   ║
   ║  • Создание колоды...                     ║
   ╚═══════════════════════════════════════════╝
7. Кнопка генерации
```

### Состояния кнопки генерации:

**1. Idle (готов к генерации):**
```
┌──────────────────────────────────┐
│ 📥 Сгенерировать карточки ANKI   │
└──────────────────────────────────┘
```

**2. Генерация (disabled):**
```
┌──────────────────────────────────┐
│ ⏳ Генерация карточек...          │
└──────────────────────────────────┘
```

**3. Недостаточно токенов (disabled):**
```
┌──────────────────────────────────┐
│ Недостаточно токенов              │
└──────────────────────────────────┘
```

---

## 🔄 Полный workflow пользователя

```
1. Пользователь вводит слова через WordChipsInput
   └─ Немецкие слова автоматически обрабатываются

2. Нажимает "Автоперевод" (опционально)
   └─ Пустые переводы заполняются

3. Настраивает медиа:
   ├─ ✅ Генерировать изображения
   ├─ ✅ Генерировать аудио
   └─ Стиль: Сбалансированный

4. Нажимает "Сгенерировать карточки ANKI"

5. Появляется GenerationProgress:
   
   ┌────────────────────────────────┐
   │ 🎨 Генерация изображений       │
   │ Создаём изображение для "Hund" │
   │ ▓▓▓▓▓▓░░░░░░░░░ 40%           │
   │ 2 из 5                         │
   │ ⏱️ 3-10 секунд на слово        │
   └────────────────────────────────┘
   
   ⬇️ (через 15-50 сек)
   
   ┌────────────────────────────────┐
   │ 🔊 Генерация аудио             │
   │ Создаём аудио для "Katze"      │
   │ ▓▓▓▓▓▓▓▓▓▓▓▓░░ 80%            │
   │ 4 из 5                         │
   │ ⏱️ 2-3 секунды на слово        │
   └────────────────────────────────┘
   
   ⬇️ (через 10-15 сек)
   
   ┌────────────────────────────────┐
   │ 📦 Создание колоды             │
   │ Собираем .apkg файл с медиа... │
   └────────────────────────────────┘
   
   ⬇️ (через 1 сек)
   
   ┌────────────────────────────────┐
   │ ✅ Готово!                      │
   │ Колода успешно создана         │
   └────────────────────────────────┘

6. Файл .apkg автоматически скачивается

7. Форма сбрасывается через 2 секунды
```

---

## 🐛 Error Handling

### Graceful degradation при ошибках:

```typescript
// Если генерация одного изображения провалилась, продолжаем с остальными
for (let i = 0; i < translations.length; i++) {
  try {
    const { image_url } = await deckService.generateImage({...});
    imageFiles[pair.word] = image_url;
  } catch (error) {
    console.error(`Error generating image for "${pair.word}":`, error);
    // ✅ Продолжаем с следующим словом
    // Карточка создастся без изображения для этого слова
  }
}
```

**Результат:** Если не удалось сгенерировать медиа для 1-2 слов из 10, остальные 8-9 будут с медиа.

---

## 📦 Созданные/Обновленные файлы

### Созданные:
1. `/components/GenerationProgress.tsx` - компонент прогресса (~140 строк)
2. `/MEDIA_GENERATION_FIX.md` - эта документация

### Обновленные:
1. `/services/deck.service.ts`:
   - Добавлены `generateImage()` и `generateAudio()`
   - Обновлен тип `generateCards()`
   
2. `/pages/MainPage.tsx`:
   - Импорт `GenerationProgress`
   - Новые state: `generationStatus`, `generationProgress`
   - Полностью переписан `handleGenerate()`
   - Добавлен `<GenerationProgress />` в рендер

---

## ✅ Чеклист проверки

### API интеграция:
- [x] `POST /api/media/generate-image/` вызывается для каждого слова
- [x] `POST /api/media/generate-audio/` вызывается для каждого слова
- [x] `POST /api/cards/generate/` получает `image_files` и `audio_files`
- [x] Параметры `generate_images` и `generate_audio` больше не передаются

### Прогресс:
- [x] Отображается текущий этап генерации
- [x] Показывается прогресс-бар
- [x] Отображается текущее слово
- [x] Анимация во время загрузки
- [x] Статус "complete" перед сбросом формы

### UX:
- [x] Форма disabled во время генерации
- [x] Кнопка показывает "Генерация карточек..."
- [x] Toast уведомления на каждом этапе
- [x] Graceful error handling
- [x] Автоматический сброс формы после завершения

### Performance:
- [x] Медиа генерируется последовательно (для контроля нагрузки)
- [x] Progress обновляется после каждого слова
- [x] Timeout для автосброса формы
- [x] Обновление баланса токенов после генерации

---

## 🎯 Результат

**ДО:** .apkg файл создавался мгновенно (1-2 сек), но **БЕЗ медиа внутри** ❌

**ПОСЛЕ:** .apkg файл создается с задержкой (в зависимости от кол-ва слов), но **С медиа внутри** ✅

### Пример для 5 слов:

**Время генерации:** ~40-80 секунд

**Содержимое .apkg:**
```
my-deck.apkg/
├── media/
│   ├── 1.jpg  ← apple image
│   ├── 2.jpg  ← cat image
│   ├── 3.jpg  ← dog image
│   ├── 4.jpg  ← house image
│   ├── 5.jpg  ← car image
│   ├── 1.mp3  ← apple audio
│   ├── 2.mp3  ← cat audio
│   ├── 3.mp3  ← dog audio
│   ├── 4.mp3  ← house audio
│   └── 5.mp3  ← car audio
└── collection.anki2
```

---

## 🚀 Production Ready

Эта реализация полностью готова к production:

✅ Правильный API workflow  
✅ Прогресс-бар для UX  
✅ Error handling  
✅ Graceful degradation  
✅ Toast notifications  
✅ Disabled states  
✅ Автоматический сброс  
✅ TypeScript типизация  
✅ iOS 25 стиль дизайн  

---

**Дата исправления:** 1 декабря 2025  
**Версия:** 2.0  
**Статус:** ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ ЗАВЕРШЕНО
