# ⚡ Быстрое тестирование - Этап 13.6

## 🎯 Цель
Проверить, что медиа (изображения и аудио) корректно привязываются к словам при генерации колоды с опцией "Сохранить в Мои колоды".

---

## ✅ Минимальный тест (2 минуты)

### Шаги:

1. **Открыть главную страницу** → `/`

2. **Добавить одно слово:**
   - В поле ввода написать: `cat`
   - Нажать Enter

3. **Получить перевод:**
   - Нажать кнопку "Показать перевод"
   - Должно появиться: `cat → кошка`

4. **Включить генерацию медиа:**
   - ☑️ Сгенерировать изображения
   - ☑️ Сгенерировать аудио

5. **Проверить чекбокс:**
   - ☑️ "Сохранить в Мои колоды" должен быть включён по умолчанию

6. **Нажать "Сгенерировать карточки"**
   - Дождаться завершения (прогресс-бар)

7. **Проверить результат:**
   - ✅ Файл `Новая колода.apkg` скачан
   - ✅ Уведомление: "Колода сохранена и файл скачан!"
   - ✅ Зелёная карточка с кнопками "Открыть в редакторе" и "Мои колоды"

8. **Открыть консоль браузера (F12):**
   ```
   ✅ Медиа привязано к слову "cat": ["image_file", "audio_file"]
   🎉 Всего привязано медиа к 1 словам
   ```

9. **Нажать "Открыть в редакторе":**
   - Должна открыться страница редактора колоды
   - В таблице слов найти строку с "cat"
   - В столбце "Медиа" должны быть иконки: **🖼️ 🔊**

10. **Проверить медиа:**
    - Кликнуть на иконку 🖼️ → откроется изображение кошки
    - Кликнуть на иконку 🔊 → воспроизведётся аудио "cat"

---

## ✅ Критерий успеха

### **Если в таблице есть иконки 🖼️ 🔊 → ТЕСТ ПРОЙДЕН! 🎉**

---

## 🔍 Проверка в DevTools

### Network (вкладка Network в DevTools):

**Последовательность запросов:**

```
1. POST /api/media/generate-image/
   → Response: { "image_url": "/media/images/cat_xxx.jpg" }

2. POST /api/media/generate-audio/
   → Response: { "audio_url": "/media/audio/cat_yyy.mp3" }

3. POST /api/cards/generate/
   Body: {
     "words": ["cat"],
     "translations": {"cat": "кошка"},
     "save_to_decks": true,
     ...
   }
   → Response: { "file_id": "uuid", "deck_id": 42 }

4. GET /api/cards/decks/42/
   → Response: {
     "id": 42,
     "words": [
       {
         "id": 1,
         "original_word": "cat",
         "translation": "кошка",
         "image_file": null,  // ← Пока null
         "audio_file": null
       }
     ]
   }

5. PATCH /api/cards/decks/42/words/1/  ← ВАЖНЫЙ ЗАПРОС!
   Body: {
     "image_file": "/media/images/cat_xxx.jpg",
     "audio_file": "/media/audio/cat_yyy.mp3"
   }
   → Response: {
     "id": 1,
     "original_word": "cat",
     "image_file": "/media/images/cat_xxx.jpg",
     "audio_file": "/media/audio/cat_yyy.mp3",
     "updated_fields": ["image_file", "audio_file"]  ← Подтверждение!
   }

6. GET /api/cards/download-deck/{file_id}/
   → binary .apkg file
```

**Проверить:**
- ✅ PATCH запрос отправлен
- ✅ Статус ответа: 200 OK
- ✅ `updated_fields` содержит `["image_file", "audio_file"]`

---

## 🐛 Что делать, если тест НЕ прошёл

### Проблема 1: Нет иконок в столбце "Медиа"

**Проверить в консоли:**
```
❌ Ошибка привязки медиа к сохранённой колоде: ...
```

**Решение:**
1. Проверить Network → есть ли PATCH запрос?
2. Если PATCH запроса нет → проверить, что `deck_id` вернулся от API
3. Если PATCH вернул ошибку → проверить логи бэкенда

---

### Проблема 2: В консоли нет логов о привязке медиа

**Проверить:**
1. Чекбокс "Сохранить в Мои колоды" включён?
2. В Network есть запрос `POST /api/cards/generate/` с `save_to_decks: true`?
3. Ответ содержит `deck_id`?

---

### Проблема 3: PATCH запрос возвращает 404

**Причина:** Слово не создано в БД

**Решение:**
1. Проверить, что бэкенд создал колоду и слова
2. Проверить GET `/api/cards/decks/{deck_id}/` → должен вернуть words

---

### Проблема 4: Медиа не загружаются в редакторе

**Причина:** URL медиа неправильные или файлы не существуют

**Решение:**
1. Проверить в Network → откуда запрашиваются медиа?
2. URL должны быть вида `/media/images/xxx.jpg`
3. Проверить, что файлы существуют на сервере

---

## 📋 Дополнительные тесты (опционально)

### Тест 2: Несколько слов

Добавить 3 слова (`cat`, `dog`, `bird`) → все должны иметь медиа

### Тест 3: Без медиа

Отключить изображения и аудио → должны быть прочерки, но без ошибок

### Тест 4: Только изображения

Включить только изображения → только иконка 🖼️

### Тест 5: Только аудио

Включить только аудио → только иконка 🔊

### Тест 6: Без сохранения в колоды

Отключить "Сохранить в Мои колоды" → только скачивание, без PATCH запросов

---

## 📊 Чек-лист

- [ ] Медиа генерируются (изображения и аудио)
- [ ] Колода создаётся в "Мои колоды"
- [ ] В консоли логи: "✅ Медиа привязано к слову..."
- [ ] В Network есть PATCH запрос с кодом 200
- [ ] В редакторе отображаются иконки 🖼️ 🔊
- [ ] Изображение открывается при клике
- [ ] Аудио воспроизводится при клике
- [ ] Без медиа нет ошибок
- [ ] Без сохранения колоды нет PATCH запросов

---

## ✅ Результат

**Если все 9 пунктов отмечены → Этап 13.6 полностью работает! 🚀**

---

**Время тестирования:** ~2-5 минут  
**Дата:** 2 декабря 2025
