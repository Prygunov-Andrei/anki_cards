# 📤 Как экспортировать проект из Figma Make

## 🎯 Цель
Вы хотите забрать весь фронтенд код из Figma Make и работать с ним в своей среде разработки.

---

## 🔍 Найдите кнопку экспорта в Figma Make

### Вариант 1: Через интерфейс

Ищите одну из этих опций:

- 📥 Кнопка **"Export"** / **"Download"** в верхнем меню
- 📂 Меню **"File"** → **"Export project"** 
- ⬇️ Иконка загрузки в правом верхнем углу
- 🔧 Меню **"Settings"** → **"Export"**

### Вариант 2: Через Git

Если Figma Make подключён к GitHub:

1. Найдите репозиторий на GitHub
2. Нажмите **"Code"** → **"Download ZIP"**
3. Или клонируйте: `git clone <url>`

---

## 📦 Что вы получите

После экспорта у вас будет ZIP архив или папка с такой структурой:

```
anki-generator-frontend/
├── App.tsx
├── components/          # ~30 компонентов
├── contexts/            # 4 контекста
├── hooks/               # 1 хук
├── lib/                 # Утилиты и конфиг
├── locales/             # 7 языков
├── pages/               # 7 страниц
├── services/            # API сервисы
├── styles/              # CSS
├── types/               # TypeScript типы
├── utils/               # Вспомогательные функции
└── ... (возможно конфиги)
```

---

## ⚠️ Что НЕ включено в экспорт

Figma Make автоматически управляет этими файлами, поэтому их нужно создать вручную:

### 1. `package.json`
### 2. `vite.config.ts`
### 3. `tsconfig.json`
### 4. `index.html`
### 5. `main.tsx`

**Все шаблоны этих файлов** находятся в **EXPORT_PROJECT.md** - просто скопируйте оттуда!

---

## 🚀 Быстрый старт после экспорта

```bash
# 1. Распакуйте ZIP (если скачали архив)
unzip anki-generator.zip
cd anki-generator-frontend

# 2. Создайте недостающие конфиги
# (см. EXPORT_PROJECT.md - скопируйте содержимое файлов)

# 3. Установите зависимости
npm install

# 4. Запустите dev сервер
npm run dev

# 5. Откройте http://localhost:3000
```

---

## 📋 Минимальный набор файлов для работы

Если Figma Make не экспортирует конфиги, создайте их вручную:

### Обязательные:

1. **`package.json`** - зависимости проекта  
   → Скопируйте из **EXPORT_PROJECT.md** раздел "package.json"

2. **`index.html`** - HTML точка входа  
   → Создайте в корне проекта

3. **`main.tsx`** - точка входа React  
   → Создайте в корне проекта

4. **`vite.config.ts`** - конфигурация сборщика  
   → Нужен для `npm run build`

5. **`tsconfig.json`** - конфигурация TypeScript  
   → Для работы с TypeScript

---

## 💡 Если экспорт не работает

### План Б: Копирование вручную

Если в Figma Make нет кнопки экспорта:

1. **Откройте файловую структуру** в Figma Make (обычно левая панель)

2. **Для каждой папки** (components, pages, и т.д.):
   - Откройте файл
   - Скопируйте содержимое (Ctrl+A, Ctrl+C)
   - Создайте такой же файл локально
   - Вставьте содержимое (Ctrl+V)

3. **Создайте конфиги** из EXPORT_PROJECT.md

4. **Запустите** `npm install` и `npm run dev`

### План В: Через браузер DevTools

1. Откройте DevTools (F12)
2. Вкладка **"Sources"**
3. Найдите исходники в дереве файлов
4. Копируйте файлы один за другим

---

## ✅ Проверка после экспорта

После того как настроили проект локально:

```bash
# Запуск
npm run dev
# ✅ Должен открыться http://localhost:3000

# Сборка
npm run build
# ✅ Должна создаться папка dist/

# Проверка работы
# ✅ Приложение загружается
# ✅ API запросы идут на ngrok
# ✅ Можно войти/зарегистрироваться
```

---

## 🔗 Следующие шаги

После успешного экспорта:

1. **Для разработки:**  
   → Работайте локально в VSCode/WebStorm  
   → `npm run dev` для запуска

2. **Для деплоя на Django:**  
   → Следуйте **QUICK_DEPLOY.md**  
   → Или используйте скрипт **deploy.sh**

3. **Для production:**  
   → См. **DEPLOYMENT_GUIDE.md**

---

## 📞 Если нужна помощь

### Не можете найти кнопку экспорта?
→ Попробуйте меню, правый клик, или обратитесь в поддержку Figma Make

### Экспорт не включает конфиги?
→ Это нормально! Скопируйте шаблоны из **EXPORT_PROJECT.md**

### npm install выдаёт ошибки?
→ Проверьте версию Node.js: `node --version` (нужна v18+)

---

**Начните с поиска кнопки экспорта в Figma Make! 🔍**

Если не найдёте - следуйте **EXPORT_PROJECT.md** для ручного копирования.
