# Отладка проблемы с добавлением слов

## Проблема
Слова подтверждаются как добавленные, но фактически не появляются в колоде.

## Добавленное логирование

### 1. В `deck.service.ts` (строки 95-109)
```
📤 API request: addWordsToDeck - что отправляется на backend
📥 API response: addWordsToDeck - что возвращает backend
📊 Response words array - массив слов в ответе
```

### 2. В `DeckEditorPage.tsx` (строки 103-111)
```
🔵 Adding words to deck - начало операции
🟢 Updated deck received - полученная колода
🟢 Words in updated deck - массив слов
🟢 Words count - количество слов
```

### 3. В `DeckEditorPage.tsx` (строки 45-55)
```
🔄 Deck state updated - изменение состояния deck
```

### 4. В `WordsTable.tsx` (строки 47-51)
```
📋 WordsTable render - что получила таблица для отображения
```

## Как отлаживать

1. Откройте консоль браузера (F12 → Console)
2. Попробуйте добавить слово в колоду
3. Найдите в консоли логи с эмоджи (🔵, 📤, 📥, 🟢, 🔄, 📋)
4. Проверьте:
   - Отправляются ли правильные данные (📤)
   - Что возвращает backend (📥)
   - Обновляется ли состояние deck (🔄)
   - Получает ли таблица новые данные (📋)

## Возможные причины проблемы

### 1. Backend не возвращает обновленную колоду
**Симптом:** В логе 📥 не видно массива `words` или он пустой
**Решение:** Проблема на backend, нужно проверить Django view

### 2. Backend возвращает неправильную структуру
**Симптом:** В логе 📥 есть слова, но в другом формате (например, `word` вместо `original_word`)
**Решение:** Нужно добавить маппинг данных в frontend или исправить serializer на backend

### 3. Состояние не обновляется
**Симптом:** В логе 📥 все правильно, но 🔄 не срабатывает или показывает старые данные
**Решение:** Проблема с React state, проверить setDeck()

### 4. WordsTable не получает данные
**Симптом:** 🔄 показывает правильные данные, но 📋 показывает пустой массив
**Решение:** Проблема с пропсами, проверить передачу deck.words

## Следующие шаги

После получения логов из консоли, мы сможем точно определить где именно происходит сбой и исправить проблему.
