#!/bin/bash

##############################################
# 📁 Скрипт организации структуры проекта
# Перемещает все файлы фронтенда в /frontend/
##############################################

GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

echo -e "${GREEN}╔═══════════════════════════════════════╗${NC}"
echo -e "${GREEN}║  📁 Организация структуры проекта     ║${NC}"
echo -e "${GREEN}╚═══════════════════════════════════════╝${NC}"
echo ""

# Создаём папку frontend
echo -e "${BLUE}[1/3]${NC} Создание папки /frontend/..."
mkdir -p frontend

# Список директорий для перемещения
DIRS=(
    "components"
    "contexts"
    "hooks"
    "lib"
    "locales"
    "pages"
    "services"
    "styles"
    "types"
    "utils"
)

# Перемещаем директории
echo -e "${BLUE}[2/3]${NC} Перемещение папок..."
for dir in "${DIRS[@]}"; do
    if [ -d "$dir" ]; then
        echo -e "${YELLOW}  ↗️  $dir → frontend/$dir${NC}"
        mv "$dir" frontend/
    fi
done

# Перемещаем отдельные файлы проекта
echo -e "${BLUE}[3/3]${NC} Перемещение файлов..."

FILES=(
    "App.tsx"
    ".env.production"
)

for file in "${FILES[@]}"; do
    if [ -f "$file" ]; then
        echo -e "${YELLOW}  ↗️  $file → frontend/$file${NC}"
        mv "$file" frontend/
    fi
done

# Создаём README для frontend
echo -e "${BLUE}[4/4]${NC} Создание README..."
cat > frontend/README.md << 'EOF'
# 🎨 Anki Generator - Frontend

React + TypeScript + Vite приложение для генерации ANKI карточек.

## 🚀 Быстрый старт

### Разработка

```bash
npm install
npm run dev
```

Приложение откроется на `http://localhost:3000`

### Production сборка

```bash
npm run build
```

Создаст папку `dist/` с оптимизированными файлами.

## 📁 Структура проекта

```
frontend/
├── App.tsx                 # Главный компонент
├── components/             # React компоненты
│   ├── ui/                # UI компоненты (shadcn/ui)
│   └── ...
├── contexts/              # React контексты
├── hooks/                 # Custom hooks
├── lib/                   # Утилиты и конфигурация
├── locales/               # Переводы (7 языков)
├── pages/                 # Страницы приложения
├── services/              # API сервисы
├── styles/                # CSS стили
├── types/                 # TypeScript типы
└── utils/                 # Вспомогательные функции
```

## 🌍 Локализация

Поддерживаются языки:
- 🇷🇺 Русский (ru)
- 🇬🇧 Английский (en)
- 🇵🇹 Португальский (pt)
- 🇩🇪 Немецкий (de)
- 🇪🇸 Испанский (es)
- 🇫🇷 Французский (fr)
- 🇮🇹 Итальянский (it)

## 🎨 Дизайн

- **Оптимизировано для:** iPhone 17 Air, iOS 25
- **Цветовая схема:** 
  - Голубой: #4FACFE
  - Розовый: #FF6B9D
  - Желтый: #FFD93D
- **UI библиотека:** shadcn/ui + Radix UI
- **Стили:** Tailwind CSS v4

## 🔧 Технологии

- **Framework:** React 18.3
- **Language:** TypeScript 5.4
- **Build Tool:** Vite 5.1
- **Router:** React Router v6
- **Styling:** Tailwind CSS v4
- **Icons:** Lucide React
- **Forms:** React Hook Form
- **Charts:** Recharts
- **Notifications:** Sonner

## 🌐 API

Backend: Django REST Framework  
Development: `https://get-anki.fan.ngrok.app`  
Production: `/api` (тот же домен)

## 📦 Деплой

См. корневую документацию:
- `QUICK_DEPLOY.md` - Быстрый деплой
- `DEPLOYMENT_GUIDE.md` - Полное руководство
- `deploy.sh` - Автоматический скрипт

## 📄 Лицензия

См. корневой LICENSE файл
EOF

echo ""
echo -e "${GREEN}╔═══════════════════════════════════════╗${NC}"
echo -e "${GREEN}║      ✅ Организация завершена!        ║${NC}"
echo -e "${GREEN}╚═══════════════════════════════════════╝${NC}"
echo ""
echo -e "${YELLOW}📋 Структура проекта:${NC}"
echo -e "  ${BLUE}frontend/${NC}              # ← Весь код фронтенда"
echo -e "  ${BLUE}├── components/${NC}"
echo -e "  ${BLUE}├── contexts/${NC}"
echo -e "  ${BLUE}├── pages/${NC}"
echo -e "  ${BLUE}└── ...${NC}"
echo ""
echo -e "${YELLOW}📝 Документация осталась в корне:${NC}"
echo -e "  ${BLUE}QUICK_DEPLOY.md${NC}"
echo -e "  ${BLUE}DEPLOYMENT_GUIDE.md${NC}"
echo -e "  ${BLUE}deploy.sh${NC}"
echo ""
echo -e "${GREEN}🎉 Готово! Теперь можно коммитить в Git${NC}"
