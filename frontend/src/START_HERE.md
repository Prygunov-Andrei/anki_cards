# 🎯 Начните здесь - Организация проекта для Git

## Что нужно сделать

Вы хотите перенести все файлы фронтенда в папку `/frontend/` для добавления в Git репозиторий.

---

## ⚡ Быстрые шаги

### Шаг 1: Организуйте структуру

```bash
# Сделайте скрипт исполняемым
chmod +x organize_frontend.sh

# Запустите
./organize_frontend.sh
```

**Что произойдёт:**
- ✅ Создастся папка `/frontend/`
- ✅ Все файлы кода переместятся в `/frontend/`
- ✅ Документация останется в корне
- ✅ Создастся `/frontend/README.md`

### Шаг 2: Проверьте структуру

```bash
# Должно быть так:
ls -la

# Результат:
# frontend/              ← Весь код фронтенда
# README.md              ← Главный README
# QUICK_DEPLOY.md        ← Документация
# deploy_from_frontend.sh ← Скрипт деплоя
# organize_frontend.sh    ← Скрипт организации
# .gitignore             ← Git игнор файл
# ...
```

### Шаг 3: Добавьте в Git

```bash
# Инициализируйте репозиторий (если ещё не сделали)
git init

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit: Organized frontend structure"

# Добавьте удалённый репозиторий
git remote add origin https://github.com/your-username/anki-generator.git

# Отправьте в репозиторий
git push -u origin main
```

---

## 📁 Итоговая структура для Git

```
your-repo/
├── frontend/                    # ← Весь фронтенд код
│   ├── App.tsx
│   ├── components/
│   ├── contexts/
│   ├── pages/
│   ├── services/
│   ├── locales/
│   ├── lib/
│   ├── styles/
│   ├── types/
│   ├── utils/
│   ├── hooks/
│   ├── .env.production
│   └── README.md
│
├── backend/                     # ← Ваш Django бэкенд (добавите позже)
│   └── ...
│
├── docs/                        # ← Можно создать для документации
│   ├── QUICK_DEPLOY.md
│   ├── DEPLOYMENT_GUIDE.md
│   └── ...
│
├── .gitignore                   # ← Git ignore
├── README.md                    # ← Главный README
├── deploy_from_frontend.sh      # ← Скрипт деплоя
└── organize_frontend.sh         # ← Скрипт организации
```

---

## 🎯 Что дальше?

### Для разработки

```bash
cd frontend
npm install
npm run dev
```

### Для деплоя на Django

```bash
# 1. Отредактируйте deploy_from_frontend.sh
nano deploy_from_frontend.sh
# Укажите: DJANGO_PATH="/путь/к/django"

# 2. Запустите
chmod +x deploy_from_frontend.sh
./deploy_from_frontend.sh
```

---

## 📚 Полезные файлы

После организации читайте:

- **README.md** - Обзор всего проекта
- **frontend/README.md** - Документация фронтенда
- **QUICK_DEPLOY.md** - Инструкция по деплою
- **DEPLOYMENT_GUIDE.md** - Детальное руководство

---

## ✅ Чеклист

- [ ] Запустил `./organize_frontend.sh`
- [ ] Проверил что все файлы в `/frontend/`
- [ ] Создал Git репозиторий (`git init`)
- [ ] Добавил файлы (`git add .`)
- [ ] Сделал коммит (`git commit`)
- [ ] Отправил в удалённый репозиторий (`git push`)

---

**Начните с команды: `./organize_frontend.sh` 🚀**
