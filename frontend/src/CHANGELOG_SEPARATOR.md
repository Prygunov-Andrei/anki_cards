# 📋 Changelog: Замена разделителя с запятой на точку с запятой

## 🎯 Цель изменения

Изменить разделитель для парсинга слов с **запятой (`,`)** на **точку с запятой (`;`)** во всех компонентах ввода слов.

## ✅ Выполненные изменения

### 1. **Компонент WordChipsInput** (`/components/WordChipsInput.tsx`)

**Изменено:**
- ✅ Комментарии: "запятой" → "точкой с запятой"
- ✅ Логика разбиения: `char === ','` → `char === ';'`
- ✅ Обработка клавиш: `e.key === ','` → `e.key === ';'`
- ✅ Обработка вставки: `includes(',')` → `includes(';')`

**До:**
```typescript
// Запятая вне скобок - это разделитель
if (char === ',' && depth === 0) { ... }

// Enter или запятая - добавить слово
if (e.key === 'Enter' || e.key === ',') { ... }
```

**После:**
```typescript
// Точка с запятой вне скобок - это разделитель
if (char === ';' && depth === 0) { ... }

// Enter или точка с запятой - добавить слово
if (e.key === 'Enter' || e.key === ';') { ... }
```

---

### 2. **Компонент WordInput** (`/components/WordInput.tsx`)

**Изменено:**
- ✅ Регулярное выражение: `.split(/[,\n]+/)` → `.split(/[;\n]+/)`
- ✅ Placeholder текст: "через запятую" → "через точку с запятой"
- ✅ Подсказка: "запятыми" → "точкой с запятой"

**До:**
```typescript
// Разделяем по запятым или переносам строки
const words = text.split(/[,\n]+/)

placeholder="Введите слова через запятую или с новой строки"
```

**После:**
```typescript
// Разделяем по точкам с запятой или переносам строки
const words = text.split(/[;\n]+/)

placeholder="Введите слова через точку с запятой или с новой строки"
```

---

### 3. **Файлы локализации** (все 7 языков)

#### 🇷🇺 Русский (`/locales/ru.ts`)
```diff
- commaSeparated: '(через запятую, новую строку или Enter)',
+ commaSeparated: '(через точку с запятой, новую строку или Enter)',
```

#### 🇬🇧 Английский (`/locales/en.ts`)
```diff
- commaSeparated: '(comma, newline or Enter)',
+ commaSeparated: '(semicolon, newline or Enter)',
```

#### 🇩🇪 Немецкий (`/locales/de.ts`)
```diff
- commaSeparated: '(Komma, Neue Zeile oder Enter)',
+ commaSeparated: '(Semikolon, Neue Zeile oder Enter)',
```

#### 🇪🇸 Испанский (`/locales/es.ts`)
```diff
- commaSeparated: '(coma, salto de línea o Enter)',
+ commaSeparated: '(punto y coma, salto de línea o Enter)',
```

#### 🇵🇹 Португальский (`/locales/pt.ts`)
```diff
- commaSeparated: '(vírgula, quebra de linha ou Enter)',
+ commaSeparated: '(ponto e vírgula, quebra de linha ou Enter)',
```

#### 🇫🇷 Французский (`/locales/fr.ts`)
```diff
- commaSeparated: '(virgule, saut de ligne ou Entrée)',
+ commaSeparated: '(point-virgule, saut de ligne ou Entrée)',
```

#### 🇮🇹 Итальянский (`/locales/it.ts`)
```diff
- commaSeparated: '(virgola, a capo o Invio)',
+ commaSeparated: '(punto e virgola, a capo o Invio)',
```

---

## 🔍 Затронутые компоненты

### Прямое влияние:
1. ✅ `WordChipsInput.tsx` - основной компонент ввода слов с чипсами
2. ✅ `WordInput.tsx` - textarea для ввода слов
3. ✅ Все 7 файлов локализации (ru, en, de, es, pt, fr, it)

### Косвенное влияние:
1. `SmartWordInput.tsx` - использует WordChipsInput
2. `MainPage.tsx` - использует WordChipsInput
3. `DeckEditorPage.tsx` - использует SmartWordInput

---

## 📝 Примеры использования

### До изменений:
```
hello, world, test
Привет, мир, тест
```

### После изменений:
```
hello; world; test
Привет; мир; тест
```

---

## ✨ Дополнительные возможности (сохранены)

### 1. Умное разбиение со скобками
Точки с запятой внутри скобок **игнорируются**:

```
der Hund; rennen (rannte; ist gerannt); das Buch
```
Результат: 3 слова
- `der Hund`
- `rennen (rannte; ist gerannt)` ← точка с запятой внутри скобок сохранена
- `das Buch`

### 2. Разбиение по переносам строк
Работает как раньше:
```
hello
world
test
```

### 3. Обработка вставки из буфера
При вставке текста с точками с запятой или переносами - автоматически разбивает на слова.

---

## ⚠️ Важные замечания

### Запятые остаются допустимыми внутри слов/фраз:
```typescript
const validPattern = /^[\p{L}\p{N}\s\-'äöüßÄÖÜàèéêëîïôùûüÿçñ()[\]{}/–—.,?!…:;]+$/u;
```

Примеры допустимых вводов с запятыми **внутри** слов:
- ✅ `Hello, world!` (фраза с запятой)
- ✅ `Wie geht's dir?` (предложение)
- ✅ `a, b, c, d` (буквы с запятыми)

Но **разделение** происходит только по точке с запятой:
```
Hello, world!; How are you?; Good morning
```
Результат: 3 фразы

---

## 🧪 Тестирование

### Рекомендованные сценарии тестирования:

1. **Базовый ввод с точкой с запятой:**
   ```
   word1; word2; word3
   ```

2. **Ввод с переносами строк:**
   ```
   word1
   word2
   word3
   ```

3. **Смешанный ввод:**
   ```
   word1; word2
   word3
   ```

4. **Немецкие слова с артиклями (скобки):**
   ```
   der Hund; rennen (rannte; ist gerannt); das Buch
   ```

5. **Фразы с запятыми внутри:**
   ```
   Hello, how are you?; Good morning, sir; Thank you, very much
   ```

6. **Нажатие клавиши `;` на клавиатуре** - должно добавить слово
7. **Вставка из буфера** текста с точками с запятой

---

## ✅ Результат

Теперь пользователи должны использовать **точку с запятой (`;`)** для разделения слов, что:
- ✅ Позволяет вводить фразы и предложения с запятыми
- ✅ Улучшает UX для ввода целых предложений
- ✅ Сохраняет умное разбиение со скобками
- ✅ Поддерживается во всех 7 языках интерфейса

---

## 📅 Дата изменения

12 декабря 2024 года
