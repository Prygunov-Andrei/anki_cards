# ANKI Flashcard Generator - Frontend

React + Vite фронтенд для генерации карточек Anki.

## 🚀 Быстрый старт

### Требования

- Node.js 18+ и npm/yarn/pnpm

### Установка зависимостей

```bash
cd frontend
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `build/`.

---

## ⚙️ Конфигурация

### API Backend URL

URL бэкенда настраивается в файле `src/lib/config.ts`:

```typescript
export const API_BASE_URL = 
  (typeof import.meta !== 'undefined' && import.meta.env?.VITE_API_BASE_URL) 
  || 'https://get-anki.fan.ngrok.app';
```

### Переменные окружения

Создайте файл `.env` в папке `frontend/` для локальной разработки:

```env
VITE_API_BASE_URL=http://localhost:8000
```

Или используйте переменную окружения при запуске:

```bash
VITE_API_BASE_URL=http://localhost:8000 npm run dev
```

---

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── components/      # React компоненты
│   │   ├── ui/          # UI компоненты (Radix UI)
│   │   └── ...
│   ├── pages/           # Страницы приложения
│   ├── contexts/        # React Context провайдеры
│   ├── hooks/           # Custom React hooks
│   ├── lib/             # Утилиты и конфигурация
│   │   ├── api.ts       # API клиент
│   │   └── config.ts    # Конфигурация API
│   ├── services/        # Сервисы (API вызовы)
│   ├── locales/         # Локализация (i18n)
│   ├── types/           # TypeScript типы
│   └── styles/          # Глобальные стили
├── package.json
├── vite.config.ts
└── index.html
```

---

## 🔧 Технологии

- **React 18** - UI библиотека
- **Vite** - Сборщик и dev-сервер
- **TypeScript** - Типизация
- **React Router** - Маршрутизация
- **Radix UI** - UI компоненты
- **Tailwind CSS** - Стилизация
- **Axios** - HTTP клиент

---

## 📝 Документация

Подробная документация по разработке фронтенда находится в:

- **[Техническое задание Frontend](../docs/FRONTEND_TASKS.md)** - Полное ТЗ с этапами разработки
- **[План разработки](../docs/DEVELOPMENT_PLAN.md)** - Общий план разработки проекта

---

## 🐛 Отладка

### Проблемы с подключением к бэкенду

1. Проверьте, что бэкенд запущен на `http://localhost:8000`
2. Проверьте настройки CORS в бэкенде
3. Проверьте URL в `src/lib/config.ts`

### Проблемы с зависимостями

```bash
# Очистить кэш и переустановить
rm -rf node_modules package-lock.json
npm install
```

---

## 📦 Деплой

См. [Документацию по деплою](../docs/DEPLOYMENT.md)

---

**Последнее обновление:** 7 декабря 2025
