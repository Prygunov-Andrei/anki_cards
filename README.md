# Anki Card Generator

[![GitHub](https://img.shields.io/badge/GitHub-Repository-blue)](https://github.com/Prygunov-Andrei/anki_cards)

## Описание

**Anki Card Generator** — веб-приложение для автоматической генерации карточек Anki для изучения иностранных языков. Приложение позволяет создавать двусторонние карточки с переводами, аудио и изображениями, которые можно импортировать в популярное приложение для запоминания Anki.

## Для чего нужна программа

Приложение предназначено для небольшой группы пользователей (5-7 человек), изучающих португальский или немецкий языки. Оно упрощает процесс создания карточек Anki, позволяя быстро генерировать колоды с переводами, аудио и изображениями.

## Основные возможности

### Текущая версия (Этапы 1-4 завершены ✅)

✅ **Ввод слов**
- Ввод списка слов через запятую (например: `casa, palavra, livro`)
- Поддержка португальского (`pt`) и немецкого (`de`) языков
- Автоматическое определение языка из профиля пользователя
- Визуальное отображение добавленных слов с возможностью удаления

✅ **Управление переводами**
- Ручной ввод переводов на русский язык для каждого слова
- Валидация обязательных полей
- Динамический интерфейс для ввода переводов

✅ **Генерация карточек Anki**
- Автоматическое создание `.apkg` файлов (формат колод Anki)
- Двусторонние карточки:
  - Иностранное слово → Русский перевод
  - Русский перевод → Иностранное слово
- Поддержка медиафайлов (аудио и изображения) - готово к использованию

✅ **Управление словами**
- Сохранение всех введенных слов в базе данных
- Проверка на дубликаты
- История созданных слов
- Фильтрация по языку и поиск через API

✅ **Пользовательские аккаунты**
- Регистрация и авторизация
- Персональные настройки (предпочитаемый язык)
- Приватное хранение слов каждого пользователя
- Автоматическое сохранение выбранного языка в профиле

✅ **Веб-интерфейс**
- Современный UI на React + TypeScript + TailwindCSS
- Адаптивный дизайн
- Интуитивно понятные формы
- Автоматическое скачивание сгенерированных файлов

### Планируемые возможности (Этап 5)

🚀 **Загрузка медиафайлов**
- Загрузка аудиофайлов (MP3) через веб-интерфейс
- Загрузка изображений (JPG, PNG) через веб-интерфейс
- Предпросмотр загруженных файлов

🚀 **Автоматизация (будущее)**
- Автоматический перевод через API (Google Translate, DeepL)
- Автоматическая генерация аудио через TTS (Text-to-Speech)
- Автоматический поиск/генерация изображений

🚀 **Дополнительные функции (будущее)**
- Выбор из ранее сохраненных слов
- История создания колод
- Статистика использования слов
- Расширенная настройка шаблонов карточек

## Как это работает

### Процесс создания карточек

1. **Ввод данных**
   - Пользователь вводит слова через запятую
   - Выбирает язык (португальский или немецкий)
   - Вводит переводы для каждого слова
   - Загружает аудиофайлы и изображения (опционально)

2. **Обработка на сервере**
   - Проверка на дубликаты в базе данных
   - Сохранение слов в персональную базу пользователя
   - Генерация `.apkg` файла с использованием библиотеки `genanki`
   - Упаковка медиафайлов в архив

3. **Скачивание и использование**
   - Автоматическое скачивание сгенерированного файла
   - Импорт `.apkg` файла в приложение Anki
   - Начало изучения слов по методу интервального повторения

### Структура карточек

Каждая карточка содержит:
- **Исходное слово** (португальское или немецкое)
- **Перевод** на русский язык
- **Аудио** произношения (опционально)
- **Изображение** для визуального запоминания (опционально)

Из каждой записи создаются **две карточки**:
- Карточка 1: Показывается иностранное слово → нужно вспомнить перевод
- Карточка 2: Показывается перевод → нужно вспомнить иностранное слово

## Технические детали

### Технологический стек

**Backend:**
- Python 3.10+
- Django 4.x — веб-фреймворк
- Django REST Framework — API
- PostgreSQL — база данных
- genanki — генерация `.apkg` файлов

**Frontend:**
- React 18+ с TypeScript
- TailwindCSS — стилизация
- Axios — HTTP-клиент

### Формат файлов Anki

- **`.apkg` файл** — это ZIP-архив, содержащий:
  - Базу данных SQLite с записями и карточками
  - Папку `media/` с аудио и изображениями
  - Метаданные колоды (название, описание, настройки)

- **Один `.apkg` файл = одна колода карточек**
- Колода может содержать множество карточек
- Файл можно импортировать в Anki Desktop, AnkiMobile или AnkiWeb

## Преимущества

- ⚡ **Быстрое создание** — генерация колоды за секунды
- 🎯 **Двусторонние карточки** — изучение в обе стороны
- 🎨 **Мультимедиа** — поддержка аудио и изображений
- 💾 **История** — все слова сохраняются в базе данных
- 🔒 **Приватность** — персональные данные каждого пользователя
- 🚀 **Масштабируемость** — готовность к автоматизации в будущем

## Особенности

- 🆓 **Бесплатное использование** — приложение полностью бесплатно
- 👥 **Для небольшой группы** — разработано для личного использования (5-7 пользователей)
- 🎨 **Простой интерфейс** — минималистичный фронтенд для быстрой работы

## Статус разработки

### ✅ Завершенные этапы

- **Этап 1**: Настройка проекта и инфраструктуры ✅
- **Этап 2**: Backend - Модели данных и аутентификация ✅
- **Этап 3**: Backend - Генерация карточек Anki ✅
- **Этап 4**: Frontend - Базовый интерфейс ✅

### 🚧 В разработке

- **Этап 5**: Frontend - Загрузка медиафайлов и улучшения UX

### 📋 Планируется

- **Этап 6**: Интеграция, тестирование и оптимизация
- **Этап 7**: Деплой и финализация

---

## Документация для разработчиков

### Требования

- Python 3.10 или выше
- Node.js 18+ и npm
- PostgreSQL 12+
- Git

### Установка и настройка

#### 1. Клонирование репозитория

```bash
git clone https://github.com/Prygunov-Andrei/anki_cards.git
cd anki_cards
```

#### 2. Настройка Backend (Django)

```bash
cd backend

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

#### 3. Настройка базы данных PostgreSQL

```bash
# Создание базы данных
createdb anki_db

# Или через psql
psql -U postgres
CREATE DATABASE anki_db;
```

#### 4. Настройка переменных окружения

Создайте файл `.env` в папке `backend/`:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/anki_db
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000
```

#### 5. Применение миграций

```bash
cd backend
python manage.py migrate
python manage.py createsuperuser  # Создание администратора (опционально)
```

#### 6. Настройка Frontend (React)

```bash
cd frontend
npm install
```

#### 7. Запуск приложения

**Быстрый запуск (рекомендуется):**
```bash
./start.sh
```

Скрипт автоматически:
- Проверит и установит зависимости
- Применит миграции
- Запустит backend на http://localhost:8000
- Запустит frontend на http://localhost:3000

**Ручной запуск:**

Backend (в одном терминале):
```bash
cd backend
source venv/bin/activate
python manage.py runserver
```

Frontend (в другом терминале):
```bash
cd frontend
npm start
```

Приложение будет доступно по адресу `http://localhost:3000`

### Структура проекта

```
anki-card-generator/
├── backend/                 # Django проект
│   ├── manage.py
│   ├── requirements.txt
│   ├── .env                 # Переменные окружения (не в git)
│   ├── config/              # Настройки Django
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── apps/
│   │   ├── users/           # Модель пользователя и аутентификация
│   │   ├── words/           # Модель слов и логика обработки
│   │   └── cards/           # Генерация .apkg файлов
│   └── media/               # Временное хранение медиафайлов
│
├── frontend/                # React приложение
│   ├── src/
│   │   ├── components/
│   │   │   ├── WordInput.tsx
│   │   │   ├── LanguageSelector.tsx
│   │   │   └── GenerateButton.tsx
│   │   ├── services/
│   │   │   └── api.ts
│   │   └── App.tsx
│   └── package.json
│
├── docs/                     # Документация
│   ├── plan.md              # План разработки
│   ├── development-plan.md  # Детальный план
│   ├── setup.md             # Инструкция по настройке
│   └── ...
├── start.sh                  # Скрипт запуска приложения
└── README.md                 # Этот файл
```

### API Endpoints

#### Аутентификация

- `POST /api/auth/register/` — Регистрация нового пользователя
- `POST /api/auth/login/` — Вход пользователя

#### Профиль пользователя

- `GET /api/user/profile/` — Получение профиля (требует аутентификации)
- `PATCH /api/user/profile/` — Обновление профиля (требует аутентификации)

#### Работа со словами

- `GET /api/words/list/` — Список всех слов пользователя
  - Query params: `?language=pt|de&search=...`

#### Генерация карточек

- `POST /api/cards/generate/` — Генерация .apkg файла (требует аутентификации)
- `GET /api/cards/download/<file_id>/` — Скачивание сгенерированного файла

Подробное описание API смотрите в [документации](./docs/api.md).

### Разработка

#### Запуск тестов

```bash
cd backend
python manage.py test
```

#### Создание миграций

```bash
cd backend
python manage.py makemigrations
python manage.py migrate
```

#### Сборка Frontend для production

```bash
cd frontend
npm run build
```

### Зависимости

**Backend (requirements.txt):**
```
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
psycopg2-binary==2.9.9
genanki==0.13.0
python-dotenv==1.0.0
```

**Frontend (package.json):**
- React 18+
- TypeScript
- TailwindCSS
- Axios

### Полезные ссылки

- [Anki Documentation](https://docs.ankiweb.net/)
- [genanki Library](https://github.com/kerrickstaley/genanki)
- [Django REST Framework](https://www.django-rest-framework.org/)
- [React Documentation](https://react.dev/)

---

*Приложение находится в активной разработке.*

